# Teach actionlint about the self-hosted runner label so
# `runs-on: [self-hosted, 4-lom]` doesn't error "unknown label". This repo only
# references the shared runner pool; it does not own or register it.
self-hosted-runner:
  labels:
    - 4-lom
