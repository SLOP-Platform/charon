# E7 — README work-engine docs pass

Final user-facing docs for the shipped work-engine (ADR-0010). README.md only; docs-only.

## Decisions / scope
- Kept **gateway-first** primary (D001): the gateway sections lead; the engine is framed
  as an opt-in consumer that never touches the gateway request path.
- Replaced the thin "Autonomous mode (optional)" blurb with a "Work engine (opt-in)"
  section describing **only shipped code**, verified against:
  - `cli.py` `work` subcommand + `run_work` (board → scheduler → fenced `coordinator.run`
    → per-unit `land_unit` → `validate_product`); `--units`, `--backend`, `--acp-cmd`,
    `--autonomy` (default L1), `--sandbox`, `--capacity-policy`.
  - `land.py` — propose-default gate; auto-land (`allowlist`/`scanners_required`,
    ADR-0012) is present but `run_work` reports proposals only (`auto_land` read
    generically; trust-extending behavior stays gated, D005/D006).
  - `intake.py` — Phase 1 (human-reviewed, data-only) and Phase 2 `autonomous_intake`
    (`enabled=False` default, confidence gate, falls back to proposal).
  - `engine/capacity.py` — FixedCap default; AIMD opt-in/gated (D007).
- Sandbox policy table per **D013**; container-as-trust-boundary per **D012**.
- Noted the native engine supersedes the `charon-private/fleet/` dev harness (D003) and
  flagged **D015** verified isolation as still-gated/future (Open in DECISIONS).

## No aspirational features
Every claim maps to shipped code or an Accepted/Settled decision; the lone "future" item
(D015) is labelled as gated.
