# RIG-STATE-HYGIENE — review-log fragment

**Ticket:** RIG-STATE-HYGIENE
**Branch:** chore/rig-state-hygiene
**Date:** 2026-07-15
**Author:** (droid)

## What was produced

Three small rig-hygiene fixes consolidated:

1. **`fleet/reds.tsv` in `.gitignore`** — auto-gen REDS registry was perpetually dirty (tracked, regenerated by build-rig). Added to `.gitignore` + `git rm --cached`.

2. **`__pycache__/` + `*.pyc`** — already present in `.gitignore` (lines 23-24). No tracked `.pyc` files found; no additional action needed.

3. **`fleet/state/CONFIG-SOURCES.tsv`** — created the config-drift source registry with two rows: `local` (~/.charon) and `4lom` (CG gateway via ssh). Added `!`-negation in `.gitignore` so this tracked file survives the `fleet/state/*` gitignore rule.

## Scope self-check

Changed files (all within `owns:` or review-log):
- `.gitignore` — owned
- `fleet/state/CONFIG-SOURCES.tsv` — owned
- `docs/review-log/RIG-STATE-HYGIENE.md` — this fragment

No out-of-scope files touched.
