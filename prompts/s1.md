Implement D013 (docs/DECISIONS.md) — make the worker SANDBOX POSTURE a user-selectable
policy. Read docs/DECISIONS.md (D013 + D012), docs/adr/0009-l3-unattended-autonomy-
escalation-gate.md, and src/charon/fence.py (`AutonomyPolicy` / `assert_environment` /
`scrubbed_env`) FIRST. This modifies the autonomy/security gate — land a short plan note in
docs/REVIEW-LOG.md BEFORE code (house rule).

GOAL: expose a `sandbox` policy the user selects, mapping onto the EXISTING fence env-flag
mechanism — WITHOUT changing default behavior.

BUILD:
1. src/charon/config.py — add a `sandbox` setting, enum `hybrid` (default) | `container` |
   `host`, readable from charon.toml + env (reuse the existing config-load path; no new dep).
2. src/charon/fence.py — `AutonomyPolicy` honors the policy:
   - `hybrid` = EXACTLY today's behavior (host OK ≤L1 / behind the loud override; container
     required for L2+). This must be byte-for-byte the current default — prove with a
     regression test.
   - `container` = require `CHARON_CONTAINER_VERIFIED` for ALL units; the uncontained
     override path is refused.
   - `host` = allow uncontained behind the existing loud override flags.
   ADDITIVE ONLY: do NOT weaken any existing gate, do NOT remove the loud override /
   unattended flags, do NOT change `scrubbed_env`.
3. src/charon/cli.py — surface `--sandbox <policy>` and show the active policy in
   `doctor`/diagnostics.

Tests (proven-red): extend tests/test_fence.py + tests/test_config.py — each policy's gate
behavior; `hybrid` == current default (regression); `container` refuses uncontained L2 even
WITH the override; `host` requires the loud override.

CONSTRAINTS: own ONLY src/charon/config.py, src/charon/fence.py, src/charon/cli.py (+ extend
tests/test_fence.py, tests/test_config.py, + append a docs/REVIEW-LOG.md plan note). This is
the SECURITY/autonomy gate — be conservative, additive, fully tested; the `hybrid` default
must reproduce current behavior exactly. Stdlib-only core. Gate green every commit (pytest,
ruff check, mypy src/charon, check_boundary.py src, check_version.py). No secrets.
Conventional commits. Open a DRAFT PR base=master; do NOT merge.
