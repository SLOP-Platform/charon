You are working on the Charon repo (SLOP-Platform/charon) on branch feat/docker-default-gateway.

Make the default container command run `charon gateway`: update the Dockerfile CMD, the docker-compose.yml gateway service, and the README Docker section to match. No ADR needed.

Own ONLY Dockerfile, docker-compose.yml, and README.md (Docker section) — never touch other files. Keep the gate green: pytest, ruff check, mypy src/charon, python3 tools/check_boundary.py src, python3 tools/check_version.py. No secrets in the repo. Conventional commits. Open a draft PR; do not merge.
