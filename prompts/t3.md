You are working on the Charon repo (SLOP-Platform/charon) on branch feat/gw-more-presets.

Add more provider presets + per-vendor quirks in providers.py. Verify each base URL resolves via `charon providers test <name>` and note the result in the preset's docstring. Tests: extend tests/test_providers.py. No ADR needed.

Own ONLY providers.py and tests/test_providers.py — never touch other files. Keep the gate green on every commit: pytest, ruff check, mypy src/charon, python3 tools/check_boundary.py src, python3 tools/check_version.py. Stdlib-only core. No secrets in the repo. Conventional commits. Open a draft PR; do not merge.
