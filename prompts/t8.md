You are working on the Charon repo (SLOP-Platform/charon) on branch feat/consensus-breaker, cut from a master that already contains the merged T1 (PERF-4) changes.

Implement a real consensus reviewer adapter + a circuit breaker on repeated failures. Put the breaker in failover.py (NOT coordinator.py) so this ticket stays disjoint from T7. Land a plan note in docs/REVIEW-LOG.md before code.

Own ONLY: adapters/review_mock.py (-> real), new adapters/review.py, ports/reviewer.py, and failover.py — never touch other files. Tests: extend tests/test_consensus_gate.py and tests/test_failover.py. Keep the gate green on every commit: pytest, ruff check, mypy src/charon, python3 tools/check_boundary.py src, python3 tools/check_version.py. Stdlib-only core. No keys/secrets in the repo. Conventional commits. Open a draft PR with **base = master** (`gh pr create --base master …`); do not merge.
