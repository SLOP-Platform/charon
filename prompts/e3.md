Build the intelligent scanner matrix for Charon's land gate. Read
docs/adr/0010-native-work-engine-substrate.md (D4 — the scanner matrix) and the ADR-0010
entry in docs/REVIEW-LOG.md FIRST. OPERATOR HARD REQUIREMENT: lightweight + performant —
RIGHT tools, not all tools; it must never become a throughput-killing blanket gate.

GOAL: a change-scoped, parallel, cached scanner layer wired into land.py as ADVISORY
(propose-mode). Required/fail-closed status is RESERVED for auto-land, which stays deferred —
do NOT make anything required.

BUILD:
1. src/charon/scanners.py — a pluggable scanner runner implementing ADR-0010 D4 exactly:
   - Tier A (always eligible): gitleaks (already invoked by land.py — reuse, don't duplicate).
   - Tier B (run ONLY if the unit diff touches that file-domain): ruff (Python; reuse the
     gate's ruff incl. security S-rules — zero new cost), shellcheck (only if *.sh changed),
     actionlint (only if .github/workflows/* changed).
   - Tier C (gated/dropped): semgrep = opt-in "deep scan" flag ONLY, pinned minimal local
     ruleset, NEVER `--config auto`. osv-scanner + license = OFF by default (the stdlib-only
     core has no deps to scan); feature-flag for dep-bearing repos only.
   - PERFORMANCE CONTRACT: change-scoped (skip any scanner whose file-domain isn't in the
     diff), run scanners in PARALLEL with a hard per-tool timeout, cache results by file
     content-hash, advisory-vs-required tiering. A missing optional tool = skip-with-note,
     never a hard fail in propose mode.
2. Wire scanners.py into src/charon/land.py as ADVISORY output accompanying the proposal
   (nothing required — auto-land is deferred).
3. A small benchmark harness/test measuring each scanner's wall-time on a representative
   diff (the "measured-before-required" gate, ADR-0010 D4).

Tests: tests/test_scanners.py — change-scoping (shell scanner skipped when no *.sh changed),
parallel + timeout behavior, content-hash cache hit, advisory-not-blocking in propose mode,
missing-tool graceful skip.

CONSTRAINTS: own ONLY src/charon/scanners.py, src/charon/land.py, tests/test_scanners.py.
The privileged core stays STDLIB-ONLY — scanners are invoked as external subprocesses (like
gitleaks), never imported. Do NOT touch docs/REVIEW-LOG.md (a sibling ticket owns docs this
wave). Gate green every commit (pytest, ruff, mypy src/charon, check_boundary, check_version).
No secrets. Conventional commits. Open a DRAFT PR base=master; do NOT merge.
