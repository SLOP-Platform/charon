You are working on the Charon repo (SLOP-Platform/charon) on branch feat/tier2b-worker.

Implement the web/worker split with a real POST /v1/runs: the exposed web process enqueues; a separate background worker runs the privileged loop (the exposed web process must NEVER run the loop in-process). Land a plan note (queue/worker boundary) in docs/REVIEW-LOG.md before code.

Own ONLY service/app.py and new service/worker.py — never touch other files. Tests: extend tests/test_service_api.py and tests/test_service_main.py. Keep the gate green on every commit: pytest, ruff check, mypy src/charon, python3 tools/check_boundary.py src, python3 tools/check_version.py. Stdlib-only core. No keys/secrets in the repo. Conventional commits. Open a draft PR; do not merge.
