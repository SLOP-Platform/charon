You are working on the Charon repo (SLOP-Platform/charon) on branch feat/perf4-parallel-decomposition.

Implement PERF-4 (parallel independent units) + work-decomposition. The design is ALREADY LOCKED in docs/adr/0006-parallel-units-and-work-decomposition.md plus its reconciliation in docs/REVIEW-LOG.md — implement to it, do NOT re-architect. Two binding rules: (1) parallelism is between independent units, never between stages of one ticket (the role-DAG runs sequentially within a ticket); (2) the shared budget is bounded-overshoot (<=1 in-flight checkpoint per unit) via atomic claim-then-spend under one lock. Carry CONC-1..4 and run the pre-code globals audit named in the ADR.

Own ONLY these files (never touch others): coordinator.py, api.py, cli.py, router.py, ledger.py, new parallel.py (run_parallel bounded pool), new decompose.py (Triage->...->Close role DAG). Tests: tests/test_parallel.py, tests/test_decompose.py, extend test_coordinator.py (proven-red first, per PLAN-tier4 section 5).

Build order: guard_dir nesting -> race-free budget -> backend/lock isolation -> run_parallel -> decompose.py -> parallel+L2. Keep the gate green on every commit: pytest, ruff check, mypy src tests, python3 tools/check_boundary.py src, python3 tools/check_version.py. Privileged core stays stdlib-only. No keys/secrets in the repo. Conventional commits. Open a draft PR; do not merge.

## REPORT BACK — MECHANIZED FORMAT (required)
End your session by emitting EXACTLY this block. Fixed fields, one line each, no diffs or logs.
Validate it before you finish: `bash /home/stack/charon-private/fleet/check-session-report.sh <file>`
Full spec + rationale: `/home/stack/charon-private/fleet/SESSION-REPORT-FORMAT.md`

```
=== SESSION REPORT v1 ===
TICKET:       <ticket-id>
SESSION:      <jedi-name> | <model>
STATUS:       DONE | BLOCKED | REFUSED | PARTIAL
COMMIT:       <sha> | none
FILES:        <n> changed: <paths>
OWNS-OK:      yes | NO — <file> is owned by <ticket>
GATE:         PASS | FAIL — <detail>
TESTS:        <n> passed, <n> failed, <n> skipped
RED-PROOF:    broken=<exit> green=<exit> | n/a — <why> | NOT-DONE
OBSERVABLE:   MET | DEFERRED — <what could not be observed and why>
RAN:          <what you proved by EXECUTING>
READ:         <what you concluded by READING only>
BRIEF-ERRORS: none | <what this brief got factually wrong>
BLOCKED-BY:   none | <ticket or condition>
NEXT:         <the single thing the manager should do next>
=== END REPORT ===
```
**Emit it even if you are BLOCKED or REFUSED — especially then.** A correct refusal is the most
valuable report there is; a silent exit is worth nothing. **BRIEF-ERRORS is not optional politeness**
— on 2026-07-26 sessions caught nonexistent files in an OWNS clause, a ticket whose work already
existed, and a wrong premise about `upstream_model`. The brief is wrong more often than the session is.
