You are working on the Charon repo (SLOP-Platform/charon) on branch fix/gw-downgrade-normalize.

Fix R10d downgrade normalization in proxy.py only (the classify path): use a prefix/normalized compare so an upstream returning a normalized/alias model id (e.g. provider-prefixed) is not misflagged as a downgrade. Tests: extend tests/test_proxy.py. Add a one-line note to docs/REVIEW-LOG.md (no ADR needed).

Own ONLY proxy.py and tests/test_proxy.py — never touch other files. Keep the gate green on every commit: pytest, ruff check, mypy src/charon, python3 tools/check_boundary.py src, python3 tools/check_version.py. Stdlib-only core. No secrets in the repo. Conventional commits. Open a draft PR; do not merge.
