Implement OPT-IN auto-land (ADR-0007 D5 / D006, un-gated by operator D016). This is the
HIGHEST-BLAST-RADIUS feature in Charon — it merges agent-produced code with no human at the
merge. Read docs/adr/0007-parallel-work-engine.md (D5), docs/DECISIONS.md (D006/D007/D016),
and src/charon/land.py + src/charon/scanners.py FIRST.

HOUSE RULE (mandatory): land a new **docs/adr/0012-auto-land.md** + a 3-lens adversarial
review reconciliation in docs/REVIEW-LOG.md (privilege-escalation · injection/provenance ·
fail-open) BEFORE writing code. The ticket is the primary attack surface; parallelism
multiplies one injection into N landings — design accordingly.

GOAL: an opt-in, batch-atomic auto-land path on top of the existing propose-default gate —
NEVER the default; off unless explicitly enabled.

BUILD (all safety mechanisms are REQUIRED, not optional):
1. src/charon/land.py — add an auto-land path: opt-in flag (read from config); **batch-atomic**
   (one decomposition lands as ONE all-or-nothing unit — never partial); **path-allowlist**
   (refuse writes outside declared owned_paths); **sensitive-path human-hold ALWAYS** (even on
   green — conftest/tests, .git/hooks, .github/, .claude/, build hooks, etc.); the unit's
   acceptance + tests run **in sandbox**; gitleaks leak HOLDS; and scanners (scanners.py) are
   **REQUIRED / fail-closed** when auto-land is on (flip D007). **Fail-closed everywhere**: any
   missing-but-expected check, any ambiguity → HOLD (propose), never auto-merge.
2. src/charon/config.py — add the auto-land opt-in setting (default OFF) + the allowlist/
   sensitive-path config surface.

Tests: tests/test_autoland.py — proven-red: default is propose (off); out-of-allowlist write
HOLDS; sensitive path HOLDS even on green; failing acceptance/test HOLDS; scanner finding
HOLDS (required when on); gitleaks leak HOLDS; a fully-clean in-allowlist batch auto-lands
atomically; a partial-batch failure lands NOTHING.

CONSTRAINTS: own ONLY src/charon/land.py, src/charon/config.py, tests/test_autoland.py (+ the
NEW docs/adr/0012-auto-land.md + a docs/REVIEW-LOG.md note). Import scanners.py — do NOT edit
it. Stdlib-only core (git/scanners via subprocess). Gate green every commit. No secrets.
Conventional commits. Open a DRAFT PR base=master; do NOT merge. The manager gates this one
especially hard.
