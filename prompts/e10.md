Build the AIMD capacity limiter for Charon's native work-engine (ADR-0010 / D016). Read
docs/adr/0010-native-work-engine-substrate.md (D2 scheduler + capacity seam), docs/DECISIONS.md
(D004, D007, D016), and the E2 entry in docs/REVIEW-LOG.md FIRST. Depends on E2 (merged):
src/charon/engine/scheduler.py + src/charon/engine/capacity.py already exist.

KEY FRAMING — YOU ARE AN EXTENDER, NOT A CREATOR:
E2 already shipped src/charon/engine/capacity.py as a SEAM: a `CapacityLimiter` Protocol +
a default `FixedCap` implementation + a selector the scheduler consults. Do NOT rewrite or
duplicate that seam. Your job is to ADD an adaptive implementation that plugs into it.

GOAL: an AIMD (additive-increase / multiplicative-decrease) `CapacityLimiter` that adjusts
per-tier concurrency from observed signals (success → additive increase up to a ceiling;
failure/backpressure → multiplicative decrease to a floor), selectable via config alongside
the existing FixedCap default.

BUILD:
1. src/charon/engine/capacity.py — EXTEND in place:
   - Add an `AimdCap` (or similarly named) class implementing the SAME `CapacityLimiter`
     Protocol E2 defined. Reuse the Protocol + selector; do not fork them.
   - Additive increase on success, multiplicative decrease on failure/backpressure; clamp to
     [floor, ceiling] from config; conservative defaults.
   - AIMD stays OFF by default (FixedCap remains the default) — it is trust-extending
     automation per D004; selectable via the existing config policy, not forced on.
2. tests/test_capacity.py — proven-red: AIMD increases additively on a success streak,
   decreases multiplicatively on failure, clamps at floor/ceiling, and the selector returns
   AIMD only when configured (FixedCap otherwise).

CONSTRAINTS: own ONLY the files listed in your board ticket's `owns:` line
(src/charon/engine/capacity.py, tests/test_capacity.py) — nothing else. capacity.py already
exists: EDIT it, do not recreate it. If your work needs a file outside your `owns:`, STOP and
report it — do NOT create or edit it. Import scheduler/board/config; do not edit them.
Stdlib-only core. Gate green every commit (pytest, ruff, mypy src/charon, check_boundary,
check_version). No secrets. Conventional commits. Add a docs/REVIEW-LOG.md note for any
non-trivial decision. Open a DRAFT PR base=master; do NOT merge.
