You are working on the Charon repo (SLOP-Platform/charon) on branch feat/charon-land, cut from current master.

Implement ADR-0007 D4/D6 (propose-default gated landing) + D3 (consumer-supplied units loader). Read docs/adr/0007-parallel-work-engine.md (D3/D4/D6) first. Build a new `charon land` that takes a completed unit's result (its branch + ledger, produced by the existing run_parallel/coordinator) and runs the gate, then PROPOSES — it does NOT auto-merge.

Gate (tiered, fast-first), all-green or HOLD:
- diff-scope guard: writes outside the unit's declared owned-paths → hold.
- sensitive-path HOLD (always, even on green): `.github/workflows/`, `Dockerfile`, install/setup scripts, pre-commit config, dependency manifests/lockfiles, `.git/hooks`, `conftest.py`/`tests/`, `pyproject.toml`/`setup.py`/`[project.scripts]`, `Makefile`, `.claude/`, `CODEOWNERS`, `.envrc`.
- the unit's executable acceptance check + tests.
- gitleaks if installed (advisory; fail-closed only when expected).
Default = PROPOSE: open a PR (base master) per unit / advance lkg only on a human merge. (Batch-atomic auto-land is a later opt-in — do NOT build auto-merge now.)
Also add a `--units <file>` loader (consumer-supplied units: a TOML/JSON list of {goal, accept, tier, owned_paths}) feeding the existing run path.

Own ONLY: NEW `src/charon/land.py`, `src/charon/cli.py` (the `land` subcommand + `--units` loader). Tests: NEW `tests/test_land.py`. Read ledgers/worktrees produced by N1/the existing code; do NOT modify `api.py`, `coordinator.py`, or the adapters (other tickets own them).

Keep the gate green every commit: pytest, ruff check, mypy src tests, python3 tools/check_boundary.py src, python3 tools/check_version.py. Stdlib-only core (scanners invoked as optional external subprocesses). No keys/secrets in the repo. Conventional commits. Open a draft PR with base = master; do not merge.

## REPORT BACK — MECHANIZED FORMAT (required)
End your session by emitting EXACTLY this block. Fixed fields, one line each, no diffs or logs.
Validate it before you finish: `bash /home/stack/charon-private/fleet/check-session-report.sh <file>`
Full spec + rationale: `/home/stack/charon-private/fleet/SESSION-REPORT-FORMAT.md`

```
=== SESSION REPORT v1 ===
TICKET:       <ticket-id>
SESSION:      <jedi-name> | <model>
STATUS:       DONE | BLOCKED | REFUSED | PARTIAL
COMMIT:       <sha> | none
FILES:        <n> changed: <paths>
OWNS-OK:      yes | NO — <file> is owned by <ticket>
GATE:         PASS | FAIL — <detail>
TESTS:        <n> passed, <n> failed, <n> skipped
RED-PROOF:    broken=<exit> green=<exit> | n/a — <why> | NOT-DONE
OBSERVABLE:   MET | DEFERRED — <what could not be observed and why>
RAN:          <what you proved by EXECUTING>
READ:         <what you concluded by READING only>
BRIEF-ERRORS: none | <what this brief got factually wrong>
BLOCKED-BY:   none | <ticket or condition>
NEXT:         <the single thing the manager should do next>
=== END REPORT ===
```
**Emit it even if you are BLOCKED or REFUSED — especially then.** A correct refusal is the most
valuable report there is; a silent exit is worth nothing. **BRIEF-ERRORS is not optional politeness**
— on 2026-07-26 sessions caught nonexistent files in an OWNS clause, a ticket whose work already
existed, and a wrong premise about `upstream_model`. The brief is wrong more often than the session is.
