You are working on the Charon repo (SLOP-Platform/charon) on branch feat/l3-unattended, cut from a master that already contains the merged T1 (PERF-4) changes.

Implement L3 unattended autonomy: the autonomy escalation gate + fence policy. This is the highest-blast-radius, security-critical ticket — land an ADR/plan note + an adversarial-review reconciliation in docs/REVIEW-LOG.md BEFORE code.

Own ONLY fence.py and coordinator.py — never touch other files. Tests: extend tests/test_fence.py and tests/test_coordinator.py. Keep the gate green on every commit: pytest, ruff check, mypy src/charon, python3 tools/check_boundary.py src, python3 tools/check_version.py. Stdlib-only core. No keys/secrets in the repo. Conventional commits. Open a draft PR with **base = master** (`gh pr create --base master …`); do not merge.
