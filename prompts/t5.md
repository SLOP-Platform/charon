You are working on the Charon repo (SLOP-Platform/charon) on branch feat/live-acp-handoff.

Implement a live cross-vendor handoff between two real ACP agents, with a doctor proof. Land a short plan note (integration shape) in docs/REVIEW-LOG.md before code.

Own ONLY adapters/acp.py and doctor.py — never touch other files. Tests: tests/test_handoff_crossvendor.py (integration/proof) and extend tests/test_handoff.py. Keep the gate green on every commit: pytest, ruff check, mypy src/charon, python3 tools/check_boundary.py src, python3 tools/check_version.py. Stdlib-only core. No keys/secrets in the repo. Conventional commits. Open a draft PR; do not merge.
