You are building the FIRST wave of Charon's native work-engine (ADR-0010). Read
docs/adr/0010-native-work-engine-substrate.md (D2 "anti-dilution guard" + build-sequence
step 0) and the ADR-0010 entry in docs/REVIEW-LOG.md FIRST.

GOAL: lock the gateway/engine boundary BEFORE any engine code exists, so the engine can
never dilute the gateway request path or install footprint (ADR-0005 R3 / ADR-0007 D11).

BUILD (extend, don't rewrite the existing checks):
1. tools/check_boundary.py — add a pass over src/charon/engine/ (and src/charon/ports/worker.py
   if it ever exists) that forbids any import whose top-level package is NOT stdlib or
   `charon.*` (mechanizes "the engine stays stdlib-only" — subprocess/urllib only). The
   engine dir does NOT exist yet: the pass must be a clean no-op when absent and enforce
   once it appears.
2. tests/test_boundary.py — add a TRANSITIVE import test (use the `sys.modules` closure in a
   subprocess, NOT AST): import `charon.proxy_server`, `charon.gateway`, `charon.service.app`,
   then assert no loaded module name starts with `charon.engine` or equals
   `charon.ports.worker`. This guarantees the gateway path never pulls the engine, even
   indirectly.

CONSTRAINTS: own ONLY tools/check_boundary.py and tests/test_boundary.py. Do NOT create
engine/ or any other file. Keep all existing boundary checks intact. Gate green on every
commit: pytest, ruff check, mypy src tests, python3 tools/check_boundary.py src,
python3 tools/check_version.py. Stdlib-only. No secrets. Conventional commits. Open a DRAFT
PR with base=master (`gh pr create --base master --draft …`); do NOT merge — the manager
gates and merges.

## REPORT BACK — MECHANIZED FORMAT (required)
End your session by emitting EXACTLY this block. Fixed fields, one line each, no diffs or logs.
Validate it before you finish: `bash /home/stack/charon-private/fleet/check-session-report.sh <file>`
Full spec + rationale: `/home/stack/charon-private/fleet/SESSION-REPORT-FORMAT.md`

```
=== SESSION REPORT v1 ===
TICKET:       <ticket-id>
SESSION:      <jedi-name> | <model>
STATUS:       DONE | BLOCKED | REFUSED | PARTIAL
COMMIT:       <sha> | none
FILES:        <n> changed: <paths>
OWNS-OK:      yes | NO — <file> is owned by <ticket>
GATE:         PASS | FAIL — <detail>
TESTS:        <n> passed, <n> failed, <n> skipped
RED-PROOF:    broken=<exit> green=<exit> | n/a — <why> | NOT-DONE
OBSERVABLE:   MET | DEFERRED — <what could not be observed and why>
RAN:          <what you proved by EXECUTING>
READ:         <what you concluded by READING only>
BRIEF-ERRORS: none | <what this brief got factually wrong>
BLOCKED-BY:   none | <ticket or condition>
NEXT:         <the single thing the manager should do next>
=== END REPORT ===
```
**Emit it even if you are BLOCKED or REFUSED — especially then.** A correct refusal is the most
valuable report there is; a silent exit is worth nothing. **BRIEF-ERRORS is not optional politeness**
— on 2026-07-26 sessions caught nonexistent files in an OWNS clause, a ticket whose work already
existed, and a wrong premise about `upstream_model`. The brief is wrong more often than the session is.
