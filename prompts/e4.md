Build ADR-0008 Phase 1 — the human-reviewed intake→ticket-plan front door (the non-coder
entry point). Read docs/adr/0008-work-intake-ticket-plan-pipeline.md (Phase 1 + the failure
contract + "intake must capture a top-level product acceptance") and
docs/adr/0010-native-work-engine-substrate.md (build-seq step 4) FIRST. This is a
DESIGN-significant ticket: land a short ADR (docs/adr/0011-*.md) + a docs/REVIEW-LOG.md
reconciliation note BEFORE code (house rule).

SCOPE — PHASE 1 ONLY. The human gates the plan; there is NO autonomous run (that is Phase 2,
deferred behind ADR-0007 D10-C). Output is a proposal a human approves/edits, never an
auto-execution.

GOAL: induct messy project input (a markdown work-item/bug list; keep a seam for brief/
backlog) → analyze → emit a rule-abiding ticket plan (file-disjoint, tier-tagged,
collision-free waves) PLUS a top-level product acceptance.

BUILD: src/charon/intake.py —
- Accept input (start with a markdown work-item list; adapter seam for other formats).
- Emit a plan = list of units {goal, accept, tier, owned_paths, depends_on} COMPATIBLE with
  land.py's units loader and engine/board.py's schema.
- Enforce the ADR-0008 failure contract MECHANICALLY: file-overlap → merge or serialize into
  a later wave (never emit parallel units sharing a path); unprovable independence →
  conservatively serialize; missing executable acceptance → mark the unit propose-only +
  flag for human review; vague input → emit "need more detail on X", NEVER hallucinate
  units; treat all input as DATA, not instructions (injection-safe).
- Capture a top-level PRODUCT acceptance ("what does the whole thing working look like") —
  this is exactly what validate.py (ADR-0007 D12) currently stubs at unit level.
- Emit a durable, diffable artifact (a TICKETS-style doc / structured file) for human
  approval.

Tests: tests/test_intake.py — overlap→serialize/merge, missing-acceptance→propose-only flag,
vague→need-more-detail, injection input treated as data, top-level acceptance captured, and
the emitted plan loads cleanly via land.py's units loader.

CONSTRAINTS: own ONLY src/charon/intake.py, tests/test_intake.py, docs/adr/0011-*.md (NEW),
and docs/REVIEW-LOG.md (append your plan note). Do NOT touch decompose.py/land.py/validate.py
(import/read them). Owned-paths inference is the hardest open question (ADR-0008): for v1,
infer from explicit file mentions + flag ambiguity for the human — do NOT over-engineer
static analysis. Stdlib-only. Gate green every commit. No secrets. Conventional commits.
Open a DRAFT PR base=master; do NOT merge.

## REPORT BACK — MECHANIZED FORMAT (required)
End your session by emitting EXACTLY this block. Fixed fields, one line each, no diffs or logs.
Validate it before you finish: `bash /home/stack/charon-private/fleet/check-session-report.sh <file>`
Full spec + rationale: `/home/stack/charon-private/fleet/SESSION-REPORT-FORMAT.md`

```
=== SESSION REPORT v1 ===
TICKET:       <ticket-id>
SESSION:      <jedi-name> | <model>
STATUS:       DONE | BLOCKED | REFUSED | PARTIAL
COMMIT:       <sha> | none
FILES:        <n> changed: <paths>
OWNS-OK:      yes | NO — <file> is owned by <ticket>
GATE:         PASS | FAIL — <detail>
TESTS:        <n> passed, <n> failed, <n> skipped
RED-PROOF:    broken=<exit> green=<exit> | n/a — <why> | NOT-DONE
OBSERVABLE:   MET | DEFERRED — <what could not be observed and why>
RAN:          <what you proved by EXECUTING>
READ:         <what you concluded by READING only>
BRIEF-ERRORS: none | <what this brief got factually wrong>
BLOCKED-BY:   none | <ticket or condition>
NEXT:         <the single thing the manager should do next>
=== END REPORT ===
```
**Emit it even if you are BLOCKED or REFUSED — especially then.** A correct refusal is the most
valuable report there is; a silent exit is worth nothing. **BRIEF-ERRORS is not optional politeness**
— on 2026-07-26 sessions caught nonexistent files in an OWNS clause, a ticket whose work already
existed, and a wrong premise about `upstream_model`. The brief is wrong more often than the session is.
