Document Charon's completed work-engine for users (final docs pass). Read
docs/adr/0010 + docs/DECISIONS.md and the shipped engine: cli.py `work` command,
src/charon/engine/*, intake.py, validate.py, the auto-land path in land.py, Phase-2 in
intake.py, the sandbox policy (D013) and AIMD capacity. FIRST.

GOAL: README reflects the finished product: gateway-first (PRIMARY) + the opt-in native
work-engine (analyze→decompose→assign→propose-default land→validate), accurate to the
shipped code only.

BUILD: README.md — add/extend a "Work engine (opt-in)" section: what it is; how to run it
(the `charon work` command); the propose-default → opt-in auto-land posture; the
fence/sandbox policy (D013); intake Phase 1 (human-reviewed plan) and Phase 2 (autonomous);
and that the native engine supersedes the external dev harness for real use. Keep the
gateway-first framing primary (D001). NO aspirational features — describe only what shipped;
note any still-gated items (e.g. D015 verified isolation) as future.

CONSTRAINTS: own ONLY README.md. Docs only; do NOT touch code. Gate green. Conventional
commits. Open a DRAFT PR base=master; do NOT merge.
