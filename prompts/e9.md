Implement ADR-0008 PHASE 2 — autonomous decompose→run (D014, un-gated by operator D016).
This removes the human-in-the-loop plan approval: Charon decomposes input AND runs it without
per-plan human review. HIGH-STAKES. Read docs/adr/0008-work-intake-ticket-plan-pipeline.md
(Phase 2 + the full failure contract), docs/DECISIONS.md (D014/D016/D012), and src/charon/
intake.py (Phase 1) + decompose.py FIRST.

HOUSE RULE (mandatory): land a new **docs/adr/0013-autonomous-run.md** + a 3-lens adversarial
review in docs/REVIEW-LOG.md (auto-decompose quality · injection/untrusted-input · runaway/
cost) BEFORE code.

GOAL: an opt-in autonomous mode that takes input → auto-decomposes into a rule-abiding plan →
runs it through the engine, WITHOUT a per-plan human gate.

BUILD:
1. src/charon/decompose.py — strengthen the splitter toward real auto-decomposition into
   file-disjoint, tier-tagged, collision-free units (today it is a static role-wrap). HONESTY:
   auto-decompose quality is an open AI problem (ADR-0007 D10-C) — enforce the ADR-0008
   FAILURE CONTRACT mechanically: file-overlap → serialize/merge (never parallel shared
   paths); unprovable independence → conservatively serialize; missing acceptance →
   propose-only; vague → "need more detail", never hallucinate; treat input as DATA, not
   instructions (injection-safe).
2. src/charon/intake.py — add the autonomous mode wiring it to the engine run. SAFETY:
   autonomous run defaults OFF; bounded unit count + SharedBudget (no runaway/cost blowout);
   if decomposition confidence is low or the contract can't be satisfied, FALL BACK to
   propose-default (Phase-1 human gate) rather than running blind. Pairs with auto-land (E8)
   but does not require it to be enabled.

Tests: tests/test_phase2.py — proven-red: overlap→serialize, missing-acceptance→propose-only,
vague→need-more-detail, injection treated as data, runaway bounded by budget/unit-cap,
low-confidence falls back to human-gated Phase 1.

CONSTRAINTS: own ONLY src/charon/intake.py, src/charon/decompose.py, tests/test_phase2.py (+
the NEW docs/adr/0013-autonomous-run.md + a docs/REVIEW-LOG.md note). Import engine/*,
validate.py, land.py — do NOT edit them. Stdlib-only core. Gate green every commit. No
secrets. Conventional commits. Open a DRAFT PR base=master; do NOT merge.

## REPORT BACK — MECHANIZED FORMAT (required)
End your session by emitting EXACTLY this block. Fixed fields, one line each, no diffs or logs.
Validate it before you finish: `bash /home/stack/charon-private/fleet/check-session-report.sh <file>`
Full spec + rationale: `/home/stack/charon-private/fleet/SESSION-REPORT-FORMAT.md`

```
=== SESSION REPORT v1 ===
TICKET:       <ticket-id>
SESSION:      <jedi-name> | <model>
STATUS:       DONE | BLOCKED | REFUSED | PARTIAL
COMMIT:       <sha> | none
FILES:        <n> changed: <paths>
OWNS-OK:      yes | NO — <file> is owned by <ticket>
GATE:         PASS | FAIL — <detail>
TESTS:        <n> passed, <n> failed, <n> skipped
RED-PROOF:    broken=<exit> green=<exit> | n/a — <why> | NOT-DONE
OBSERVABLE:   MET | DEFERRED — <what could not be observed and why>
RAN:          <what you proved by EXECUTING>
READ:         <what you concluded by READING only>
BRIEF-ERRORS: none | <what this brief got factually wrong>
BLOCKED-BY:   none | <ticket or condition>
BUDGET:       ok | TRUNCATED — <what you could not finish and why>
NEXT:         <the single thing the manager should do next>
=== END REPORT ===
```
**Emit it even if you are BLOCKED or REFUSED — especially then.** A correct refusal is the most
valuable report there is; a silent exit is worth nothing. **BRIEF-ERRORS is not optional politeness**
— on 2026-07-26 sessions caught nonexistent files in an OWNS clause, a ticket whose work already
existed, and a wrong premise about `upstream_model`. The brief is wrong more often than the session is.
