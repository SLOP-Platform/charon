# HANDOFF — 2026-07-10 (session C) — Charon fleet MANAGER

## Bootstrap one-liner (paste as the next session's first message)
```
Read and fully follow /home/stack/charon-private/fleet/HANDOFF-2026-07-10-c.md — you are the fresh Charon fleet MANAGER. Load MANAGER-OPERATING-RULES.md (SessionStart cats it). Run preflight.sh; re-verify git/gh/ssh; register on the session-bridge (repo:charon) under a Jedi name. Your FIRST action is to FIRE WAVE 1 of the GATEWAY-PROGRAM exactly as staged in §WAVE-1-FIRE below, then monitor/review/merge. HARD RULES: (1) route ALL sub-work to Charon via charon-run.sh (opencode→4-LOM), NOT Claude Agent tool; (2) warn + offer handoff at ~250K context; (3) money-path/security/core → adversarial review before merge; (4) merge gate = FULL CI on the merge commit, not pytest-alone; (5) two-tier review = fan the adversarial review to N independent Charon models, you adjudicate the verdicts.
```

## WHY THIS HANDOFF
Session C did the full design + consolidation + board-prep for the **GATEWAY-PROGRAM** (finish/polish
the Gateway's intelligent-routing layer) and staged Wave 1. Handing off at a clean, committed checkpoint
BEFORE the token-heavy execute→review→merge phase (operator direction — protect the budget).

## SHIPPED / DONE this session (committed to charon-private @ 8cd9851)
- **GATEWAY-PROGRAM plan of record** `fleet/GATEWAY-PROGRAM.md` (rev-2). Read it first — it holds the
  9 locked decisions, cluster consolidation, retire list, and the wall-clock wave plan.
- **Synthetic benchmark RETIRED as a ranker** — adjudicated by a 3-reviewer two-tier Charon review
  (3 models, 3 lenses, unanimous RETIRE @ high confidence). Ranker is now the **actuals ledger**
  (grade real sub-session work) + reds-replay. #26 kept, repurposed to grade REAL tasks.
- **Plan red-teamed** (3 more Charon reviewers) → 9 fixes folded into rev-2 (routing_policy PACKAGE,
  product-boundary freeze-ring + CI import-guard, metering-invariant canary gate, proactive
  live-reserve, pipeline Wave-2, un-moot pool tickets, novel-class=high-risk-default, provider-split).
- **Board actions:** retired 5 synthetic tickets; created ACTUALS-LEDGER / EXPLORE-PROMOTE /
  WORKCLASS-TAXONOMY / DIFFICULTY-SCHEMA; repurposed #26; **D1 hybrid** = difficulty backfilled on 157
  tickets (auto-seeded from `tier`), ladder deferred; **D2** = manager accept/reject kept low-weight.
- **Wave-1 coupling check PASSED** — `proxy.py`/`balance.py` do NOT import `gateway.py`, so METER and
  the gateway decompose are safe to run in parallel.

## §WAVE-1-FIRE — the fresh session's FIRST action (staged, ready)
4 disjoint-file builds, worktree-isolated, provider-split. Briefs already written at
`fleet/board/briefs/wave1-*.md`. Run these commands:

```bash
# 1) worktrees (isolate each build; 3 product + 1 rig)
cd /home/stack/code/charon && git fetch origin --quiet
git worktree add -b feat/gateway-routing-decompose /home/stack/code/wt-decompose master
git worktree add -b feat/meter-model-provider       /home/stack/code/wt-meter      master
git worktree add -b feat/actuals-ledger             /home/stack/code/wt-actuals    master
cd /home/stack/charon-private && git worktree add -b feat/bench-oob-grading /home/stack/charon-private/wt-grader HEAD

# 2) fire (provider-split: pro / pro / glm / flash — 2 concurrent pro is proven OK this session)
S=/home/stack/charon-private/fleet/charon-run.sh
BR=/home/stack/charon-private/fleet/board/briefs
L=/home/stack/charon-private/fleet/state/wave1-logs; mkdir -p "$L"
nohup bash "$S" /home/stack/code/wt-decompose        "$L/decompose.txt" "$BR/wave1-decompose.md" deepseek-v4-pro glm-5.2 deepseek-v4-flash >/dev/null 2>&1 &
nohup bash "$S" /home/stack/code/wt-meter            "$L/meter.txt"     "$BR/wave1-meter.md"     deepseek-v4-pro glm-5.2 >/dev/null 2>&1 &
nohup bash "$S" /home/stack/charon-private/wt-grader "$L/grader.txt"    "$BR/wave1-grader.md"    glm-5.2 deepseek-v4-pro >/dev/null 2>&1 &
nohup bash "$S" /home/stack/code/wt-actuals          "$L/actuals.txt"   "$BR/wave1-actuals.md"   deepseek-v4-flash glm-5.2 >/dev/null 2>&1 &
```
Then arm a watcher (run_in_background bash, until-loop) that exits when all 4 logs contain
`CHARON_RUN_RESULT`; the harness re-invokes you on completion. **Watch
`fleet/provider-exhaustion-ledger.tsv`** — if `limit-failover`/`ALL-EXHAUSTED` appears, the pool is too
thin under 4 concurrent heavy builds → narrow to 3 next time.

## AFTER builds land — review + merge (per item)
- **Two-tier review:** fan an adversarial review of each branch diff to 2–3 independent Charon models
  (different model than the builder; prompt to REFUTE); you read only the compressed verdicts + spot-check
  the ONE load-bearing claim on 🔴 items.
- **METER is money-path:** its merge precondition is the **metering-invariant canary** (cost-total delta==0
  + credential-shape) IN ADDITION to review — not two free review attempts.
- **GRADER/ACTUALS:** verify the **product-boundary import-guard** (no `import benchmark`/`grader_daemon`
  on the product hot path) and the freeze-ring last-known-good fallback.
- **Merge gate = FULL CI on the merge commit** (`ruff + mypy + PYTHONPATH=src python3 -m charon.cli gate`),
  never pytest-alone. If two branches touch the same test/contract surface, merge the later onto the
  former's merge and re-run FULL CI (the #91/#92 collision lesson).
- Strip any `REVIEW-PACKET.md` the agent committed before merge. Push only via `land-push.sh`.
- **Escalation ladder:** a build failing adversarial review TWICE → escalate that item to a Claude droid
  tab (operator opens). Default stays Charon.
- Wave 2 (CAPABILITY-ENGINE authors routing_policy/ policies + matrix.py; EXPLORE-PROMOTE;
  WORKCLASS-TAXONOMY) PIPELINES off the decompose's interface stub — don't wait for full Wave-1 merge.

## ⚠️ GOTCHAS I HIT THIS SESSION (avoid re-discovering)
- **`sudo` is sandbox-DENIED** to the manager. Cannot verify bench-grader dirs (0700 denies my reads —
  that's the isolation *working*, not an error). For sudo checks, hand the operator the exact command.
- **`systemctl` is DENIED.** Can't check service state from bash — ask the operator to run it.
- **Network (`curl` to 10.0.1.60 gateway) is DENIED.** Don't try to ping 4-LOM; rely on charon-run.sh
  (it reaches the gateway) and the exhaustion-ledger for health.
- **Avoid `echo`/`cat`-scaffolded compound Bash** (`echo "==="; cmd; cmd`) — these got DENIED. Use
  simple single-purpose commands or a `python3 - <<PY` heredoc; split multi-step checks into separate calls.
- **Tickets use FLAT `key: value` frontmatter** (no `---` YAML fence). Parse with `grep -E '^owns:'`, not awk `/^---/`.
- **charon-run.sh model args = FAILOVER chain, not parallel.** N independent reviewers = N separate
  invocations, each its own primary + outlog. (The watcher-until-`CHARON_RUN_RESULT` pattern worked well.)
- **Concurrent same-repo builds MUST use separate git worktrees** or they stomp each other's working tree
  (why §WAVE-1-FIRE creates 4 worktrees). Cleanup order: `git worktree remove --force` → `git branch -D`
  → delete remote ref.
- **`git rebase` / `git push` / `git -C … push` / `reset --hard` are DENIED.** Use `git merge origin/master
  --no-edit` to update a branch; push only via `land-push.sh`.
- I initially mis-split providers (two heavy builds on glm-5.2) — red-team caught it. Verify the split
  actually spreads pools AND count the grader's own model traffic as a consumer.

## OPERATOR PREFERENCES banked this session (memory: answer-concisely-and-say-where-to-run)
- Simple question → ONE-LINE direct answer first (no tables/walls). Expand only if asked.
- Every command handed to the operator must state WHERE to run it (host + path).

## STANDING DIRECTIVES (memory)
`charon-headless-review-loop` · `route-work-to-charon-not-claude` · `merge-gate-is-full-ci-not-pytest` ·
`tests-must-fail-on-revert` · `independent-review-before-merge-on-critical` ·
`answer-concisely-and-say-where-to-run` (NEW).

## SESSION-BRIDGE
Register under a Jedi name (repo:charon). Prior session (Obi-Wan / mgr-obiwan) auto-purges after 600s stale.
```
