# Provider configuration

Each provider entry requires a `name` and a base endpoint. Example:

```json
{
  "name": "prov-a",
  "base": "https://a.example.com"
}
```

The `base` value is the upstream base endpoint.
