# Task: translate a short English paragraph to French

Translate the following English paragraph to French. The translation
must be a faithful, natural French rendering — NOT a literal word-for-
word gloss.

```
The gateway receives a request, inspects the message for any images,
and routes the request to a model that supports vision. If no vision-
capable model is available, the request fails with a clear error.
```

Write your translation to `answer.txt`. The grader checks the
translation contains specific French vocabulary (the expected
translations of the key technical terms).
