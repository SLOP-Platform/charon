# Task: short instruction-following

Follow these instructions EXACTLY:

1. Write a single line of text: `hello world`.
2. On the next line, write a single line: `goodbye world`.
3. On the next line, write the integer `42`.
4. On the next line, write the string `done`.

Write the four lines (and only the four lines) to `answer.txt`.
