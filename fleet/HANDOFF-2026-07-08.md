# Charon Manager HANDOFF — 2026-07-08

> **Bootstrap (paste into a fresh session to take over as manager):**
> ```
> Read and fully follow /home/stack/charon-private/fleet/HANDOFF-2026-07-08.md — you are the fresh Charon fleet MANAGER. FIRST run the startup first-acts (bash preflight.sh; bash model-scorecard.sh --due), re-verify durable state via git/gh/ssh, register on the session-bridge (repo:'charon') under a Grand Master Jedi name (yoda/luke-skywalker/satele-shan). Then work the PRIORITIZED BACKLOG in this handoff top-down. Delegate substantive work to sub-sessions that WRITE findings to files and return short pointers; gate/merge/push/dialogue only; keep auto-compact ON; adversarial review of significant/core code. Move deliberately, one instruction at a time; pause after any question/handed action.
> ```

---

## 1. SHIPPED / DEPLOYED THIS SESSION (2026-07-07, all live)

| Area | What shipped | Evidence |
|---|---|---|
| **Benchmark-v2** | SHIPPED + validated on a **live glm-5.2 run** — real tokens captured, real cross-run discrimination (not just S0–S6 saturation) | fleet commits `f42ea99` (harden harness + build v2 scoring + token capture + purge contaminated rows), `a4ea41f` (close bench-run-collision + bench-premature-grade), `71c725f` (normalize model id to bare curated form), `d47f49d` (fix model-misdetect, refuse-on-ambiguity + clean reset path) |
| **Durable cross-host session-bridge coordinator** | DEPLOYED on Roci (`10.0.1.51`) — daemon + DB live, tunnel confirmed end-to-end (register/board/unregister round-tripped through the tunnel). **Cutover staged**, activates on next bridge-daemon/opencode restart, not yet hot | `scratch/roci-7c-report.md`, `scratch/bridge-phase01-review.md` |
| **Model/routing cleanup on live 4-LOM v0.3.6** | OpenCode Zen **removed from 47 pools** (full removal, not demote) + **hidden in the opencode picker** (`disabled_providers:["opencode"]`); kept only the free `big-pickle` and one skip-listed dead pool retired; dead `gpt-5.3-codex-spark` (zen-only, no alternative) fully retired (pool + both model entries); cooldown clamp deployed (v0.3.6: clamp Retry-After ≤120s + order cooled bucket by remaining time); dead-`*-free-go` entries pruned; HuggingFace **ACTIVATED as backup on 6 pools** (glm-5.2/5.1/5, deepseek-v4-flash, kimi-k2.6/2.5 — token rotated + secured, live 200 verified); `/models` catalog cleaned (Charon-first picker + OpenRouter/NanoGPT direct-bypass groups added below it); model-id normalization landed (opencode's `charon/glm-5.2`-namespaced ids now collapse to bare `glm-5.2` everywhere) | fleet commits `1c54ef4`, `cfa3159` (public-clean guard — product repo), `f3a73f2`, `f369b7c` (v0.3.6 bump — product repo); fleet-repo commits `ba0f9d5` (#14/#15 ticket→best-agent assignment engine), `8497425`, `31e8064`, `df6f1c0` (red closures); reports: `scratch/zen-remove-report.md`, `scratch/spark-retire-report.md`, `scratch/hf-activate-report.md`, `scratch/models-catalog-cleanup-report.md`, `scratch/model-id-normalize-report.md` |

**Exact reds closed this session** (all verified, see §5): `bench-run-collision`, `bench-premature-grade`, `bench-model-misdetect`, `bench-model-id-namespacing`, `cooldown-anchor-demotion`, `stale-charon-token-env`, `dead-free-go-entries`, `opencode-zen-pool-primary`.

**Product repo (`/home/stack/code/charon`) currently at `v0.3.5` released / `v0.3.6` tagged and live-deployed to 4-LOM** (cooldown clamp + public-clean guard land on top of v0.3.5; container `charon-gateway-1` healthy).

---

## 2. THE BIG STRATEGIC FINDING

> **The adversarial validity review found the benchmark is a SMOKE-TEST, not a valid ranker.**
> See `fleet/BENCHMARK-VALIDITY-REVIEW.md` (verdict: "closer to theater than measure" for ranking) and memory `benchmark-not-a-valid-ranker`.

Why (ranked): (1) **CRITICAL** — graders are world-readable and grading is self-driven/self-reported by the model under test (`cat graders/s2.py` is one command away from the answer key) → gameable **now**. (2) Near-zero discrimination — 5/7 sections saturate at 100 for every model. (3) Blind to the real failure modes the live scorecard already caught (glm scored 100 on a path it BLOCKed live; deepseek confabulated a commit history). (4) N=1 per section + proven harness-artifact variance (glm-5.2 scored S3 100 then 75, S5 100 then 60 across runs — motivates §4's #16). (5) Spoon-fed single-file Charon-shaped tasks (weak construct validity).

**Pending DECISION for next session:** adopt the **real-outcomes pivot** — demote synthetic S0–S6 to a smoke-test; re-ground the ranking/grades brain in the `source=live` actuals ledger (already out-discriminates the synthetic 100s) + **replaying real `reds.tsv` entries** (un-memorizable, self-refreshing, ready-made `check_cmd` graders — ticket #25). This changes the **pools-redesign grades-table source** from "benchmark-fed" to "real-outcomes-fed" (strictly more valid, same schema). Operator is leaning yes but this is **not yet final** — confirm before Phase-2 pools work depends on it.

**Also carry forward:** the pools redesign is **DESIGN-OF-RECORD** (`fleet/POOLS-REDESIGN-ADR-v2.md`, superseding v1 after `POOLS-REDESIGN-REVIEW.md`'s REWORK verdict) but **Phase-1 build has NOT started**. Phase 1 (tier-collapse + real cost/health routing) has **no benchmark dependency** and can start regardless of the P0 decision above; only Phase 2 (per-work-class capability grades) is gated behind a decision-differentiation gate and depends on which signal (synthetic vs real-outcomes) feeds it.

---

## 3. PRIORITIZED BACKLOG (work top-down)

> Ticket numbers are session-bridge board task ids from the live session — they will **not** persist to a fresh board, so each is given with its one-line intent. Re-file as fleet board tickets if a durable record is needed.

### P0 — first
| # | Intent |
|---|---|
| #26 | Grader-integrity / out-of-band grading — the benchmark is gameable until scoring is moved off the graded model's box and out of self-report. Top fix; nothing the benchmark says is trustworthy until it lands. |
| — | **DECISION:** confirm the real-outcomes benchmark pivot (§2) with the operator before Phase-2 pools work depends on the grades source. |
| #20 | Provisional-vs-active scoring — lets new tests collect data without touching live grades until proven to discriminate. Enabler for #26/#25 and for pools Phase 2. |

### P1
| # | Intent |
|---|---|
| #16 | Aggregate multiple runs per (model, section) — proven needed by the glm-5.2 100→75/100→60 variance already recorded. |
| #17 | Difficulty calibration — statistical validity companion to #16 (current sections saturate; need headroom). |
| #25 | Test-freshness / reds-replay — the valid ranking signal per §2's pivot; real, self-refreshing, deterministic `check_cmd`s already exist in `reds.tsv`. |
| #27 | Gate #14 (ticket→best-agent assignment, already built via `ba0f9d5`) on discrimination — closes the same inert-composite carve-out the validity review flagged for the assignment consumer. |
| #33 | Secret hot-rotation — `secrets.apply_to_env()` uses `os.environ.setdefault`, a structural no-op for any key already resident in process env; give it a force-refresh mode so provider-key rotation doesn't need a container restart. Root-caused this session (`scratch/hf-token-reload-verify.md`) but not fixed. |
| — | **Pools-redesign Phase 1 build** — tier-collapse + real cost/health routing (`POOLS-REDESIGN-ADR-v2.md` Phase 1/1.5). Grades-table source is now real-outcomes (pending the P0 decision above), but Phase 1 itself has no benchmark dependency and can start immediately. **Prereq — the pricing injection point is now known**: `models.json` has no `cost_input`/`cost_output`; populate `cost_rank` via an OpenRouter alias-fix or a `models.dev` import (this was previously "unlocated," per ADR-v2 §"Pricing source"; still needs to be built, not just named). |

### P2
| # | Intent |
|---|---|
| #12 | Cooldown fix-3 — follow-on to the deployed clamp (`f3a73f2`/v0.3.6); check for remaining edge cases. |
| #30 | Catalog + pricing sync + drift detector (feeds #31) — mechanize `models.dev`/provider `/models` → Charon catalog metadata sync + structural-drift surfacing. |
| #31 | Full-catalog search + curate — blocked on #30. |
| #29 | `-free-zen-model` drift — residual cleanup after the zen-family removal (47 pools) and spark retirement this session; check for stragglers. |
| #18 | Review the 4 low-confidence `work_class` backfills on tickets (from the #14/#15 assignment-engine build, `ba0f9d5`). |
| #19 | Triage the 5 pre-existing board reds flagged but not yet dispositioned. |

### P3 — blocked/deferred
| # | Intent |
|---|---|
| #21/#22/#23 | Blocked by #20 (provisional-vs-active scoring must land first). |
| #31 | Also listed above — blocked by #30. |
| #32 | Add LongCat-2.0 — blocked on a Meituan LongCat API Platform signup (`https://api.longcat.chat/openai`, model id `LongCat-2.0`, OpenAI-compatible, needs a **new provider integration**, not a catalog tweak). **Not** on OpenRouter (confirmed 0 matches for `longcat`/`meituan`) or HuggingFace (weights-only, zero inference providers attached). The `openrouter/owl-alpha` stealth slot some journalism links to LongCat-2.0 was evaluated and **retired as a route** — too fragile/unverifiable (no OpenRouter-side metadata confirms the link; a cloaked slot can be swapped/retired without notice). See `scratch/longcat-availability.md`. |

---

## 4. OPEN REDS

**Live count: 0 open.** Run `bash preflight.sh` to re-verify — all 17 rows in `reds.tsv` are `status: closed` as of this write-up.

Notable closures this session (all `manual:`-verified, evidence in `reds.tsv`):
- `cooldown-anchor-demotion` — **FIXED + closed**: `f3a73f2` (clamp Retry-After ≤120s + cooled-bucket order-by-remaining), Opus-reviewed SHIP, deployed live as v0.3.6, container healthy.
- `bench-model-id-namespacing` — **CLOSED**: `detect_model.py`/`bench.sh` normalize the provider prefix via `rsplit` (mirrors `proxy._normalize_model_id`); 7 fragmented `charon/glm-5.2` rows merged into bare `glm-5.2` (kept the newer 07-07 hardened-harness scores per operator call — flags a real S3/S5 regression vs the superseded 07-06 run, worth a second look).
- `bench-run-collision` / `bench-premature-grade` — closed via `f42ea99` (flock + staleness-gated grading + mtime-stability gate).
- `bench-model-misdetect` — closed via `d47f49d` (refuse-on-ambiguity instead of guessing).
- `dead-free-go-entries`, `stale-charon-token-env`, `opencode-zen-pool-primary` — closed (see reds.tsv for exact evidence per row; `opencode-zen-pool-primary`'s demote-to-last-resort fix was itself **superseded** this session by the full-removal pass, `zen-remove-report.md`).

**If preflight surfaces anything new, register it in `reds.tsv` before doing anything else** — never ignore a pre-existing red.

---

## 5. KEY GOTCHAS (carry forward — do not relearn)

- **Push ONLY via** `bash /home/stack/charon-private/fleet/land-push.sh <branch-or-tag> [repo]` (raw push deny-listed). Works for the fleet repo too.
- **Deploy via** `bash /home/stack/charon-private/fleet/deploy.sh v<tag>` (backup/verify/rollback built in). **v0.3.6 is live** on 4-LOM.
- **4-LOM (`ssh -i ~/.ssh/4lom stack@10.0.1.60`, container `charon-gateway-1`)**: `sudo` needs a password, but plain `docker exec` is unprivileged and works fine (stack is in the `docker` group) — don't reach for sudo on routine setup-API calls.
- **Roci passwordless-sudo IS set** — the earlier `roci-backup.md` recon predates this; current state allows non-interactive `sudo` on Roci.
- **Setup-API pool/model edits are the way to change live routing** (`POST /charon/pools`, `/charon/remove`, etc. — hot-reloads via `_reload()`, no restart needed). **Always back up `/data` first** (off-host copy under `/home/stack/backups/charon-4lom-*` + in-container `.bak` timestamped copies — see this session's reports for the pattern).
- **Secret rotation currently needs a container restart** (#33, ticket above) — `secrets.apply_to_env()` uses `os.environ.setdefault`, a no-op once a key is already resident in the process env. Rotating `HF_TOKEN` on disk does **not** take effect live; only a restart picks it up.
- **The durable cross-host bridge cutover is staged**, not hot — takes effect on the next bridge-daemon/opencode restart, not immediately.
- **Benchmark usage:** pass `--model` explicitly (auto-detect can misdetect across concurrent sessions — mostly hardened now via `d47f49d`'s refuse-on-ambiguity, but explicit is still safer); run `bench.sh reset --model <id>` before re-running a model that's already scored; in the opencode picker, use **"Charon Gateway"** entries (OpenCode Zen is now hidden via `disabled_providers`; two emergency-bypass groups — OpenRouter-direct and NanoGPT-direct — sit below it for use if the gateway itself is down).
- **`/data/rotate-hf.py`** helper exists on the gateway host for HF key rotation (writes the new token to `secrets.json`; still requires the restart above to go live).
- **4 tiers vs 3** — `POOLS-REDESIGN-ADR-v2.md` keeps "~4" as a target label but does not pre-commit; current data (3/4 scored models land in the same top bin) supports 3 real strata. Don't hard-code 4 into the Phase-1 build.

---

*Durable state (git/gh/ssh/reds.tsv) verified via direct read this session (2026-07-08 write-up). Re-verify via `preflight.sh` before acting — this doc is a snapshot.*
