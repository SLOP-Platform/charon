# Charon Manager HANDOFF — 2026-07-06

> **Bootstrap (paste into a fresh session to take over as manager):**
> ```
> Read and fully follow /home/stack/charon-private/fleet/HANDOFF-2026-07-06.md — you are the fresh Charon fleet MANAGER. FIRST run the startup first-acts: `bash /home/stack/charon-private/fleet/preflight.sh` (surfaces open reds + active detectors) and `bash /home/stack/charon-private/fleet/model-scorecard.sh --due`. Re-verify durable state via git/gh/ssh. Register on the session-bridge (repo:"charon"). Gate/merge/push/dialogue only; delegate substantive work to sub-sessions that WRITE findings to a file and return a short pointer (never dump full reports into primary); keep auto-compact ON; read big docs in narrow slices. Balanced model policy: build with Sonnet + gateway models, reserve Opus for review of money/correctness. Never ignore pre-existing red — register it in reds.tsv on discovery. Move slowly, one instruction at a time; pause after any question/handed action.
> ```

## SHIPPED THIS SESSION (all live)
- **Product master → released v0.3.5 → DEPLOYED live to 4-LOM** (`ghcr.io/slop-platform/charon:v0.3.5`, verified healthy). Landed: #5 request-normalizer (strip reasoning_content), #6 auto cost_rank, **failover-401 fix** (unknown "model not supported" 401s now fail over + synth 503 all-exhausted terminal), **per-session cost counter** (`X-Charon-Session` header + read-only `GET /charon/cost?session=`, LRU-capped 4096, Opus-reviewed), salvage `check_public_clean.py` lint tool, + 2 CI fixes (pytest basetemp per-job isolation on the shared runner; hermetic `test_no_secrets`).
- **Live gateway config:** 8 models added (206 total): gpt-oss-120b(groq), gemma-4-31b(cerebras), gemma-4-26b-a4b(or), hy3-preview(or,paid), mimo-v2.5+pro(nanogpt), minimax-m3(together), step-3.7-flash(or). `gpt-5.5` reordered NanoGPT-primary. `minimax-m3-free` fixed (dead -go removed). **`auto` pool rebuilt** → 7 diverse providers (groq→cerebras→nanogpt-anchor→deepseek→neuralwatt→openrouter→together), verified 200 (was 48× dead opencode-zen).
- **Token-burn root cause fixed + enshrined:** `autoCompactEnabled` was false → manager context never compacted. Now ON. Doctrine baked into START-SESSION.md/BOOTSTRAP.md/handoff.sh + memory `manager-context-burn-control`.

## THE BENCHMARK (fleet/benchmark/, fully built + Opus-verified graders)
- **Run it:** in an opencode tab, pick a model with `/model`, then paste: `read this and execute: /home/stack/charon-private/fleet/benchmark/RUN-BENCHMARK.md`. It auto-detects the model (reads opencode.db; fallback self-report), runs all 7 sections (S0–S6, S6=frontend) one at a time, auto-scores into `model-scorecard.tsv`, prints a single-tier chart: per-section grade + time + cost_usd + corrections + totals + **OVERALL TIER (Frontier/Strong/Capable/Basic/No-Tier) + intra-tier rank**.
- Graders are deterministic + Opus-reviewed (S2/S6 anti-dodge gates close the "green-but-inert" trap; 20/20 self-tests). **NOT yet run against real models** — that's the obvious next step (glm-5.2, deepseek-v4-pro first).
- **cost_usd:** global mode works now; true per-section isolation needs `CHARON_BENCH_SESSION_ID` set + the `X-Charon-Session` header wired in opencode.json BEFORE launching the tab (bench.sh can't inject it into a running process). `/charon/cost` endpoint is live on v0.3.5.

## REDS REGISTRY (fleet/reds.tsv + preflight.sh — now with ACTIVE DETECTORS)
`preflight.sh` scans open reds AND auto-detects classes (untracked-drift, secret-scan, repo-drift, health). **Register every pre-existing red on discovery** — that's the mechanized fix for "sessions ignore pre-existing."
- **Open (3):** `dead-free-go-entries` (P2 — 5 dead `*-free-go`; now mostly self-healing via the live failover-401 fix, but prune for cleanliness), `cooldown-anchor-demotion` (P2 — order_by_cooldown can demote the nanogpt anchor behind exhausted providers), `stale-charon-token-env` (P2 — shell profile has a stale `CHARON_GATEWAY_TOKEN` → 10.0.3.91).
- **Closed this session:** auto-pool-broken (rebuilt 7-diverse), version-gate-false-positive, bridge-daemon-down, silent-downgrade, handoff-pipefail-mask, board-validator-red, gpt55-pool-order, failover-401.
- **NEW finding to triage:** opencode-zen provider is in a live outage/depletion (401s across the board) — it still backs several pools; worth its own investigation/ticket.

## MODEL LIST + SCORECARD
- opencode `/model`: curated `charon`=30, `charon-full`=207 (all live models). Regen via `~/.config/opencode/regen-charon-models.sh`. **opencode reads config only at process start — a FULL `pkill opencode && opencode` (in WSL, not `-f`) is required to pick up changes.**
- Requested-but-absent-from-catalog: Owl Alpha (alias pulled — gone), Laguna M.1 (excluded, train-on-inputs ToS), Gemini 2.5 (deprecating — skipped). Provider research in `MISSING-MODELS-PROVIDERS.md`.
- Scorecard (`model-scorecard.sh`/`.tsv`): glm-5.2 (clean #5, **inert #6** — green-but-does-nothing), deepseek-v4-pro (correct #6 code+tests but **left uncommitted + confabulated** a false commit history when asked). Both stumbled on #6. Efficiency cols: time_s/cost_usd/corrections.

## NEXT PRIORITIES (roughly ordered)
1. **Run the benchmark** against glm-5.2 + deepseek-v4-pro (first real scores) — harness + the fixed `auto` pool are both ready.
3. Prune the 5 `dead-free-go-entries`; investigate the **opencode-zen outage** (it dominates several pools).
4. `cooldown-anchor-demotion` + `stale-charon-token-env` (operator can clean the shell profile).
5. **GUI build** — `board/GUI-SVELTE-BUILD.md` is fully scoped (static Svelte, 2 API endpoints in Phase 0, pipeline-integration acceptance criterion, CI-fresh dist). Ready to build; recommended build model = deepseek(backend)/glm-or-gpt5.5(frontend, TBD by benchmark), Opus review only.
6. Wire benchmark per-session cost (now that `/charon/cost` is live): set `CHARON_BENCH_SESSION_ID` + opencode header.
7. Parked/prior: obol Phase 1 (OBOL-PHASE-1-DESIGN.md), DRAIN balance-drain proxy-wiring (+ persistence req), pools simplification, meta-rules incorporation, SLOP ticket #1331 (mediastack frontend→svelte, parked).

## KEY GOTCHAS (do not relearn)
- **Push ONLY via** `bash /home/stack/charon-private/fleet/land-push.sh <branch-or-tag> [repo]` (raw push deny-listed; AUTONOMOUS flag ON). Works for the fleet repo too (`… master /home/stack/charon-private/fleet`).
- **Merge to master** in the main tree (on master, clean) OR an isolated `git worktree`; gate = BOTH `PYTHONPATH=src python3 -m charon.cli gate` AND `PYTHONPATH=src python3 -m pytest -q`.
- **Deploy:** `bash /home/stack/charon-private/fleet/deploy.sh v<tag>` (backup/verify/rollback; verify-probe cache-collision now fixed with a per-call nonce). Requires a released image first: bump pyproject → push → tag `vX.Y.Z` → push tag (release.yml builds+publishes on the SHARED runner — CI temp-isolation fix prevents concurrent-job collisions).
- **Gateway caches responses by body-hash** (1h TTL, no nonce/auth in key) — identical probes self-collide (`X-Charon-Provider: cache`). Bit the deploy verify; may affect benchmark cost/failover testing on identical requests.
- **4-LOM config lives on the `/data` named volume** — survives image swaps; deploy must not wipe it. ssh `-i ~/.ssh/4lom stack@10.0.1.60`, container `charon-gateway-1`.
- **Droid/opencode briefs must end with an explicit `LAST STEP (REQUIRED): git add -A && git commit … + report SHA`** (BRIEF-TEMPLATE.md; memory droid-brief-final-commit-rule) — deepseek left work uncommitted.

## END-OF-SESSION ADDENDA
- **Benchmark VALIDATED live** — first real run: **gpt-5.4 → FRONTIER (composite 95.8)**, only ding S3 ci-infra=75 (missed pipefail). Auto-detect, all 7 sections, auto-score, anti-dodge gates (S2/S6) passed honestly, and **real cost_usd captured** (~$0.08/section). The whole apparatus works. Scores in `model-scorecard.tsv`.
- **How the score works (for reference):** deterministic 0–100 per section = correctness + scope-discipline + anti-dodge proof (NO LLM judge). Composite = mean(S1–S6) → tier. `corrections`=cap only (>3 caps score; timeout→0); `time_s`/`cost_usd`=recorded, used for intra-tier tiebreak, NOT in the base grade. **Initiative/novelty NOT measured** (S5 even penalizes overbuild). **Benchmark-v2 options:** (a) weight efficiency into the score, (b) add a separate LLM-judge "initiative" axis (trades determinism).
- **opencode meter FIXED:** 29/30 curated models got `limit`+`cost` (from models.dev; `auto` left bare). Restart opencode to see % + $. CAVEAT: it's *nominal* models.dev pricing → overstates flat-sub (NanoGPT) routed models.
- **PRICING-SOURCE CORRECTION (important):** Charon's `/data/models.json` has **no** `cost_input`/`cost_output` — the "SR-5b pricing in models.json" assumption is STALE. Yet the gateway computes `cost_usd` (proven by the benchmark). **NEXT SESSION: locate where the gateway's per-token pricing actually lives** (separate pricing file? cost_rank-derived? check `gateway.json`/`quality.json`/proxy.py cost calc) — this is a prerequisite for the catalog-sync mechanization below.
- **MECHANIZATION TO BUILD — catalog + pricing sync pipeline:** `models.dev (+ provider /models)` → `charon-catalog-sync` (new: auto-apply pricing/limits to Charon; diff new/removed/renamed models, surface structural drift for review) → `regen-charon-models.sh` (extend to carry limit+cost) → `opencode.json`. Plus a `preflight` drift **detector** (stale pricing / model gone upstream) + optional scheduled sync. Rule: metadata auto-syncs; structural changes human-gated. This is the durable fix for "catalogs & prices drift and nothing updates."

*Durable state verified via git/gh/ssh this session. Re-verify before acting.*
