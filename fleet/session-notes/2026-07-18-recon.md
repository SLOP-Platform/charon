# RECON — 2026-07-18 (read-only, build-plan input)

Rig `/home/stack/charon-private` @ master `2eb6d11`. Product `/home/stack/code/charon` @ `643ea1c`.
No branches/worktrees created. `fleet/_lib.sh`, `fleet/done.sh`, `fleet/tests/verify-merged-repo-aware.test.sh`
NOT touched (owned by a concurrent agent).

## 0. Advisory commands run

All three are read-only as invoked (`foreman.sh` with no `--fix` is report-only per
`fleet/foreman-cadence.sh:12`; `status.sh` and `report.sh` are pure readers). Nothing skipped.

### foreman.sh verdict: **[FAIL] STARVING TIERS: frontier strong economy** (0 claimable each)

Proposed provably-safe remedies (NOT applied):
- clear quarantine `CAPTURE-WIRING-TIMEOUT-FIX` (passes decomposition gate)
- clear quarantine `COVERAGE-META-GATE`
- clear quarantine `FT-LIMITS-GROQ-RECONCILE`
- done-mark merged dep `STARTUP-CONTEXT-DIET` (PR #108 MERGED but unmarked) → unblocks `SYNC-SCHEDULE`

Composition health: `[ok] no live owns-collisions`.

### status.sh
droids 5 (all idle/claiming, uptime ~2d10h) · ready 3 · claimed 0 · PR-open 33 · done 5 · blocked 11 · parked 3.

## 1. LIVE tickets (unclaimed, unparked, deps satisfied)

Exactly **3**, and all three are currently QUARANTINED — they show `ready` on the board but
foreman reports 0 claimable per tier. They become claimable the instant quarantine is cleared.

| id | tier | repo | owns | depends_on | claimed | money/trust path? |
|---|---|---|---|---|---|---|
| CAPTURE-WIRING-TIMEOUT-FIX | strong | charon-private | `fleet/charon-run.sh` | SALVAGE-STASH-CHARON-RUN (DONE) | no | **YES** — work_class_note: "scorecard-integrity — a stray capture row skews real-outcome ranking data" → adversarial review |
| COVERAGE-META-GATE | strong | charon-private | `fleet/checks/rule-coverage.sh`, `fleet/state/RULE-REGISTRY.tsv`, `fleet/tests/rule-coverage.test.sh` | (none) | no | no (ci-infra) — but it is a GATE, so gates-must-actually-run applies |
| FT-LIMITS-GROQ-RECONCILE | economy | charon-private | `fleet/state/FREE-TIER-LIMITS.tsv`, `fleet/tests/ft-limits-reconcile.test.sh` | (none) | no | **YES** — work_class: money-path → adversarial review |

Post-remedy a 4th becomes live: `SYNC-SCHEDULE` (economy) once STARTUP-CONTEXT-DIET is done-marked.

### OWNS-COLLISION MATRIX (live set)
**Zero collisions.** The three live tickets' owns sets are pairwise disjoint
(`fleet/charon-run.sh` | `fleet/checks/*`+`RULE-REGISTRY.tsv` | `FREE-TIER-LIMITS.tsv`).
All three can run in ONE wave. Confirmed independently by `foreman.sh` composition health.

Historical note: the only recent owns-collision (`fleet/charon-run.sh`, shared by
CAPTURE-WIRING-TIMEOUT-FIX and SALVAGE-STASH-CHARON-RUN) was already dep-sequenced away in
`15396a7`, and SALVAGE is DONE.

## 2. Open PRs — RIG (Nnyan/charon-private), 15 open

No CI in this repo (all PRs "no checks reported"); every verdict rests on executed local evidence.

| PR | branch | verdict | note |
|---|---|---|---|
| #115 | feat/stage-demux | not-yet-reviewed | outside prior evidence scope |
| #114 | feat/handoff-root-archive | not-yet-reviewed | |
| #113 | feat/selfcheck-cycle-gate | not-yet-reviewed | |
| #107 | feat/bench-provisional-scoring | **DO-NOT-MERGE** | bench-provisional-deepdive: DESIGN-NEEDS-REVISION, central verdict must be INVERTED; ships inert scoring into money path |
| #105 | feat/assign-dispatch-pick-fix | **SEND-BACK** (fix specified) | gate swap on `args.candidates`; see §4 |
| #104 | feat/memory-index-compaction | **DO-NOT-MERGE** | strips summaries from all 118 index entries + vacuous test; also 128-file/277k-line graphify-out debris in the diff |
| #103 | feat/droid-lifecycle-reap | **SEND-BACK** (fix specified) | fail-closed the unique-commit check |
| #101 | feat/github-limits-hardening | **SEND-BACK** (one-char fix) | `-r`→`-q` + non-fixture cache test; 2 accept bullets need an operator scope call |
| #97 | feat/ssot-drift-gate | not-yet-reviewed | gate-tightener, likely inert on merge (pr-audit finding 5) |
| #96 | feat/reachability-gate | not-yet-reviewed | idem |
| #95 | feat/work-gate-universal | not-yet-reviewed | idem |
| #93 | feat/pricing-limits-check-sh | not-yet-reviewed | money-path adjacent → adversarial review |
| #90 | feat/gate-creation-standardize | not-yet-reviewed | idem |
| #62 | feat/session-end-push-gate | not-yet-reviewed | crash-signal title (launcher auto-commit) |
| #47 | audit/land-sh-postmortem | not-yet-reviewed | audit doc |

Already merged since the 07-16 review (no longer open, confirms that wave landed):
#92, #94, #98, #99, #106, #108.

## 3. Open PRs — PRODUCT (SLOP-Platform/charon), 9 open

| PR | branch | verdict |
|---|---|---|
| #174 | feat/meter-doc-reconcile | not-yet-reviewed (docs; money-path docs → verify against live wiring) |
| #172 | feat/graceful-degrade | not-yet-reviewed |
| #171 | feat/price-refresher | not-yet-reviewed (crash-signal title; **money-path**) |
| #170 | feat/api-decompose-cycle-fix | **DO-NOT-MERGE** — see §4 |
| #169 | docs/charon-flowchart | not-yet-reviewed (docs only) |
| #164 | feat/capability-actuals-deadref-cleanup | **RE-RUN CI then merge** — prior red was a confirmed infra flake, not re-run since |
| #161 | feat/web-roadmap-generator | not-yet-reviewed |
| #135 | feat/ft-catalog-seed | **SEND-BACK** — NOT crash-partial despite title; needs a 3-line fix (per product-clear) |
| #86 | dependabot github-actions bump | **MERGE-READY after rebase** — rebase-cure proven empirically |

Open operator question carried forward from product-clear (not a merge decision):
`src/charon/decompose_planner.py:282-283,332-334` encodes never-Anthropic as a **planner-only**
invariant and states "the gateway's tier-voter is allowed Anthropic". The standing HARD RULE is
gateway-wide. Either a deliberate narrowing (needs explicit operator re-confirm) or a silent scope
reduction landed in #168.

## 4. Prior premises — CONFIRMED IN CODE (all 5 still hold)

1. **rig #101 `gh -r`** — **CONFIRMED.** `feat/github-limits-hardening:fleet/gh-cache.sh:70` still
   uses `-r '.[] | . as $pr | ...'` inside `_gh_merged_files_tsv` (line 53), while the working
   sibling `_gh_merged_tsv` at line 42 correctly uses `-q`. `-r` is not a `gh pr list` flag →
   call fails → `rm -f "$cf.tmp"` → cache never written → `merged_prs_touching_file` empty →
   `done.sh` false-negative → REFUSE (exit 3). Fix: line 70 `-r` → `-q`, plus one non-fixture
   test asserting the cache file is written (all 19 existing tests set `GH_MERGED_FILES_FIXTURE`
   and short-circuit the broken call).
2. **rig #103 reaper** — **CONFIRMED.** `feat/droid-lifecycle-reap:fleet/reap-orphans.sh:158-162`
   unchanged: `git log --oneline "$BASE_REF..$branch" 2>/dev/null | wc -l` — an unresolvable
   `$BASE_REF` is swallowed and yields `unique=0` → DEAD+CLEAN → `git branch -D`. Script still
   never fetches. Fix: capture exit status, UNKNOWN→PRESERVE, add `git rev-parse --verify` guard
   + `git fetch origin --quiet`, at BOTH call sites (also `fleet-droid.sh:p0_worktree_setup`).
3. **rig #104 compaction + vacuous test** — **CONFIRMED both halves.**
   `fleet/tests/memory-compact-hook.test.sh:180-182` sets `HOME=/dev/null/should-not-be-touched`
   and asserts `[ ! -e "/dev/null/should-not-be-touched" ]` — a path under `/dev/null` can never
   exist, so f1 passes unconditionally regardless of the script. Dry-run against the live index
   still reports `pointer lines preserved: 0; truncated to pointer: 118`.
   **NEW finding:** `git diff --stat master..feat/memory-index-compaction` = 128 files,
   73,100 insertions / 402,794 deletions, almost all `graphify-out/graph.json` (-277k lines) and
   `graphify-out/manifest.json`. That generated-artifact churn is unrelated debris and must be
   stripped before this branch is landed in any form.
4. **rig #105 `assign.py` swap in `main()`** — **CONFIRMED.** The provider swap
   `grades = _DispatchGradesProvider(args.tsv) if args.tsv else _DispatchGradesProvider()` sits
   in `main()` **above** the `if args.candidates:` block that follows it 4 lines later — so the
   discriminator is already parsed and available at the swap site, un-gating F13 for every
   `assign.sh` invocation. Fix as previously specified (gate on `args.candidates`).
5. **product #170** — **CONFIRMED both halves.** `tools/check_arch.py:269 _scan_module_level_imports`
   still does `for node in ast.walk(tree)` at line 291 → descends into function bodies, so the two
   deferred imports (`config/keyprobe.py:41`, `providers.py:260`) are counted as module-level edges
   and manufacture the cycle. And the diff contains `- state_dir: str = api.DEFAULT_STATE_DIR` /
   `+ state_dir: str = ".charon"` — an SSOT break in product source to silence the second false
   positive. Verdict stands: **DO-NOT-MERGE**. Blast radius is worse than the note implies: the
   branch also deletes 119 lines of `tests/test_providers.py` and 63 of `tests/test_recommend.py`.

## 5. Two pre-existing reds — REPRODUCED on current master, ONE shared root cause

### Reproduction
```
$ python3 fleet/capability/selftest.py
  ci-infra -> None ; money-path -> None ; routing -> None ; ... (all 7 work_classes -> None)
  [FAIL] at least 2 distinct models win across the 7 work_classes — winners: [None]
  IndexError: list index out of range   (selftest.py:115, money_rank[-1])

$ bash fleet/tests/assign-dispatch.test.sh   -> 3 passed, 3 failed
  FAIL a1 real-outcome pick (modelB) leads the resolved chain (got 'modelA,modelB,modelC')
  FAIL d1 --print-model prints ONLY the picked model id (got '')
  FAIL e1 pick stays within the offered --candidates set (got '')
```

### Root cause — the SAME regression, not two bugs
Commit `9f644a1 feat(eval-promotion-gate): control-panel discrimination gate on both write paths`
added the F13 control-panel admission gate to `fleet/capability/grades.py` (+173 lines).
`_rows_for()` at `grades.py:578` now defaults `require_control_panel: bool = True`
(`_control_panel_for` at `:505`). Neither consumer supplies a control-panel row:

- `selftest.py` drives `ScorecardGradesProvider` over its `FIXTURE_TSV` frozen fixture, which has
  no control-panel row → every row denied → every `assign()` returns `picked=None` and an empty
  `ranked` list → `money_rank` is `[]` → `money_rank[-1]` raises IndexError. The IndexError is the
  *symptom*; the 7 `-> None` lines printed above it are the actual failure.
- `assign-dispatch.test.sh` drives the dispatcher's real-outcome lane through the same gate →
  live rows denied → `--print-model` emits nothing (d1, e1) and the resolved chain silently falls
  back to the unchanged static order (a1). Note b1/c1 still PASS precisely because those two cases
  *expect* the static fallback — the gate makes every case look like "no live data".

So `9f644a1` (an F13 write-path gate) leaked into the **read** path and denies all grades.

### Suggested host ticket — **ASSIGN-DISPATCH-PICK-FIX** (rig PR #105) for BOTH
That ticket exists for exactly this seam: "bypass F13 control-panel gate on dispatcher's
real-outcome lane". Both reds are the F13 gate denying a pre-vetted lane. Fixing #105 correctly
(gating the provider swap on `args.candidates`) clears the `assign-dispatch.test.sh` failures
directly, and the same `require_control_panel=False` analysis carve-out clears `selftest.py`.

Scope caveat for the build brief: `ASSIGN-DISPATCH-PICK-FIX` currently declares
`owns: fleet/capability/assign.py` only. Both fixes reach `fleet/capability/selftest.py` and
(for the read-path carve-out) `fleet/capability/grades.py`. **Widen owns to
`fleet/capability/assign.py, fleet/capability/selftest.py, fleet/capability/grades.py`** in the
same brief — per never-ignore-preexisting-issues these are fixed inside current work, never as a
separate cleanup ticket. Widening does NOT create a collision: no other live or open-PR ticket
owns those paths.

This ticket is `work_class: rig-meta` with `work_class_note: MONEY-adjacent — a wrong pick routes
real work/spend to the wrong model` → **adversarial review required**.
