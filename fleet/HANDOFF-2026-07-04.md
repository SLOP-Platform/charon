# Charon Manager HANDOFF — 2026-07-04

> **Bootstrap one-liner (paste into a fresh session to take over as manager):**
>
> ```
> Read and fully follow /home/stack/charon-private/fleet/HANDOFF-2026-07-04.md — you are the fresh Charon fleet MANAGER session. Start at "DO NEXT", re-verify all durable state yourself via git/gh/ssh (NOT the handoff's claims), and after ANY operator-action hand-off or question, STOP and wait. You gate/merge/push/dialogue only; run substantive work in sub-sessions; never spawn droids.
> ```

**Role reminder:** you are the fleet MANAGER. You watch, gate, review, merge, push, and talk to the operator. You do NOT build product in this session — route product builds to fresh DeepSeek/Claude droid tabs the operator opens. Run your own investigate/design/audit/review work in sub-sessions to keep this primary lean. Always apply a standing blast-radius + outside-the-box lens; auto-spawn a read-only review on high-blast-radius triggers.

---

## DO NEXT (in order)

1. **DEPLOY v0.3.1 to 4-LOM.** This is the critical path — see §1. **v0.3.1 published GREEN at handoff** (release run `28690333579`: image-smoke ✓ + gate ✓ + publish ✓; the `v0.3.1` image tag is live on GHCR). Quick sanity re-check before deploying:
   ```
   gh api /orgs/SLOP-Platform/packages/container/charon/versions --jq '.[] | select(.metadata.container.tags|length>0) | .metadata.container.tags'
   ```
   Expect `["v0.3.1"]` present → proceed straight to deploy (§1).
2. **Add CommandCode as a provider** (operator-directed, biggest cost win) — §3. Re-supply `COMMANDCODE_API_KEY`, probe, add to `secrets.json` + built-in preset, place PRIMARY in `deepseek-v4-pro` pool and favorably in Claude pools.
3. **Launch the signed-off build sessions** — §4. Hand the operator the two DeepSeek launch one-liners (SR chain + TIER-SELECT-A). They can run in parallel (disjoint owns).
4. Keep gating the proxy_server.py single-writer chain and the RFL/SR-13 tail — §4/§5.

---

## 1. RELEASE / DEPLOY — the critical path

### What happened
- SR wave **SR-1/2/3/5/10/11 merged to `master`** via PR #82 (feat/prod-install; master was `5057143`). Version bumped **0.2.0 → 0.3.0**.
- **v0.3.0 image NEVER published.** The `release.yml` `image-smoke` job runs on the self-hosted **4-LOM runner** and bound host **`:8080`**, which the **production gateway already holds** → `port is already allocated`. Release run failed; GHCR stayed at `v0.2.1`.
- **v0.3.1 republish:** first attempt (PR #84, commit `fcf5bd5`, "bind smoke-test to a free host port") **still failed** — a fixed host port + fixed container name is not concurrency-safe against the live runner.
- **Second fix (PR #85, commit `f72d577`) — "make image-smoke concurrency-safe": ephemeral host port + run-scoped container name + per-ref concurrency group.** Its release run (`28690333579`) went fully **GREEN** (image-smoke 8s ✓, gate 1m40s ✓, publish 37s ✓). **The `v0.3.1` image tag is now published on GHCR.** `pyproject` is at `version = "0.3.1"`.
- Root cause was always the smoke job colliding with the live prod gateway on the shared self-hosted runner — the fix (ephemeral-port + unique-container + concurrency-group) resolved it. If a future release regresses, that's the pattern to preserve.

### DEPLOY to 4-LOM (`10.0.1.60`)
- **Goal:** replace the live band-aided container with the **source-matched v0.3.1 image**, moving toward **SR-10's repo compose** (single-producer, image-only; `CHARON_HOME` + `CHARON_STATE_DIR=/data`; command `charon gateway`).
- **BACKUP `/data` FIRST** (pools/keys/reorders live there and MUST survive — see §2). Then pull the new tag, swap, verify health + all 47 pools, rollback on any failure.
- Deploying the source-matched image **retires the live band-aids** (see §2) since the fixes are now in the image.
- **Deploy drift lessons (memory `charon-deploy-drift-lessons`, SR-10 D024):** deployed image has historically ≠ source — `CHARON_HOME` empty → keys missing; opencode-zen dropped; entrypoint differed. **All config/secrets/state must sit on the mounted `/data` volume.** Verify env (`CHARON_HOME`, `CHARON_STATE_DIR=/data`) and command after swap.
- **Access pattern:** `ssh -i ~/.ssh/4lom stack@10.0.1.60`. Local docker/curl are deny-listed; **ssh-wrapped docker works.** Prefer the `/charon` setup API for config; use docker-via-ssh for the container swap.

---

## 2. 4-LOM LIVE STATE (band-aids, all via `/charon` setup API hot-reload, backed up)

- **Silent-downgrade leak CLOSED** (the namespaced-id double-bill).
- **Provider keys live ONLY in `/data/secrets.json`:** `deepseek-direct`, `nanogpt`, `neuralwatt`, and a **valid openrouter inference key**. (Public repo — never commit keys/IPs/hostnames/`/home/stack` paths.)
- **Cross-account fallback across 47 pools:** `opencode-zen`/`go` primary → `deepseek` / `nanogpt` / `neuralwatt` / `openrouter`.
- **`gpt-5.5` + `gpt-5.4-pro` reordered openrouter-FIRST** (opencode returns a terminal `400` for them).
- These are runtime reorders/keys on the `/data` volume; deploying the source-matched image keeps them (volume persists) and retires the *image-level* band-aids.

---

## 3. COMMANDCODE — add it (operator-directed, high value)

**Why:** operator's CommandCode **"Go" plan = $40 of DeepSeek-V4-Pro usage for $1/mo** → far cheaper than metered opencode-zen. This directly fixes the original "balance burning" problem.

- **Key:** `COMMANDCODE_API_KEY` — in the session transcript / to be re-supplied by operator.
- **base_url:** `https://api.commandcode.ai/provider/v1`
- **Verified OpenAI-compatible** (`/models` → 200). Serves **BOTH** Claude models (opus-4-8, sonnet-5, fable-5…) **AND DeepSeek V4 Pro**.

**Do:**
1. Run a real **completion probe** (not just `/models`) to confirm chat/completions works.
2. Add key to `/data/secrets.json` (4-LOM) and add a **built-in preset in `src/charon/providers.py`** — ticket it (product change, goes through the normal gate/merge chain; carries no fleet/SLOP strings).
3. Place it **HIGH — likely PRIMARY — in the `deepseek-v4-pro` pool**, and favorably in the Claude pools. Manual ordering now; automatic once **SR-5b** lands (cost-aware).

---

## 4. TICKETS / BUILD PLAN

Operator wants **DeepSeek V4 Pro for non-critical work to conserve Claude.** Full WCI plan: `REMAINING-WORK-OPTIMIZED.md`. The one real bottleneck is the **`proxy_server.py` single-writer chain** — six tickets want that file; everything else is file contention, not logic deps.

### Signed off & buildable NOW

**A) SR chain: SR-7 → SR-5b → SR-8** (one DeepSeek session, **STRICTLY SEQUENTIAL** — all own `proxy_server.py`).
- Order matters: **SR-5b immediately after SR-7.** SR-7 removes the `cost>0` record guard (so zero-cost served calls still advance the universal cap); **SR-5b must NOT reinstate that guard** — it feeds its computed `cost_usd` into the same call sites SR-7 hardened.
- SR-8 = wire the 6 constructed-but-dead modules; decision CLEARED/operator-approved. Recommendation: **WIRE all six** — 4 always-on (observability, request_inspector, session_affinity, virtual_keys inert-unless-configured), 2 opt-in/OFF-by-default (speculative_execution, consensus — spend multipliers). See `SR-8-RECS.md`.
- Brief: `SESSION-CHAIN-SR7-SR5b-SR8.md` (stacked branches: SR-7 off master → SR-5b off SR-7 → SR-8 off SR-5b; commit each, do NOT push/PR — Claude reviewer + operator gate).
- **DeepSeek launch one-liner:**
  ```
  Read and fully follow /home/stack/charon-private/fleet/SESSION-CHAIN-SR7-SR5b-SR8.md
  ```

**B) TIER-SELECT Phase-A** (`model_catalog.py` NEW + `cli.py` picker; disjoint from proxy_server.py) → **parallel** DeepSeek session, SAFE alongside the SR chain.
- Catalog is pure stdlib DATA, provider-agnostic, no fleet/SLOP strings in `src/`. Phase-B (web picker, touches proxy_server.py) is deferred.
- Brief: `SESSION-TIER-SELECT-A.md`.
- **DeepSeek launch one-liner:**
  ```
  Read and fully follow /home/stack/charon-private/fleet/SESSION-TIER-SELECT-A.md
  ```

### Approved / staged
- **SR-6 Phase-1** — un-gated, approved: default-ON `anthropic_prompt_cache` — inject exactly one `cache_control:{"type":"ephemeral"}` breakpoint on the last system block (or last tool) of an already-Anthropic body, only when the prefix clears ~2048 tokens; idempotent; OpenAI-wire routes byte-identical passthrough. Full design + ticket-ready spec in `SR-6-DESIGN.md`. Owns `translate.py` (NEW) + `proxy_server.py` (+ plumbing in `gateway.py`/`providers.py`). Claude, on the proxy_server.py chain.
- **SR-6 Phase-2** — full bidirectional OpenAI↔Anthropic translation (request+response+SSE remap). Created & **PARKED** (`board/SR-6-Phase2.md.parked`) — the large/risky surface, not needed to bank the saving.
- **SR-13** — GUI session-login. Design in `AUTH-GUI-DESIGN.md`. Sequence **after SR-5b**; it's the current TAIL of the proxy_server.py chain (refactors `_handle()`). Coordinate with token rotation (§6). Claude.

### RFL-1..5 (created; chain after SR-13 on proxy_server.py)
From `RELAYFREELLM-COMPARISON.md`. All except RFL-5 collide on proxy_server.py (single-writer), so they queue behind SR-13.
- **RFL-1 — proactive quota tracking = TOP priority** (pre-flight sliding-window RPM/RPD/TPM check). depends_on SR-13.
- **RFL-2 — chat playground UI** (`/chat`). depends_on RFL-3.
- **RFL-3 — image/vision-aware routing.** depends_on RFL-1.
- **RFL-4 — per-provider limit editor** (console POST endpoints) → **DeepSeek**. depends_on RFL-2, RFL-1.
- **RFL-5 — context compaction** (stdlib extractive TF/reservoir, EXPERIMENTAL, opt-in/OFF-by-default, own file `context_shaper.py` → NO proxy_server.py collision, tier `frontier`) → **Claude**.

### Also worth ticketing (from `X-POST-EVAL.md`)
- **Tool-call repair module** (`tool_repair.py`) — **the biggest lever for DeepSeek-V4 reliability; Charon has none today.** Schema-only validate-then-repair on `message.tool_calls[].function.arguments`, symmetric to `response_normalizer`, stdlib-only, config-gated, **OFF by default for state-mutating calls** (repair format/schema, never guess semantic values), per-rule observability counter. ~1 ticket, medium effort. (Ignore the "16,000 variations / 6-of-10-beats-Opus" vendor hype — a couple dozen rules at Charon scale.)

---

## 5. KEY GOTCHAS (do not relearn the hard way)

1. **Local gate MUST be `python3 -m charon.cli gate`** (runs ruff/mypy/boundary/version/gate-registry) **plus** `PYTHONPATH=src python3 -m pytest -q`. Bare `mypy src/charon` misses tests and **REDDENS CI — it bit us twice.**
2. **Push ONLY via `bash /home/stack/charon-private/fleet/land-push.sh <branch>`.** Raw `git push` and the `git -C <path> push` form are both deny-listed. The **AUTONOMOUS flag is ON** (`state/AUTONOMOUS`), so land-push pushes directly; if it's ever flipped OFF, land-push refuses and prints the operator's manual command.
3. **Prod edits (4-LOM):** `ssh -i ~/.ssh/4lom stack@10.0.1.60`. Use the `/charon` setup API or docker-via-ssh (local docker/curl are deny-listed; ssh-wrapped works). **ALWAYS BACKUP `/data` first** (and before ANY data-loss-risky op — docker rm/down, redeploy, config overwrite).
4. **Token rotation DEFERRED** — rotating logs out the operator's opencode client; do it ONLY when they're at the terminal. Coordinate with SR-13 (both touch `secrets.json`; single writer).
5. **All keys in `secrets.json` only.** Repos are PUBLIC — never commit tokens/IPs/hostnames/`/home/stack` paths/dev-meta.
6. **Product vs build-rig boundary:** Charon ships standalone; fleet/SLOP/runner never leak into `src/`.
7. **Manager discipline:** don't build product here; open droid tabs via operator; give the exact copy-pasteable command for anything the operator must run; pause after any question/handed action.

---

## 6. SEQUENCE HAZARDS
- **proxy_server.py single-writer chain** (the critical path): `SR-7 · SR-5b · SR-6 · SR-8 · TIER-SELECT-B · SR-13 · RFL-1 · RFL-3 · RFL-2 · RFL-4` — one writer at a time, any order but ungated-first. SR-6/SR-8 gates are cleared, so no stall.
- **SR-7 ↔ SR-5b** share the exact spend-limiter call sites (~`:659` pre-flight check, ~`:801` record). SR-5b must build ON TOP of SR-7 — never reinstate the `cost>0` guard.
- **cli.py:** TIER-SELECT-A writes it before SR-13.
- **SR-13 ↔ token rotation:** land SR-13 after rotation or coordinate the single `secrets.json` writer.

---

## 7. REFERENCE DOCS (in `/home/stack/charon-private/fleet/`)
- `REMAINING-WORK-OPTIMIZED.md` — WCI wave plan / batching / dependency graph.
- `SR-6-DESIGN.md` — Phase-1 spec + Phase-2 shape.
- `SR-8-RECS.md` — per-module wire/remove recommendation (all 6 → wire).
- `RELAYFREELLM-COMPARISON.md` — source of RFL-1..5.
- `X-POST-EVAL.md` — CommandCode/Awais thread eval → tool-repair ticket.
- `AUTH-GUI-DESIGN.md` — SR-13 GUI login design.
- `SESSION-CHAIN-SR7-SR5b-SR8.md`, `SESSION-TIER-SELECT-A.md` — the two DeepSeek build briefs (launch one-liners in §4).
- Board tickets: `board/SR-*.md`, `board/RFL-*.md`, `board/TIER-SELECT.md`. Wave manifest: `waves/smart-routing.json`.
- Product standing orders: `/home/stack/code/charon/AGENTS.md`; process: `WORKFLOW.md`; decisions: `/home/stack/code/charon/docs/DECISIONS.md`.
- Memory: `charon-project-state`, `charon-deploy-drift-lessons`, `charon-silent-downgrade-leak`, `investigate-and-backup-before-data-loss`.

---
*Durable state was verified via git/gh at handoff (releases, run 28690333579, GHCR tags, board files). Re-verify before acting — do not trust these numbers blind.*
