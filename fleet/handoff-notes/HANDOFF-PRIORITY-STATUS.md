# HANDOFF-PRIORITY-STATUS — audit of every priority handed off by session `satele-shan`

**Audited:** 2026-07-31 (read-only; nothing modified, committed, or pushed)
**Source of truth:** `/home/stack/charon-private/fleet/SESSION-HANDOFF-satele-shan.md`
**Method:** primary evidence only — board files, `fleet/state/{claims,submitted,done}/`, `gh pr view/list`
on both repos, and `git log origin/master` in both repos. The handoff's own claims were NOT trusted.

**Repo tips at audit time**
- rig `Nnyan/charon-private` origin/master = `dce0a70` (handoff recorded `aa22276` → **+70 commits**)
- product `SLOP-Platform/charon` origin/master = `f87d4ae` (handoff recorded `f87d4ae` → **0 commits; product master has not moved at all**)

The successor session (`tott-doneeta`) worked from ~15:55 PDT onward and did substantial rig work.
It wrote **no handoff of its own** — `fleet/SESSION-HANDOFF-satele-shan.md` is still the newest.

---

## STATUS TABLE

| Item | Gate | State | Evidence (SHA, PR#, file path) | Remaining work |
|---|---|---|---|---|
| **GATE 0 — five re-measurements** | 0 | **DONE** | Commit `32f65e3` "board-hygiene: GATE 0 re-measure — all five premises hold; litellm used is 6 not 5"; results written into `fleet/SESSION-HANDOFF-satele-shan.md:58-79` with today's date, attributed to session `tott-doneeta`. All 5 premises hold; no ticket re-scoped. | None for the gate. #2 (36/2570 opencode-vs-gateway) and #4 (10 KSF gates on disk) are LIVE and will drift — re-run before quoting. Anything still quoting "5 of 52" litellm params must say **6**. |
| ├ #1 catalog context coverage | 0 | DONE | 216 of 859 — EXACT match to claim (`:62`) | — |
| ├ #2 opencode list vs gateway | 0 | DONE | 36 listed / 2570 served — drifted +3 served, premise holds and is worse (`:63`) | live number, re-measure |
| ├ #3 litellm Router params | 0 | DONE (**corrected**) | `litellm_router.py:363-370` passes **6**, not 5 (`:64,:68-71`). ADR-0021's 28/14/16 disposition unaffected | — |
| ├ #4 KSF gates on disk vs registered | 0 | DONE | 10 on disk / 5 registered / 0 modules — drifted +1 (`:65`) | live number, re-measure |
| └ #5 two capability engines LOC | 0 | DONE | rig 3295 / product 3621 — EXACT (`:66`) | — |
| **GATE 1 — ORPHAN-CLAIM-FORENSICS** | 1 | **DONE — DONE-WHEN literally true** | PR **#289** MERGED 2026-07-31T23:29:20Z, commit `9253547` (`reconcile-stale-claims --orphans` mode). Ticket archived: `fleet/board/archive/ORPHAN-CLAIM-FORENSICS.md`; done marker `fleet/state/done/ORPHAN-CLAIM-FORENSICS`; retired at `abe9a2e`. **Re-ran `bash fleet/validate_board.sh` → `0` RED, "GREEN board structurally valid".** 9 destroyed tickets restored at `e4e5582`. | None for the gate. But the restore was **incomplete** — see STOP-WORKER row. |
| **ADR-0021 / LITELLM-CAPABILITY-ADOPTION** | 2 (#1) | **IN-FLIGHT — STRANDED. The one GATE 2 item with zero progress.** | Worktree `/home/stack/charon-wt/LITELLM-CAPABILITY-ADOPTION` @ `0cd6b6d` on `design/litellm-capability-adoption`, parented on `f87d4ae`. **No PR exists** in `SLOP-Platform/charon` (checked #175-#204). `docs/adr/0021-litellm-capability-adoption.md` **ABSENT** from product origin/master (`git ls-tree origin/master docs/adr/` → no 0021). Board ticket LIVE: `fleet/board/LITELLM-CAPABILITY-ADOPTION.md` (restored at `e4e5582`). Claim marker LIVE: `fleet/state/claims/LITELLM-CAPABILITY-ADOPTION` (heartbeat 1785519177). | Open a PR and land it. The handoff called this "docs+test, zero risk, do this first" and it is the only unlanded GATE 2 item that needs no rebase. Claim is held by a worktree that is otherwise idle. |
| **feat/ticket-lifecycle-canary** | 2 (#2) | **DONE** | PR **#293** MERGED 2026-07-31T23:52:03Z (`18e84eb` merge, `5c608db` content). Ticket archived `fleet/board/archive/TICKET-LIFECYCLE-CANARY.md`, retired at `f47ded1`, done marker `fleet/state/done/TICKET-LIFECYCLE-CANARY`. | None. |
| **review/reconcile-gate-design** | 2 (#3) | **DONE (merged) — retirement MISSED** | PR **#182** MERGED 2026-07-31T23:51:35Z, merge commit `5e7e0f1` "review(design): adversarial verdict on UNIFIED-RECONCILIATION-GATE-DESIGN (PR #178)". | Board hygiene debt: `fleet/board/REVIEW-RECONCILE-GATE-DESIGN.md` is **still in the active board**, still has a `fleet/state/submitted/REVIEW-RECONCILE-GATE-DESIGN` marker, and is **not** in `fleet/board/archive/`. Retire it. |
| **feat/fixture-bypass-gate** | 2 (#4) | **DONE (merged) — retirement MISSED, one decision open** | Ticket minted PR **#295** (`f67b890`) resolving the handoff's "HAS NO BOARD TICKET" blocker. Round-2 scope PR **#311** (`65e4382`). Branch landed: PR **#312** MERGED 2026-08-01T04:10:27Z, commit `dc23209`. Gate files now on origin/master: `fleet/checks/fixture-bypass.sh`, `fleet/checks/gate-integrity.sh`, `fleet/tests/fixture-bypass.test.sh`, `fleet/tests/gate-integrity.test.sh`. | Board ticket `fleet/board/FIXTURE-BYPASS-GATE.md` **not archived**, `fleet/state/submitted/FIXTURE-BYPASS-GATE` still set. The round-2 "**drive the baseline to ZERO and delete the baseline mechanism**" decision is UNRESOLVED — `dc23209` re-baselined to 36 findings (19 new on master), i.e. the 20-entry baseline the ticket warned against is now a 36-entry baseline. `FIXTURE-BYPASS-GATE.md:84-92` explicitly says "do NOT quietly keep a baseline". |
| **feat/substrate-first-gate-v2** | 2 (#5) | **NOT-STARTED (correctly sequenced last, never reached)** | origin branch `feat/substrate-first-gate-v2` @ `c182d7e`; worktree `/home/stack/charon-private-wt/SUBSTRATE-GATE-V2`. **No PR** in any state. Drift RE-MEASURED just now: `git rev-list --count origin/master...HEAD` = **510** (handoff said "~450 and RISING"; a prior session said 419→444→451). Done markers exist only for the *predecessor* tickets `fleet/state/done/SUBSTRATE-FIRST-GATE` and `SUBSTRATE-FIRST-OWNS-BASE-REF`. | The whole job: a 510-commit rebase, then land. Manager cannot do it (`git rebase`/`merge` denied) — must be delegated to a worker tab. Content verified good (+1,662/-38 three-dot); do NOT re-condemn from a two-dot diff. |
| **CATALOG-COMPLETENESS** | 3 (#1) | **STAGED (DONE) + BUILD IN-FLIGHT** | Ticket minted `c29e6c6` (PR **#306**); **premise CORRECTED** `96dd404` (PR **#307**) — the original "0 of 859 priced" was wrong (grepped for `price`/`cost`, actual key is `cost_rank`): **212/859 carry `cost_rank`, 859/859 carry a `free` flag**, `cost_map.json` absent. Real defect re-scoped to **pool ordering ignoring the cost data it already has** (openrouter rank=50 ahead of deepseek-direct rank=8). `fleet/board/CATALOG-COMPLETENESS.md` live; `fleet/state/submitted/CATALOG-COMPLETENESS`; brief `fleet/state/agent-briefs/strong-1371711-CATALOG-COMPLETENESS.md`. Product PR **#203** OPEN (draft) `feat/catalog-completeness` created 2026-08-01T01:19:24Z. | Land PR #203. Spawned sibling `FORWARDER-COST-ORDER-FALLBACK` (P0 money) minted at `07bef51` (PR #314) — that is where the real ordering fix lives now. |
| **SPAWN-VIA-CAPABILITY** | 3 (#2) | **NOT-STARTED — SILENTLY DROPPED** | Zero evidence anywhere. `grep -rl SPAWN-VIA-CAPABILITY fleet/` returns **only `fleet/SESSION-HANDOFF-satele-shan.md`**. No board file, no archive file, no state marker, no commit in any ref (`git log --all --grep` → only the handoff-edit commit `1dc333b`), no PR in either repo. Confirmed unbuilt at source: `grep -nE "assign\|capability\|recommend" fleet/spawn-worker.sh` → **0 hits**; spawn-worker is still TOLD a model. | Write the ticket. The operator's binding constraint lives ONLY in the handoff: it **MUST cover ad-hoc briefs, not just tickets** — of ~14 lanes launched that session NONE had a ticket, so a ticket-only fix routes nothing. Briefs carry routing fields (effort, work-class, expected context size); overrides become logged exceptions. |
| **ENGINE-CONVERGE** | 3 (#3) | **NOT-STARTED — SILENTLY DROPPED** | Zero. Occurrences limited to `fleet/SESSION-HANDOFF-satele-shan.md` and one passing mention in `fleet/board/TOOL-COMPOSITION-LAYER.md`. No ticket, no marker, no commit (`git log --all --grep=ENGINE-CONVERGE` → empty), no PR. Premise re-verified at GATE 0 and unchanged: rig 3295 LOC vs product 3621 LOC. | Write the ticket. Scope from the handoff: rig `fleet/capability/` and product `src/charon/capability/` + `engine/` are two implementations of one concept; both ship a `grades.py`; product engine duplicates board/claim/reconcile the rig has in shell. ADR-0007 and ADR-0010 already mandate ONE engine — respect ADR-0007's opt-in constraint. |
| **PRICING-FEED** | 3 (#4) | **NOT-STARTED as a ticket — PARTIALLY ABSORBED. Highest information-loss risk on this list.** | No `fleet/board/PRICING-FEED.md`, no archive file, no marker, no PR. The litellm half survives ONLY as the `substrate:` line of `fleet/board/CATALOG-COMPLETENESS.md` (which points at `model_prices_and_context_window.json` and explicitly says "See the PRICING-FEED scope note in the satele-shan handoff" — a pointer to a document with no ticket behind it). `fleet/state/EVAL-REGISTRY.md` has **ZERO rows** for pricepertoken / benchlm.ai / whatllm.org / cheahjs free-llm-api-resources / 12britz awesome-free-models (grep confirmed); the only pricing row is LiteLLM-as-data-source (2026-07-12, ADOPT). | Write the ticket **before this handoff ages out**. Three operator inputs exist ONLY in `SESSION-HANDOFF-satele-shan.md:104-110` and nowhere else in the repo: (a) pricepertoken.com ships a **Price-Per-Token MCP** (live pricing + benchmarks, ~3 lines to wire); (b) **MCP-first is the preferred integration shape — check EVERY candidate for one**; (c) for EXTERNAL reference data prefer **multiple corroborating sources over SSOT** — disagreement between feeds is itself a signal; SSOT applies to data we OWN and is a category error for facts we OBSERVE. Multiple sources → ONE derived store with declared precedence. Criterion: API-addressable vs webpage-only. |
| **ORCHESTRATION-RE-RUN** | 3 (#5) | **NOT-STARTED — SILENTLY DROPPED** | Zero. Only occurrence is the handoff. No ticket, no marker, no commit, no PR. `fleet/state/EVAL-REGISTRY.md` grep for crewai/langgraph/autogen/"agents sdk" → **no rows**; the handoff's claim "**NO agent-orchestration framework has ever entered the registry**" is CONFIRMED still true. The CrewAI dismissal still lives only at `fleet/handoff-notes/RESEARCH-AGENT-COMMS-2026-07-26.md:312`. Note: an adjacent research lane DID land (`7b2af7f`/`73dd813` WORK-MGMT-LAYER, 32 candidates, incl. LangGraph-adjacent workflow engines) but it evaluated *work management*, not agent orchestration, and covers none of the four named frameworks. | Write the ticket. Add **CrewAI, LangGraph, AutoGen, OpenAI Agents SDK**. Re-derive CrewAI (it was dismissed on ONE axis — "`kickoff()` owns the loop; no external control surface" — under the OLD lens, which the corrected lens says must be RE-DERIVED, not cited). Frame: "these own the agent loop and we must stay agent-agnostic — what capability is worth taking anyway?" **HARD RULE: verdicts land in `fleet/state/EVAL-REGISTRY.md`, never in a handoff note.** |
| **GATE 4 — LETTA-REVIEW.md + MEMORY-LAYER-REVIEW.md** | 4 | **NOT-DONE — reports still unread/unreported (one incidental partial consumption)** | Both files exist and are substantial: `fleet/handoff-notes/LETTA-REVIEW.md` (16,925 B) and `fleet/handoff-notes/MEMORY-LAYER-REVIEW.md` (15,490 B), both dated Jul 31 12:32, plus brief copies under `fleet/state/agent-briefs/`. **`grep -c "Letta" fleet/state/EVAL-REGISTRY.md` = 0** — no registry row for any Letta finding. MEMORY-LAYER-REVIEW is cited only as a **secondary receipt** on the Graphiti REJECT row (`EVAL-REGISTRY.md:69`), and that row was written by a *different* lane (CONNECTION-GRAPH, `71e1d85`) evaluating Graphiti for a different purpose. No commit, note, or artifact shows the verdicts being summarised or delivered to the operator. | Read both and report. Verdicts sitting undelivered: **LETTA-REVIEW** — REJECT Letta; REJECT mnemostroma (broken); **ADOPT-PARTIAL basic-memory scheduling** (a cron set at adopt-decision time to follow up on integration — directly aimed at the "adopted-not-fully-used" #1 class). **MEMORY-LAYER-REVIEW** — REJECT Mem0 (cloud-upsell telemetry, no auto-extraction), REJECT Zep+Graphiti (Zep=SaaS egress; Graphiti alone needs Neo4j), REJECT LangMem (LangChain/LangGraph lock-in), REJECT Evermind (alpha, stub health-check), plus Lucid/Hypabase/Forgetful assessments. **~10 verdicts are stranded in handoff notes, violating the HARD RULE.** Land them into EVAL-REGISTRY. The LAND-SH-SAFE-SYNC benchmark the gate asked for WAS applied — but by the CONNECTION-GRAPH lane, not GATE 4. |
| **5a — `inert_code` never proven to catch `litellm_plane`** | 5 | **INSTANCE NOT-STARTED · CLASS QUESTION ANSWERED** | Instance: no run recorded anywhere. The benchmark is written into `fleet/board/KSF-LOAD-BEARING.md:86` ("`litellm_plane` present in..."), and that ticket has **no claim, submitted, or done marker** → never worked. `fleet/handoff-notes/KSF-SURFACE-AUDIT.md:7,58` records the *speed* hypothesis as REFUTED (`fece8fa`: 0.066s keystone / 4.464s charon) and the real blocker as **noise — 200+ false positives** from `inert_code.py:269 _resolve_call()` failing on re-exports. Class question ("how many gates are trusted on a docstring rather than a watched RED?") is now **MECHANIZED**: `fleet/checks/gate-integrity.sh` landed at `dc23209` and reports **36 findings, 19 new on master** — the believed-but-never-observed population is measured, not guessed. | Run `inert_code` against charon and report (a) the false-positive count and (b) whether `src/charon/litellm_plane/` actually surfaces. Until that is done, KSF-LOAD-BEARING remains mis-scopeable. |
| **5b — what CREATES a claim whose ticket vanishes** | 5 | **DONE — root-caused and gated** | Root cause recorded in `fleet/board/archive/ORPHAN-CLAIM-FORENSICS.md:75-84`: "**The disappearance mechanism is a MERGE, not a delete**", tying back to `board-lock.sh commit` writing bare onto local master while origin wraps the same content in a merge. Forensics landed at `10e12bb`; classifier shipped at `9253547` (PR #289); the mechanism is now GATED by **MERGE-DROP-RATCHET** — ticket `5b27716` (PR #290), fix `8bc7299` "require archive move in merge diff" (PR #291), retired at `6ae7996`. | None on the gate. **But the answer under-counted the damage** — see STOP-WORKER row: at least 10 tickets were destroyed, only 9 restored. |
| **5c — is KSF-LOAD-BEARING the wrong call (FIX vs ABANDON)** | 5 | **NOT-ANSWERED** | `fleet/board/KSF-LOAD-BEARING.md` still prescribes the 4-step FIX; the abandon option appears only as a FRAMING hypothesis ("the honest answer may be that `inert_code` should be scoped down rather than fixed. **Say so if that is what you find**" — `:52-56`). No claim/submitted/done marker; no commit or PR since `8c90b00`/`a2e6671` (PR #286) renamed FRAMEWORK-CONVERGE → KSF-LOAD-BEARING. The outside-the-box alternative (`fleet/handoff-notes/KSF-CLASS-CORPUS.md` — 15 classes, 10 ungated — is the durable prize and does not need KSF) was never evaluated. **Counter-evidence accumulated meanwhile:** product PR **#195** `feat/ksf-vendor-gates` MERGED — charon vendored 5 KSF gates, entrenching the exact "forked per-repo, not consumed" pattern the audit condemned. | Answer the question honestly before any KSF build starts. Vendoring landed while the abandon question stayed open, so the cost of a later ABANDON is now higher than when the gate was written. |
| **5d — GATE-GAP-LEDGER feedback loop** | 5 | **DONE (fed once)** | `fleet/state/GATE-GAP-LEDGER.tsv` gained exactly one row post-handoff, at commit `e60bcbb` (2026-07-31T15:55:24-07:00), verified by `git diff aa22276..origin/master -- fleet/state/GATE-GAP-LEDGER.tsv` (+1 line, 0 deletions — append-only respected). Row: `2026-07-31 · doctrine-outlives-constraint` — the session lost its whole first phase telling the operator to open droid tabs and push, because `MANAGER-OPERATING-RULES.md:54` and memory `manager-never-spawns-droids` asserted a limitation that had already shipped away (`bfb86d2`/`d391f46`/`977dc2e`; land-push.sh + AUTONOMOUS ON since 2026-07-15). Status field: **PARTIAL**. | Two gaps. (1) The row's own `gate_improvement` — a gate ensuring a doctrine line asserting "we CANNOT do X" cites the file:line that makes it true, so the citation breaks when X ships — is explicitly **NOT built and "needs a ticket"**; **no such ticket exists on the board**. (2) The gate said silence is unacceptable: there is no explicit "no further handoff misses" record, so the 70 commits of successor work carry an unrecorded verdict either way. |
| **5e — secret detection fires too late** | 5 | **PARTIALLY ANSWERED — no verdict landed, core gap UNCHANGED** | Measured directly this audit. **Scope question ANSWERED:** `.github/workflows/gitleaks.yml` has `on: pull_request` + `workflow_dispatch` and **NO `paths:` filter** → `fleet/*.md`, `fleet/state/`, `fleet/handoff-notes/` ARE in scope. It is diff-scoped by design via `bash fleet/checks/gitleaks.sh --diff` with `fetch-depth: 0` (fail-closed on shallow), pinned to gitleaks **8.21.2**, and gated by a fail-on-revert canary `fleet/tests/gitleaks-canary.test.sh`. **`.gitleaks.toml` question ANSWERED:** the file does **not exist** — defaults only (which the handoff empirically proved DO catch a bare 32-hex in a `.md`). **Local-gate question ANSWERED — and the answer is the bad one:** no `.pre-commit-config.yaml` exists, and `grep -n gitleaks fleet/land.sh fleet/board-lock.sh fleet/land-push.sh` returns **ZERO hits**. Both chokepoints every commit passes still scan for nothing. | The finding stands unchanged: **every secret still reaches local git history before anything scans it**, and this rig mirrors to a PUBLIC GitHub repo. No written verdict was recorded anywhere (commit `8702191` only baked the *investigation* into the handoff), and no ticket was minted. Write the verdict; then decide whether to add a scan at the `board-lock.sh` / `land.sh` chokepoint. Note branch protection: the workflow header says "gitleaks" must be added to required status checks once the `charon-ci` pool is live — verify whether that was ever done. |
| **STOP-WORKER-GRACEFUL-EXIT (#283)** | queued | **ABANDONED BY ACCIDENT — ticket DESTROYED and never restored; work NOT-STARTED** | Ticket was created: `e5659d0` "board-hygiene: STOP-WORKER-GRACEFUL-EXIT (P1, queued)" via PR **#283** (merge `14a2ff1`) — `git show e5659d0 --name-only` confirms `A fleet/board/STOP-WORKER-GRACEFUL-EXIT.md`. **It is GONE now:** `git cat-file -e origin/master:fleet/board/STOP-WORKER-GRACEFUL-EXIT.md` → *does not exist*; same for `fleet/board/archive/`. Destroyed by the merge resolution at `0be7d6f` ("Merge origin/master into fix/reconcile-gate-wired-v2 — resolve preflight.sh conflict"), surfaced through PR **#285**'s merge `7b2908b` — the SAME merge-drop event as the 9 tickets, but it was **NOT among the 9 restored** by `e4e5582` (which restored BRIDGE-MIGRATE-DROID-CLIENT, LITELLM-CAPABILITY-ADOPTION, PREFLIGHT-GATE-REGISTRY, PREFLIGHT-GATE-RUN-HELPER, PREFLIGHT-OWNS-ARBITRATE, SECRET-HOTROTATE, SW-IDENTITY-FOLD, SW-STATIC-LEGS-RETIRE, archive/WCI-CONTENTION-TEETH). No claim/submitted/done marker; `stop-worker.sh` unchanged. **The board reports 0 RED because nothing claims this ticket — the loss is invisible to `validate_board.sh`.** | Re-mint from the original content: `git show e5659d0:fleet/board/STOP-WORKER-GRACEFUL-EXIT.md`. Then fix the defect: `stop-worker.sh` leaves WT tab litter on ANY escalation past SIGINT (exit 15 = SIGTERM; WT `closeOnExit: graceful` only closes on exit 0) and its own header claims otherwise. Also: the restore at `e4e5582` should be re-verified for other missed casualties — 9 were restored, at least 10 were destroyed. |
| **SHARED-NAMESPACE-CONTENTION** | queued | **STAGED (ticket landed) — BUILD NOT-STARTED** | Ticket landed: `f8266ef` via PR **#288** (merge `aa22276`). Live at `fleet/board/SHARED-NAMESPACE-CONTENTION.md` — P0, `work_class: rig-meta`, `branch: fix/shared-namespace-contention`, `owns: fleet/claim-jedi-name.sh, fleet/tests/claim-jedi-name.test.sh, fleet/spawn-worker.sh, fleet/tests/scratch-namespace.test.sh`. **No claim/submitted/done marker; branch does not exist on origin.** | Build it. Two instances of ONE class (shared mutable namespace, read mutates state, nothing reaps orphans): the Jedi-name allocator and lanes writing to bare `/tmp`. Ticket's `serial_justified` forbids splitting them. Also: PR **#287** (`board/namespace-contention`) is still **OPEN** and now largely superseded by #288 — but it still carries the un-merged archive move for RECONCILE-GATE-WIRED, whose PR #285 merged 2026-07-31T19:41 while `fleet/board/RECONCILE-GATE-WIRED.md` remains in the **active** board. Close or land #287. |

---

## NOT STARTED AT ALL

Zero evidence of any work — no board file (active or archived), no state marker, no branch, no PR,
no commit in any ref, and no reference anywhere in the repo except the handoff itself:

1. **SPAWN-VIA-CAPABILITY** (GATE 3 #2) — the only occurrence in the entire rig is
   `fleet/SESSION-HANDOFF-satele-shan.md`. Confirmed unbuilt at the source: `fleet/spawn-worker.sh`
   contains no `assign`/`capability`/`recommend` reference.
2. **ENGINE-CONVERGE** (GATE 3 #3) — handoff + one incidental mention in
   `fleet/board/TOOL-COMPOSITION-LAYER.md`.
3. **ORCHESTRATION-RE-RUN** (GATE 3 #5) — handoff only; EVAL-REGISTRY still has zero rows for any
   agent-orchestration framework.
4. **PRICING-FEED** (GATE 3 #4) — no ticket. Only a `substrate:` pointer inside
   `fleet/board/CATALOG-COMPLETENESS.md` that forwards the reader back to the handoff.
5. **feat/substrate-first-gate-v2** (GATE 2 #5) — branch exists at `c182d7e` but has never had a PR;
   drift is now **510** commits (re-measured, not cited).
6. **SHARED-NAMESPACE-CONTENTION** build (ticket exists; no branch, no marker, no code).
7. **STOP-WORKER-GRACEFUL-EXIT** build (and the ticket itself no longer exists).
8. **KSF-LOAD-BEARING** build — the ticket is P0 and unclaimed; 5a's benchmark run and 5c's
   FIX-vs-ABANDON question both sit inside it, untouched.
9. **5d's follow-on ticket** — the GATE-GAP-LEDGER row states the general mechanism is not built and
   "needs a ticket"; no such ticket was minted.

---

## SILENTLY DROPPED

Called approved / decided by the handoff, yet carrying **no ticket and no PR** today:

| Item | Why it counts as dropped | Unique content at risk |
|---|---|---|
| **SPAWN-VIA-CAPABILITY** | Operator-approved in "do not re-litigate — these are decided". No ticket, no PR, one week after approval. | The **ad-hoc-briefs** constraint (~14 lanes, none with a ticket) exists only in the handoff. A ticket-only implementation would route nothing — the exact failure the operator pre-empted. |
| **ENGINE-CONVERGE** | Same. | The ADR-0007 opt-in constraint and the specific duplication evidence (both ship `grades.py`; product engine duplicates board/claim/reconcile). |
| **PRICING-FEED** | Same, and the handoff explicitly flagged it: "**additions the operator supplied that exist ONLY here — do not lose them**". | **Price-Per-Token MCP**; **MCP-first as the preferred integration shape for every candidate**; **multiple-corroborating-sources over SSOT for observed facts** (disagreement is signal). None of the five named sources has an EVAL-REGISTRY row. |
| **ORCHESTRATION-RE-RUN** | Same. | The instruction to RE-DERIVE (not cite) the one-axis CrewAI reject, and the HARD RULE that verdicts land in EVAL-REGISTRY. |
| **GATE 4 verdicts (LETTA + MEMORY-LAYER)** | Commissioned by the operator, completed, and — the handoff's own words — "**never read or reported**". Still true. | ~10 tool verdicts stranded in `fleet/handoff-notes/`, including the one ADOPT-PARTIAL on the list (basic-memory adopt-time scheduling). This is a live violation of the "a verdict that is not discoverable gets paid for twice" rule. |
| **STOP-WORKER-GRACEFUL-EXIT (#283)** | Worse than dropped — the ticket **was** minted and then physically destroyed by a merge, and the restore sweep missed it. Nothing on the board or in any gate reports its absence. | The defect and its evidence (SIGTERM exit 15 vs `closeOnExit: graceful`; the script header is WRONG; do not pipe stop-worker through `tail -1`) survive only in the handoff's Gotchas and in `git show e5659d0`. |

### Cross-cutting: board-hygiene debt created by the successful lands
Three tickets whose work is **merged** are still sitting in the **active** board with stale
`fleet/state/submitted/` markers, so the board over-reports open work:
`REVIEW-RECONCILE-GATE-DESIGN` (PR #182), `FIXTURE-BYPASS-GATE` (PR #312), and
`RECONCILE-GATE-WIRED` (PR #285, archive move still stranded in the open PR #287).
