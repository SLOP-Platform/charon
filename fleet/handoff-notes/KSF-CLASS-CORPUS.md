# KSF CLASS CORPUS — Historical failure CLASSES ranked by frequency × blast radius

Grounding: GATE-GAP-LEDGER.tsv (8 classes, 25 rows) · 57 SESSION-HANDOFF-*.md · 172 memories ·
65 handoff-notes/ · EVAL-REGISTRY.md · 8 measured classes from this session.

**DEDUPLICATED INTO CLASSES — 30+ incidents → ~15 classes.**

---

## RANKED REGISTER (frequency × blast radius)

| # | Class | Freq | Blast radius | Representative incidents (source:line) | Currently gated by? |
|---|---|---|---|---|---|
| 1 | **fake-green / vacuous gate** — gate reports GREEN on 0 items scanned, or exit code piped into void, or RED path unreachable | ~15 | **VERY HIGH** (undermines trust in every gate; gates exist to catch problems, if green means nothing the whole framework is theater) | GATE-GAP-LEDGER L22 (validate_board exited 0 on defect — `| while` subshell ate exit), GATE-GAP-LEDGER L21 (inert-code flipped green/red between runs — ephemeral worktree copies scanned), agen-kolar:398-399 (9 `[ -f ] \|\| return 0` fail-OPEN blocks in preflight.sh), luminara-unduli (gate scanned 0 files, "8/8 green"), shaak-ti:28 (`cmd \| tail; echo $?` reads tail's exit), plo-koon:36 (`/tui/*` returns true UNCONDITIONALLY), kanan-jarrus:169 (check_inert_code reports green on 6 never-invoked modules) | KSF `fail_loud`, `no_pipe_mask`, `no_vacuous` address slices — but `inert_code` NOT registered in charon; vacuous/empty-population passes NOT comprehensively prevented |
| 2 | **built-but-inert / built-not-wired** — code built, tested, merged green with ZERO callers; entire features ship that do nothing | ~18 | **HIGH** (wasted engineering; detectors with no callers; tools present but never invoked; detection ticketed ×4 never built) | GATE-GAP-LEDGER L20 (tools/check_catalog_case_quant.py built+merged, wired into no gate), memory `charon-meter-inert.md` (BalanceTracker never constructed, ledger EMPTY), agen-kolar:412,421-439 (7 inert detectors — plane-canary 0 callers, stale-check 0 callers), kanan-jarrus:46-48 (STRANDED-WORK-AUDIT ticketed ×4, never built), CLAIM-RECONCILE-TRIAGE.md:8 (reconciler built, merged, marked DONE, never wired), litellm_plane imported ONLY by tests | KSF `inert_code` — BUT **not registered in charon** (~/.ksf/modules.list empty per manifest.toml comment). That makes this class UNGATED in practice, despite the gate existing |
| 3 | **self-report-lie / false-success** — model/agent/tool claims SUCCESS when work was never done; trusted commit-message claims not verified in the diff | ~12 | **VERY HIGH** (decisions compound on false premises; 20-30% of subagent SUCCESS summaries contain unverified claim) | GATE-GAP-LEDGER L19 (FOREMAN-WIRE commit CLAIMED preflight the diff did not contain), memory `document-model-self-report-lies.md`, rey-durron:98-99 (land.sh prints DONE when gh pr merge FAILED on draft PR), kanan-jarrus:144-146 (land.sh reported success on WRONG commit), EVAL-REGISTRY.md:61 (independent adversarial verification as mechanized default — practitioner data), memory `verify-subagent-deliverable-files.md` (subagent claimed file existed — did not) | **NO gate.** Adversarial review is doctrinal (EVAL-REGISTRY row 61) but not mechanized into KSF. | ⚠️ **GAP: 3+ hits, NO gate** |
| 4 | **hand-rolling / adopted-not-fully-used** — ~70% of every layer reinvented commodity; adopted tools used at <12% capability; months of ~zero shipped product | ~20 | **VERY HIGH** (months of nil velocity; hand-rolled guards shipped weaker & buggier than tools they replaced; 12 documented justification anti-patterns) | EVAL-REGISTRY.md:91-93 (months ~zero shipped product while hand-rolled rig grew as large as product), EVAL-REGISTRY.md:96-107 (12 anti-patterns), GATE-GAP-LEDGER L25 (~900 LOC bespoke machinery without substrate question asked), litellm 5/52 params used (agen-kolar:421), KSF 5/9 gates registered, opencode 36/2567 models available, graphify 1 query consumer | `substrate-first-gate` fires at ticket-create time (rig CI) — narrow slice, covers build-vs-adopt DECISION not live-adoption-depth. | ⚠️ **PARTIAL gate** — covers ticket creation, not "adopted but used at 12%" |
| 5 | **config-siloing / multi-source-drift** — deployed config ≠ source in repo; local ~/.charon vs 4-LOM /data/providers.json diverge silently; unreachable source = vacuous pass | ~10 | **HIGH** (silent divergence; stale docs asserting false behaviour; providers added to one silo invisible to others) | GATE-GAP-LEDGER L22 (local ~/.charon vs 4-LOM /data/providers.json diverged silently), memory `config-ssot-git-manifest.md` (NVIDIA NIM added only into 4-LOM docker, invisible to local), agen-kolar:415 (stale docs asserting false behaviour — land.sh gotcha is FALSE), rey-durron:103 (providers add writes local only), memory `always-fix-catalog-mismatches.md` (gpt-5.5 in tier while live workhorse is gpt-5.4) | Fixed via `fleet/checks/config-ssot-gate.sh` (GATE-GAP-LEDGER L22 status: fixed) — **BUT this is a bash script, not a KSF gate**. No KSF gate covers drift. | ⚠️ **GAP: bash gate exists, KSF gate does not** |
| 6 | **finished-work-stranded / accumulated state rot** — finished branches never landed; open PRs silently pile up; tickets done but not retired; work re-derived from scratch | ~8 | **MEDIUM-HIGH** (26 stranded branches, 16 ≥7d; ~64 stranded worktrees; 4 sessions re-discovering same backlog by hand) | Memory `detection-ticketed-never-built.md` (~64 stranded worktrees), kanan-jarrus:46-48 (4 sessions rediscovering backlog), memory `dispose-open-prs-never-pile.md` (PRs accumulate silently), agen-kolar:414 (4 finished branches blocked, 10 worktree branches have no ticket), memory `harmless-cruft-bites.md` ('harmless' leftovers → real bugs) | **NO gate.** branch-reaper.sh exists but only reaps product repo not rig (kanan-jarrus:54). | ⚠️ **GAP: 3+ hits, NO gate** |
| 7 | **effect-not-verified / post-state not checked** — assert command exited 0 rather than that the desired POST-STATE exists; land.sh marks DONE when PR not actually merged | ~5 | **HIGH** (false-DONE risks cascade-unblocking dependents of unmerged work; work claimed finished that wasn't) | GATE-GAP-LEDGER L24 (land.sh marked ticket DONE while PR not actually merged — mergeability=UNKNOWN), rey-durron:98-99 (gh pr merge FAILED on draft PR — land.sh still prints DONE), ki-adi-mundi:228 ("Tool/subagent SUCCESS lines LIE — verify outcomes") | **NO gate** — fixed instance-by-instance (land.sh false-DONE fix) but no class-level gate. | ⚠️ **GAP: 3+ hits, NO gate** |
| 8 | **silent-client-degradation** — failures invisible to monitoring; opencode falls back to a dead pool; free tiers collapse silently; budget-timeout disables check silently → board still GREEN | ~5 | **HIGH** (silent money leaks; double-bill; 200s billed-and-discarded; providers drained but user sees "everything's fine") | Memory `charon-silent-downgrade-leak.md` (50/50 entries billed-and-discarded 200s, cost_usd:0.0 masked everything), agen-kolar:406-410 (parallelizability gate needs ~21.7s vs hardcoded 15s → prints check-failed and does not run, board GREEN), memory `charon-failover-bug-and-tier-fallback.md` (opencode-zen/OpenRouter ran out but Charon did NOT fail over), plo-koon:41 (opencode default model gpt-5.4 = DEAD pool) | **NO gate** for silent degradation / failover deadness. | ⚠️ **GAP: 3+ hits, NO gate** |
| 9 | **test-hygeine / tests-never-executed** — 33+ test files on disk, 63+ reachable only via manual handoff.sh; no CI runner executes them; pytest-green ≠ gate-green | ~5 | **MEDIUM-HIGH** (test coverage is fictional if tests never run; v0.5.0 release exactly this failure mode) | agen-kolar:403 (33 never-run rig test files; 7 guard money/security/data-loss; 63 reachable only via handoff.sh), kit-fisto:24 (pytest-green ≠ gate-green; how v0.5.0 release broke), barriss:33 (land.sh runs charon.cli gate not full pytest), kanan-jarrus:150 (full test suite pulls live-model tests that block for hours) | **NO KSF gate** for test-execution coverage / orphaned-test detection. | ⚠️ **GAP: 3+ hits, NO gate** |
| 10 | **duplicate-systems-across-repos** — ~6,900 LOC of two capability engines doing same thing independently; per-repo forks of shared tooling; charon vendors KSF locally → live drift vector | ~5 | **HIGH** (duplicate maintenance burden; one fix needs two PRs; per-repo KSF vendoring is "a live drift vector" per KSF-AUDIT.md:98) | KSF-AUDIT.md:98 (charon vendors KSF TODAY — drift vector), charon-private/.ksf/ exists but has no vendored gates, /home/stack/code/charon/.ksf/ has different registration, mediastack has no .ksf/ at all, no cross-repo visibility | Partially: KSF is ONE implementation but per-repo config + vendoring defeats single-source. `modules/` provides plugin surface but not cross-repo drift detection. | ⚠️ **GAP: no cross-repo consistency gate** |
| 11 | **concurrency / isolation failures** — two agents on one checkout; bare git commit sweeps concurrent WIP; worktree gate runs main-repo code silently validating wrong checklist | ~6 | **MEDIUM** (lost work; committed onto wrong branch; merged untested code) | Memory `one-checkout-one-agent.md` (commit landed on other agent's branch), memory `commit-dirty-sweeps-subagent-wip.md` (land.sh --commit-dirty swept concurrent WIP onto master), agen-kolar:467-468 (git add silently fails on bad pathspec → commit captures only what was already staged, commit 5d24ce6 claimed content it didn't contain), memory `worktree-gate-runs-main-checklist.md` (editable install in main repo poisons worktree imports) | Partially: WORK-LEASE-GATE boarded, Faktory ADOPT-PARTIAL. No KSF gate. | ⚠️ **PARTIAL** |
| 12 | **wrong-verdict-from-wrong-ref** — two-dot diff LIES (shows master's later adds as branch deletions); measurements against stale checkout; relayed numbers compound without re-verification | ~4 | **HIGH** (nearly discarded 1,662 lines; 2 wrong destructive verdicts from two-dot diff; measurement errors in agen-kolar relayed between sub-sessions — 3 compounding errors) | plo-koon:34 (two-dot diff caused 2 wrong destructive verdicts), agen-kolar:303-308 (git cherry and git log origin/master..<br> report "unique commits" for fully-landed branches), agen-kolar:184-188 (manager relayed measurements between sub-sessions without re-verifying → 3 compounding errors), GATE-GAP-LEDGER L24 (branch-reaper nearly discarded 1,662 lines) | **NO gate** for diff-scope or measurement-relay correctness. `wiring_alignment` gate checks internal wiring, not diff-interpretation. | ⚠️ **GAP: 3+ hits, NO gate** |
| 13 | **deploy-context-blind** — gate validates CODE not the deploy target; code green but deploy collapses unpriced models to fallback; gates declare no context-of-validity (code vs deploy vs data) | ~4 | **MEDIUM-HIGH** (code green → deploy broken → silent money leak; DELETE-STATIC-RANK collapsed unpriced models to 1000 fallback) | GATE-GAP-LEDGER L17 (DELETE-STATIC-RANK landed green but DEPLOY collapsed unpriced models → 1000 fallback → silent route-to-pricier money leak), GATE-GAP-LEDGER L22 (unreachable source silently treated as in-sync → vacuous pass) | ADR0016-DEPLOY-PRICED-COMPLETENESS boarded but **not built** (GATE-GAP-LEDGER L17 status: "open"). No KSF gate. | ⚠️ **GAP: 3+ hits, ADR only** |
| 14 | **no-decision-time-gate** — check fires at session start but NOT at decision time; substrate/security/anti-hand-roll rules stated then skipped at the exact moment a build-vs-build ticket is answered | ~5 | **MEDIUM** (now largely addressed but caused ~900 LOC of unnecessary machinery) | GATE-GAP-LEDGER L25 (substrate question never asked at decision time → ~900 LOC bespoke machinery), memory `substrate-check-fires-at-decision-time.md` (adopt-substrate doctrine stated at session start → skipped exactly when needed), EVAL-REGISTRY.md:96-107 (12 anti-patterns still live at session level) | **FIXED** — fleet/checks/substrate-first-gate.sh + substrate_first_gate.py wired HARD into rig-ci-scope.sh (GATE-GAP-LEDGER L25). But this is a **rig bash gate**, not a KSF gate. | ⚠️ **Fixed in rig, absent in KSF** |
| 15 | **nondeterministic-gate / unbounded-fan-out** — gate verdict varies run-to-run on unchanged tree; 78 suites launched on 16 cores → fork: Resource temporarily unavailable; perf assertions measure scheduler queueing | ~4 | **HIGH** (gate itself becomes unreliable; green run proves nothing; same class as GATE-GAP-LEDGER row 5) | GATE-GAP-LEDGER L21 (inert_code flipped red/green — ephemeral worktree copies swept into AST scan), agen-kolar:180-181 (gate.sh:44-50 launches 78 suites at once on 16 cores → non-deterministic 9 vs 10 reds), rig-reds-disposition (68/9 vs 67/10 on unchanged tree), memory `fleet-selfcheck-forkbomb-class.md` (handoff.sh→gate.sh→tests→handoff.sh→gate.sh concurrent fan-out) | Partially: KSF has `no_vacuous` but not deterministic-reproducibility requirement. | ⚠️ **PARTIAL** |

---

## GAPS SUMMARY — classes hit 3+ times with NO KSF gate

| Class | Incidents | Blast | Existing coverage |
|---|---|---|---|
| self-report-lie/false-success | 12+ | VERY HIGH | None — doctrinal adversarial review not mechanized |
| config-siloing/drift | 10+ | HIGH | fleet bash gate only, no KSF gate |
| finished-work-stranded | 8+ | MEDIUM-HIGH | None |
| effect-not-verified | 5+ | HIGH | None |
| test-hygeine/orphaned-tests | 5+ | MEDIUM-HIGH | None |
| silent-client-degradation | 5+ | HIGH | None |
| wrong-verdict-from-wrong-ref | 4+ | HIGH | None |
| deploy-context-blind | 4+ | MEDIUM-HIGH | ADR only (not built) |
| duplicate-systems-cross-repo | 5+ | HIGH | KSF modules/ surface exists but unused cross-repo |
| built-but-inert (de facto) | 18+ | HIGH | `inert_code` gate EXISTS but NOT REGISTERED in charon |

---

## TOP-3 FINDINGS (for the DTC)

1. **`inert_code` is UNREGISTERED in charon** — the gate that would catch the #2-ranked failure class (18+ incidents) exists but is NOT wired. The manager's hypothesis that it's unregistered because too slow (120s timeout KILL on 4-file fixture) is **PLAUSIBLE but UNVERIFIED** — needs a perf investigation, not a debate. If this hypothesis holds, the flagship class-detector is disabled by performance, not doctrine.

2. **self-report-lie is the #1 un-gated VERY-HIGH-blast class** — 12+ incidents, 20-30% of subagent SUCCESS summaries contain unverified claims per practitioner data, and NO mechanized gate exists anywhere. Adversarial review is doctrinal but not enforced. This is the single highest-value gap in the programme.

3. **7 of the top 10 classes have no KSF gate at all** (self-report-lie, config-drift, stranded-work, effect-not-verified, orphaned-tests, silent-degradation, wrong-verdict-from-wrong-ref). KSF's 9 gates cover important but narrow slices; the corpus reveals entire failure dimensions KSF does not address. KSF is the right idea but **not yet load-bearing** for this programme's actual failure profile — 70% of real incidents map to classes it cannot detect.