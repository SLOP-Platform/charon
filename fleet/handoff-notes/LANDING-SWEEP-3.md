# LANDING-SWEEP-3 — 12-branch triage report

Session: jocasta-nu | deepseek-v4-flash
Date: 2026-07-27

| # | Branch | Disposition | Rationale | Evidence |
|---|--------|-------------|-----------|----------|
| 1 | SG-ISSUE-CONTROL-PLANE `feat/sg-issue-control-plane` | LAND-READY | 4 coherent design commits seeding the SG control-plane design of record (DESIGN-SG-ISSUE-CONTROL-PLANE.md, 403 lines) + applying N1/N2/N4 review feedback + registry of 12 issue classes. Ticket is a design umbrella for 3 build slices. Board file already on master. | Ran: `git log --oneline master..feat/sg-issue-control-plane` = 4 commits. `git diff --stat master..feat/sg-issue-control-plane` includes `fleet/state/DESIGN-SG-ISSUE-CONTROL-PLANE.md +403`. Board file `fleet/board/SG-ISSUE-CONTROL-PLANE.md` already on master. |
| 2 | SW-IDENTITY-FOLD `fix/sw-identity-fold` | LAND-READY | Core identity-folding fix (PR #198, adding fp4 to `_QUANT_SUFFIX`) already landed on master (verified: proxy.py:274-275 contains `fp4`). Branch adds 2 follow-up commits: (a) aistudio gemini preview id aliasing and (b) quantization variant folding. Diff focused: +10/-328 across gateway.py, routing_policy/__init__.py, secrets.py, tests. Board says assigned to non-Anthropic model as graded sample. | Ran: `git -C /home/stack/charon-wt/SW-IDENTITY-FOLD log --oneline master..HEAD` = 2 commits. `git diff --stat master..HEAD` = 7 files changed, 10 insertions, 328 deletions. Verified `_QUANT_SUFFIX` on master includes fp4 (proxy.py:275). Ticket board file already on master. |
| 3 | REVIEW-DISPENSATION-CANARY `feat/review-dispensation-canary` | LAND-READY | 2 commits: review-dispensation-canary.test.sh (+286 lines, #200 no-op-review dogfood) + reconcile-review-gate.sh (+368 lines, folded review-gate axis). The reconcile-review-gate work that this ticket depends on is INCLUDED in the same branch. Dogfood adds the builder==reviewer self-review class to the reconcile-review-gate check. | Ran: `git log --oneline master..feat/review-dispensation-canary` = 2 commits. `git diff --stat master..feat/review-dispensation-canary -- fleet/tests/review-dispensation-canary.test.sh fleet/checks/reconcile-review-gate.sh` confirms both files added. Board file on master, ticket depends on PLANE-CANARY-REGISTRY and RECONCILE-REVIEW-GATE (latter is co-located in this branch). |
| 4 | SW-STATIC-LEGS-RETIRE `feat/sw-static-legs-retire` | ABANDON | Board describes massive scope: retire ~175 upstream_model pins, 88 enabled:false flags, rewire routing_policy/__init__.py, pools.py, catalog_refresh.py. Actual diff is only secrets.py refactoring (+7/-47) touching 2 files that are NOT part of the ticket's owns: set. The core routing changes are absent — the branch contains only a minor unrelated cleanup. | Ran: `git -C /home/stack/charon-wt/SW-STATIC-LEGS-RETIRE diff --stat master..HEAD` = `src/charon/secrets.py: +7/-26, tests/test_secrets.py: -28`. None of the owned files (catalog_refresh.py, routing_policy/__init__.py, pools.py, test_static_legs_retired.py) are touched. Board describes 4384-pool live state with 859 static entries — none of that work appears. |
| 5 | BOARD-WRITE-LOCK `fix/board-write-lock` | LAND-READY | Complete implementation: board-lock.sh (+354 lines, 5 subcommands: acquire/release/status/steal/commit), board-write-lock.test.sh (+185 lines, concurrent-writer + bare-commit-guard + master-moved + fail-closed), pre-commit hook integration (+5 lines), retire-done.sh fix (+15 lines). All mechanisms described in the board are implemented. | Ran: `git log --oneline master..fix/board-write-lock` = 1 commit. `git diff --stat master..fix/board-write-lock -- fleet/board-lock.sh fleet/tests/board-write-lock.test.sh fleet/hooks/pre-commit fleet/retire-done.sh` confirms all 4 owned files changed. Board file on master. |
| 6 | D24-SESSION-CTL-SPIKE `spike/session-ctl` | LAND-READY | Spike deliverable complete: session-ctl.sh (+82 lines) with 5 curl-only verbs (list/steer/stop/reply/watch) + handoff note (SPIKE-SESSION-CTL-2026-07-26.md). Board asks for "prove or disprove" — the spike delivered a working wrapper. 4 live sessions untouched (verified by worktree list). | Ran: `git log --oneline master..spike/session-ctl` = 1 commit. `git diff --stat master..spike/session-ctl` = adds fleet/session-ctl.sh (+82) and fleet/handoff-notes/SPIKE-SESSION-CTL-2026-07-26.md (+24). Board file on master. |
| 7 | FN-MEMORY-RETIRE-ADOPT `feat/fn-memory-retire-adopt` | NEEDS-REVIEW | Branch adds 2 curation wrappers (curation.sh +142, migrate-frontmatter.sh +118) under fleet/memory/. However: (a) the RETIRE (delete) of old fleet/memory/ files is NOT on this branch — board says "DONE on feat/fn-memory-retire" but that branch is absent; (b) no test/fail-on-revert file is present; (c) dangling refs in fleet/research.sh (owned per board) are not purged. The ADOPT wrappers are written, but the retire completeness and test coverage cannot be confirmed. | Ran: `git log --oneline master..feat/fn-memory-retire-adopt` = 1 commit. `git diff --name-status master..HEAD -- fleet/memory/` = only A (added) files, no D (deleted). No test file under fleet/tests/ for this ticket. `fleet/memory/__init__.py` still exists on master (git show master:fleet/memory/__init__.py succeeds). Board file on master. |
| 8 | LAND-GATE-RIG-SUITE `fix/land-gate-rig-suite` | LAND-READY | Complete fix: land.sh (+24 lines, rig branch adds fleet/gate.sh to existing validate_board.sh gate), land-rig-suite-gate.test.sh (+149 lines, hermetic: 4 cases including flip-on+RED->block, flip-on+GREEN->proceed, flip-unset+RED->no-block, both-gates-ran). Ships disabled (LAND_RIG_TESTS=0). land.sh hunk is additive and confined to the rig `elif`. | Ran: `git log --oneline master..fix/land-gate-rig-suite` = 1 commit. `git diff --stat master..fix/land-gate-rig-suite -- fleet/land.sh fleet/tests/land-rig-suite-gate.test.sh` confirms both files changed. `LAND_RIG_TESTS` not on master. Board file on master. |
| 9 | PREFLIGHT-OWNS-ARBITRATE `fix/preflight-owns-arbitrate` | LAND-READY | Ruling document produced: PREFLIGHT-OWNERSHIP-RULING.md (+106 lines) with per-ticket disposition for all 5 colliding tickets (MARKER-PROOF-MECHANIZE, PREFLIGHT-GATE-RUN-HELPER, RECONCILE-WIRING, REPO-MAP-CONVERGE, SYNC-SCHEDULE). Does NOT edit fleet/preflight.sh (verified). Does NOT edit the 5 tickets. | Ran: `git log --oneline master..fix/preflight-owns-arbitrate` = 1 commit. `git diff --stat master..fix/preflight-owns-arbitrate` = core addition: `fleet/state/PREFLIGHT-OWNERSHIP-RULING.md +106`. No changes to `fleet/preflight.sh` (verified with diff filter). Board file on master. |
| 10 | RECONCILE-REVIEW-GATE `feat/reconcile-review-gate` | LAND-READY | Complete check implementation: reconcile-review-gate.sh (+368 lines, R-J/R-K/R-L BLOCK conditions, fail-closed taxonomy for unknown paths/work_classes, reuses ReviewerCircuitBreaker), reconcile-review-gate.test.sh (+237 lines, 3 fixture cases including revert proof). Also updates gate-creation-standard.sh and retires obsolete gate-parity/graphify-freshness checks. | Ran: `git log --oneline master..feat/reconcile-review-gate` = 1 commit. `git diff --stat master..feat/reconcile-review-gate -- fleet/checks/reconcile-review-gate.sh fleet/tests/reconcile-review-gate.test.sh` = +368/+237. Board file on master. |
| 11 | RFL-3-CAPTURE-FIX `fix/rfl3-dogfood-capture` | LAND-READY | Small focused fix: dogfood-eval.sh (+23/-1) adds untracked file capture to git-diff evaluation, so `git add -N` ensures new untracked files appear in capture and scoring. dogfood-eval-capture.test.sh (+102 lines, hermetic: fixture candidate writing single untracked file → score >= 1, revert → 0). | Ran: `git log --oneline master..fix/rfl3-dogfood-capture` = 1 commit. `git diff --stat master..fix/rfl3-dogfood-capture -- fleet/benchmark/dogfood-eval.sh fleet/tests/dogfood-eval-capture.test.sh` = +23/+102. Board file on master. |
| 12 | SUBAGENT-WORKTREE-SANDBOX `feat/subagent-worktree-sandbox` | LAND-READY | Design doc produced: SUBAGENT-WORKTREE-SANDBOX-DESIGN.md (+231 lines). Documents the 2026-07-22 escape (vulture-investigate sub wrote absolute-path heredoc into pyproject.toml), evaluates bwrap/landlock/firejail/PreToolUse-path-guard, spawns build follow-up ticket. P2 design/eval only, no code. | Ran: `git log --oneline master..feat/subagent-worktree-sandbox` = 1 commit. `git diff --stat master..feat/subagent-worktree-sandbox -- fleet/state/SUBAGENT-WORKTREE-SANDBOX-DESIGN.md` = +231. Board file on master. |

=== SESSION REPORT v1 ===
TICKET:       LANDING-SWEEP-3
SESSION:      jocasta-nu | deepseek-v4-flash
STATUS:       DONE
COMMIT:       none
FILES:        1 changed: fleet/handoff-notes/LANDING-SWEEP-3.md
OWNS-OK:      yes
GATE:         n/a — read-only triage
TESTS:        n/a — read-only triage
RED-PROOF:    n/a — no code change
OBSERVABLE:   MET — 12 branches triaged, report written
RAN:          git log, git diff, git show for all 12 branches; verified board file existence on master; verified SW-IDENTITY-FOLD proxy.py content on master; worktree state inspected
READ:         board definition files for all 12 tickets (fleet/board/*.md)
BRIEF-ERRORS: none
BLOCKED-BY:   none
NEXT:         LAND-READY=10 NEEDS-REVIEW=1 ABANDON=1 UNSAFE=0
=== END REPORT ===
