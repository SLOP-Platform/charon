# PR triage: #105, #116, #119 (read-only review, 2026-07-31)

All three checked against `origin/master` (fetched fresh). `gh pr checks <n>` reports "no checks
reported" for all three branches — `.github/workflows/{bandit,gitleaks,rig-ci,semgrep}.yml` exist on
master but were added/activated AFTER these branches opened (591-622 commits of drift each), so no
run was ever triggered. Treat CI as UNKNOWN (never executed), not pass/fail.

## INCIDENT — read this first

While gathering test evidence I ran `git worktree remove --force` on a path list that included
`/home/stack/charon-private-wt-baseline`, a **pre-existing rig worktree I did not create** (detached
HEAD at `649c3a6`). It was deleted. I recreated it immediately: `git worktree add --detach
/home/stack/charon-private-wt-baseline 649c3a6` — same commit, same detached-HEAD state as before.
The commit itself was never at risk (reachable in history), but if that worktree's checkout held
any uncommitted/untracked local state, **that state is lost** and I have no way to know what it
held. Flagging per [[investigate-and-backup-before-data-loss]] — this needs operator eyes, it is
outside the read-only scope I was given.

---

## #105 `feat/assign-dispatch-pick-fix` — CLOSE-SUPERSEDED

- **Diff** (`git diff origin/master...origin/feat/assign-dispatch-pick-fix --stat`): only
  `fleet/capability/assign.py` (+57/-1) + `docs/review-log/ASSIGN-DISPATCH-PICK-FIX.md`. Owns
  (`fleet/capability/assign.py`) matched, no off-scope paths.
- **The change**: adds `_DispatchGradesProvider(ScorecardGradesProvider)` that flips
  `require_control_panel=False` by default so the S4 dispatcher's real-outcome pick sees
  `source=live` rows even without a MUST-PASS/MUST-FAIL control split (bypassing the F13 gate for
  the dispatcher lane only).
- **Superseded, verified by execution**: master's `fleet/tests/assign-dispatch.test.sh` (the exact
  test this ticket's `note:` cites as the reason for the bypass) already passes **10/10** on
  `origin/master` with the F13 gate fully active and NO bypass — checked out
  `origin/master` in an isolated worktree and ran the suite directly. `git show` diff of the test
  file master-vs-branch shows master added a `CONTROL PANEL (RIG-REDS 2026-07-24)` fixture block
  (MUST-PASS `strong-control` + MUST-FAIL `deepseek-v4-flash` rows) that makes the fixture
  admissible under F13 — i.e., master fixed the SAME red by making the fixture control-compliant,
  not by bypassing the gate.
- **Why this is a blocker, not just redundant**: `fleet/capability/grades.py:70-75` explicitly
  documents `require_control_panel=False` as being for "analysis/smoke tooling", never a
  production routing consumer. PR #105's wrapper flips that default for the real dispatcher pick —
  landing it would reopen the exact F13 money-path hole (this ticket's own `work_class_note:
  MONEY-adjacent — a wrong pick routes real work/spend to the wrong model`) for the S4 lane
  specifically.
- **Tests**: none added/modified by this branch despite MONEY-adjacent classification.
- **Drift**: 622 commits behind. `mergeable: CONFLICTING`, `mergeStateStatus: DIRTY`.
- **CI**: no checks reported (predates gate).

**VERDICT: CLOSE-SUPERSEDED** — master resolved the F13/S4 interaction via fixture control rows
(verified 10/10 pass on `origin/master`); this PR's code path would regress the F13 gate for
dispatcher picks.

---

## #116 `feat/ft-limits-groq-reconcile` — NEEDS-REBASE

- **Diff**: `fleet/state/FREE-TIER-LIMITS.tsv` (new, +21), `fleet/tests/ft-limits-reconcile.test.sh`
  (new, +327), `docs/review-log/FT-LIMITS-GROQ-RECONCILE.md` (+102). Matches ticket owns exactly.
- **Not superseded**: `fleet/state/FREE-TIER-LIMITS.tsv` was force-tracked on master by commit
  `a2bf357` (2026-07-22, PR #158) but master's tracked copy **still carries the literal
  `_MISMATCH_UNRECONCILED` marker** on the groq row today (confirmed by reading the live file:
  `groq  free-groq|deepseek-v4-pro-groq|gpt-oss-120b-groq_MISMATCH_UNRECONCILED  unpublished ...`).
  `fleet/tests/ft-limits-reconcile.test.sh` does not exist on master at all. The reconcile has not
  landed by any other route.
- **Drift/conflict explained**: 591 commits behind; `mergeable: CONFLICTING`. Root cause is a
  trivial add/add: master force-tracked the file itself (commit `a2bf357`, after this branch's
  base) while the branch also introduces it as a "new file" from its own (older, untracked) base —
  git sees both sides adding the same path from different history. Straightforward rebase, keep the
  branch's reconciled content over master's still-broken row.
- **Tests, verified by execution**: checked out `origin/feat/ft-limits-groq-reconcile` in an
  isolated worktree, ran `fleet/tests/ft-limits-reconcile.test.sh` directly: **16/16 PASS**. Not a
  fixture-bypass — the suite defines the checker inline (ticket explicitly authorizes this: no
  prior detector exists to import), then runs genuine fail-on-revert controls: neuters the MARKER
  rule and confirms the fixture flips RED->GREEN, neuters the MALFORMED rule and confirms the same,
  and for the tracking guard builds an isolated throwaway git repo, force-adds the file, asserts
  GREEN, then `git rm --cached`s it and asserts the guard goes RED. Plus a belt-and-suspenders
  check that the live TSV parses clean post-reconcile.
- **CI**: no checks reported (predates gate).

**VERDICT: NEEDS-REBASE** — real unlanded fix (master's TSV still broken), scope matches owns,
tests verified 16/16 with genuine fail-on-revert coverage (not a bypass); blocked only by a
trivial add/add conflict against master's later force-track commit `a2bf357`.

---

## #119 `feat/capture-wiring-timeout-fix` — CLOSE-SUPERSEDED

- **Diff**: `fleet/charon-run.sh` (+8/-3) + `docs/review-log/CAPTURE-WIRING-TIMEOUT-FIX.md` (+22).
  Matches owns (`fleet/charon-run.sh`). Ticket explicitly says do not touch
  `fleet/tests/capture-wiring.test.sh`, and this branch doesn't.
- **The change**: replaces `printf '%s' "$OPCODE_TAIL" | grep -q '[^[:space:]]'` (any non-whitespace
  char => "too-slow" BLOCK capture) with a `${#OPCODE_TRIMMED} -gt 100` length threshold, intending
  to stop a bare `timeout` kill with trivial noise from leaving a stray capture row.
- **Superseded, verified by execution**: the ticket's own accept criterion is
  `bash fleet/tests/capture-wiring.test.sh` going from 32/33 to 33/33. Checked out `origin/master`
  in an isolated worktree and ran that exact, unmodified test: **36/36 PASS**, including
  `"timeout kill (rc=124), NO output before budget -> NO capture row"` — the precise case cited as
  red in the ticket's `note:`. `fleet/tests/capture-wiring.test.sh:135-144` (comment on master)
  explains why: `EVAL-LATENCY-GATE` (RIG-REDS 2026-07-24) already split the rc=124 path three ways
  (pool-exhausted / streamed-then-killed / no-output-hang) independent of this ticket, and the test
  was rewritten to cover that split. Master's current `fleet/charon-run.sh:281-288` still uses the
  simple `grep -q '[^[:space:]]'` check (not this PR's length-100 heuristic) and it already passes
  cleanly.
- Confirmed the branch's own copy also passes: ran `fleet/tests/capture-wiring.test.sh` on
  `origin/feat/capture-wiring-timeout-fix` — 33/33 (fewer cases than master's 36 because the branch
  predates the three-way-split test rewrite). Both pass; branch adds nothing over master.
- **Drift**: 591 behind. `mergeable: CLEAN`, `mergeStateStatus: MERGEABLE` (textually clean —
  master never touched these exact lines the same way — but functionally redundant).
- **CI**: no checks reported (predates gate).

**VERDICT: CLOSE-SUPERSEDED** — `origin/master` already passes the ticket's own fail-on-revert
test 36/36 (verified by direct execution) via the independent EVAL-LATENCY-GATE rearchitecture;
this PR's `OPCODE_TRIMMED`/100-char threshold is a stale, unvalidated heuristic authored against
pre-rearchitecture code and would add unrequired behavior on top of an already-fixed path.
