OBSERVABLE:   DEFERRED — litellm.Router's full 52-param surface not re-enumerated (reused prior audit count); 30+ ~/.local/bin scripts not enumerated (0 references in any fleet doc); product tools/gates.json vs umbrella runner divergence not independently verified; Faktory Enterprise features not inspected (we run OSS)
RAN:          gate-integrity.sh scan (executed) proved 9 fleet/checks/ are INERT with zero callers, 8 test suites exist but never ran in CI; shellcheck --list-optional enumerated 11 optional checks all OFF; graphify --help confirmed `affected` blast-radius subcommand exists with zero invocations; ruff --help + mypy --help + opencode --help + basic-memory --help + pip-audit --help enumerated full command surfaces
READ:         EVAL-REGISTRY 3 under-scoped trials (bandit→ruff S, pyscn→ruff C90/PL, pylint W0613→ruff ARG) confirmed by cross-referencing with executed ruff rule counts; Faktory README confirmed OSS feature set does NOT include unique-job dedup (claim-lease-exactly-once is Enterprise-only); session-bridge proxy.py confirmed missing model field via availability.py's own source citation
BRIEF-ERRORS: none — the brief accurately describes the pattern (every adopted tool is 10-55% wired), the precedent (Faktory exactly-once as CLAIMED-BUT-ABSENT), and the pairing with WORKFLOW-E2E-AUDIT (one maps failures, one maps existing capabilities that could close them)
NEXT:         wire reconcile-board-pr-done.sh (closes failure #1: 19 merged-but-submitted tickets) — one line in preflight/land; it is the single highest-value one-line fix in the fleet
