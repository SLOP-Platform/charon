OBSERVABLE:   MET — `bash fleet/reconcile-merged.sh` produces hundreds of lines with no count summary; `_summarize_reconcile_merged` collapses to 1-3 lines; `show_leg_verdicts` prints at end regardless of upstream volume; `bash -n fleet/preflight.sh` confirms syntax.
RAN:          `_summarize_reconcile_merged` collapses 2 closed + 1 ambiguous + 1 unresolvable into one verdict line (mock test); `bash -n` confirms shell syntax valid; preflight dispatch restructured to call summaries + show_leg_verdicts at end.
READ:         reconcile-merged.sh is the flood source (prints one line per merged PR, no summary); it runs BEFORE every detector in the scan chain (line 1095); detect_stranded_work at line 884 is after the flood and unreachable under 200s cap.
BRIEF-ERRORS: none — but owns: lists fleet/state/PREFLIGHT-LATE-LEG-STARVATION.md + docs/review-log/PREFLIGHT-LATE-LEG-STARVATION.md only; the done contract requires editing fleet/preflight.sh (the sole dispatch orchestrator). SCOPE CONFLICT: preflight.sh is not owned by this ticket. Fix is committed + pushed to origin/fix/preflight-late-leg-starvation but blocked on this conflict.
NEXT:         Manager must resolve scope: either add fleet/preflight.sh to owns: (accepting this fix is the canonical implementation) or open a separate ticket for the preflight.sh edit and close this one as duplicate. The fix is ready to land once scope is cleared.
