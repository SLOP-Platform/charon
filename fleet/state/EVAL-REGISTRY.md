# EVAL-REGISTRY — consult-first tool/library evaluation registry

**Consult this BEFORE any new tool research.** Before researching, adopting, rejecting, or
re-litigating any external tool/library (or re-deciding a prior internal build-vs-adopt call),
grep this file for the tool name first. A new evaluation MUST add a row here with its
`alignment` honestly classified (see column definitions below) — citing an existing ALIGNED
row as settled is fine; citing a DRIFTED or MIXED row as settled is not (re-test it against
the framing it skipped, per its `reason`). This file is append-only; git history is the audit
trail. Seeded 2026-07-13 from `fleet/state/EVAL-REGISTRY-RECONCILE.md` (do not delete that
file — it is the full reasoning trace behind the LiteLLM rows below).

## Schema

`tool | scope | date | verdict | alignment (aligned/drifted/mixed/n-a) | reason | evidence-link | supersedes`

- **aligned** — the eval actually tested the real framing for its scope (e.g., for a
  plugin-wrap question, it tested plugin-wrap — not just full-embed-vs-hand-roll), and its
  verdict can be treated as settled.
- **drifted** — the eval rejected/accepted using a strawman (e.g., "heavy dependency conflicts
  with stdlib-only core" without checking whether Charon's existing wrap-as-optional-plugin
  pattern — Presidio, OpenTelemetry — resolves that exact objection). Re-test before trusting.
- **mixed** — some reasoning in the eval is aligned, some is drifted/unresolved; read the
  `reason` column before treating as closed.
- **n-a** — tangential/informational mention, not itself a verdict on adopting the tool.

## Rows

| tool | scope | date | verdict | alignment | reason | evidence-link | supersedes |
|---|---|---|---|---|---|---|---|
| LiteLLM | full-gateway/proxy-core replacement (`proxy.py`/`router.py`/`policy_router.py`/`failover.py`/`virtual_keys.py`/`ledger.py`/cost meter) | 2026-07-11/12 | REJECTED — KEEP-CUSTOM (conditional on differentiation shipping) | aligned | This scope IS "duplicate Charon's own product core" — the gateway core is the differentiator, not something you outsource. Correctly scoped as full-replacement, not a wrap-as-plugin candidate. | `fleet/state/GAP-AUDIT-toolfirst.md` #9, `fleet/state/GAP-REGISTER.md` row C7 | — |
| LiteLLM | Router cooldown/`allowed_fails`/health logic isolated as an optional PLUGIN behind the stdlib core (same pattern as Presidio/OTel) | 2026-07-13 | **UNRESOLVED** — not rejected, not adopted | drifted (prior evals never tested this framing) | Prior rejections (`research-litellm-bifrost.md`, `POOLS-REDESIGN-ADR-v2.md`) argued "full embed = wrong stack (FastAPI/Postgres/Redis)" then jumped straight to "hand-roll natively," skipping the plugin-wrap middle option Charon already uses elsewhere. Nobody has checked whether `litellm.Router` is cleanly separable from the rest of the import-monolithic `litellm` package. A cheap native answer (tenacity + reuse of `ReviewerCircuitBreaker`, `failover.py:73-142`) already exists, which may make resolving this moot on cost/benefit — but record as UNRESOLVED, not silently "already rejected," so it isn't re-researched from scratch nor wrongly cited as closed. | `fleet/state/EVAL-REGISTRY-RECONCILE.md` scope B; `fleet/state/HAND-ROLLED-AUDIT.md` #5 | — |
| LiteLLM | pricing DATA source only (static `model_prices_and_context_window.json`) | 2026-07-12 | ADOPT — shipped | aligned | Git-vendored MIT price JSON as a static, off-hot-path source; zero runtime dependency in the privileged gateway process. This is the plugin-wrap pattern done correctly — narrow, isolated, optional. | `code/charon/docs/adr/0016-demand-driven-capability-match.md`, PR #97, commit `3314989` | — |
| LiteLLM | tool-call schema-translation library (embed `litellm.completion()` sans proxy/DB) | 2026-07-13 | Real candidate, not a clean win — not adopted | mixed | Genuinely embeddable as a library alone (MIT, no fastapi/prisma/uvicorn) — closer to a true plugin-wrap test than the cooldown row above. Verified bug backlog (4 GitHub issues) is a solid, aligned reason. But also leans on "~200MB deps land in the privileged core" — and this scope sits ON the hot path (unlike pricing data), so whether isolation/lazy-import actually resolves that objection is not fully settled. | `fleet/state/TOOLCALL-VS-DIFF-RESEARCH.md` | — |
| LiteLLM | full feature-parity vs OpenRouter/Together/Requesty/Kong (A2A/MCP gateway, enterprise SSO/SAML/RBAC) | 2026-07-02 | Partial reject (feature-level) | aligned | Enterprise SSO/SAML/RBAC and A2A/MCP gateway are genuinely out of scope for Charon's local-first, solo-op posture regardless of embed-vs-plugin — a real scope mismatch, not a dependency-weight strawman. | `fleet/archive/ADOPT-GATEWAY-FEATURES.md` | — |
| LiteLLM | SLOP's privileged agent-orchestration-harness dependency (ms-router / execution-port plane — different product/scope from Charon's gateway) | 2026-06-26 | REJECTED — stated reason "supply-chain compromise" | **drifted / unsubstantiated — flagged for correction** | `docs/SUPPLY-CHAIN.md` has zero mentions of LiteLLM; no CVE/compromise finding cited anywhere in either repo. Every other LiteLLM rejection is architecture/stack-weight or live-bug-backlog reasoning, never security — this one is the outlier and is plausibly mis-attributed (sits next to unrelated "NanoBOT/trustclaw/one-api rejected as thin/untrusted" entries). The REJECT verdict itself may still be correct (this scope is a privileged orchestration loop, not a wrap-as-plugin candidate — same category as the full-gateway-core row above), but the stated *reason* has no evidence and must not be cited as-is. **Correction proposed, not yet applied — see item 5 of `fleet/state/S2-GROUNDING-MECHANIZED.md`.** | `code/charon/docs/adr/0003-capability-routed-agent-orchestration-harness.md`; `fleet/state/EVAL-REGISTRY-RECONCILE.md` #12 | — |
| LiteLLM | whole-product strategic question: does ~80% feature overlap make Charon's differentiation thesis moot if free-tier/funding-class/tier-substitution features don't ship | 2026-07-11/12 | OPEN — correctly scoped, not decided | aligned | A product-thesis decision for the operator, not a tool-adoption research task. Do not re-research; escalate for a decision instead. | `fleet/state/GAP-AUDIT-toolfirst.md` #9, `fleet/state/GAP-REGISTER.md` row C7 | — |
| CommandCode Provider plan | provider wiring: add as a live gateway leg | 2026-07-13 | **OVERRIDDEN-BY-OPERATOR** — wire it now | n-a (operator decision, not a re-tested eval) | Prior standing state was PLAN-GATED/NOT-WIRED (`fleet/FREE-TIER-ROUTING.md` §2.9, memory `charon-commandcode-plan-gate`): Go-tier key 403s `/chat/completions`; needed the $15/mo Provider plan, which the operator had not confirmed buying. The underlying facts have NOT changed — only the operator's directive has (mark it operator-active, produce a wiring recipe). Do not re-litigate the Go-tier-403 fact as if it were newly resolved; it wasn't tested again, just accepted. | `fleet/state/PROVIDER-WIRING-RECIPES.md` §2, `fleet/state/CG-PROVIDERS.md` | prior PLAN-GATED verdict (`FREE-TIER-ROUTING.md` §2.9) |
| Synthetic.new | provider wiring: add as a live gateway leg | 2026-07-13 | **OVERRIDDEN-BY-OPERATOR** — wire it now | n-a (operator decision, not a re-tested eval) | Prior standing state was ⛔DROPPED 2026-07-10 (`fleet/FREE-TIER-ROUTING.md` §2.6, `CG-PROVIDERS.md`, `CG-MODEL-CANDIDATES.md`): "1-concurrent-per-model kills gateway fan-out ... do not re-add." That constraint is UNCHANGED (still 1-concurrent-per-model, 500 req/5h, ~$24/wk credit ceiling) — the operator is choosing to accept the fan-out limitation, not disputing the fact. Most plausible motive: the 512K-context GLM-5.2 leg, where fan-out matters less than context headroom. Do not silently re-drop this on the next audit pass without a fresh operator conversation. | `fleet/state/PROVIDER-WIRING-RECIPES.md` §3, `fleet/state/CG-PROVIDERS.md` | prior DROPPED verdict (`FREE-TIER-ROUTING.md` §2.6) |
| Trae.ai | provider wiring: add as a live gateway leg | 2026-07-13 | **UNRESOLVED CONFLICT — flagged, not silently overridden** | mixed | Operator marked this operator-active/wiring-pending, but the prior SKIP verdict (`fleet/reviews/PROVIDER-REVIEW-2026-07-10.md`: "ByteDance AI IDE, not a model API... does not sell an OpenAI-compatible model API") was **re-confirmed independently this session** via a live web search — no hosted endpoint found, only an open GitHub feature request (`Trae-AI/TRAE#412`) that hasn't shipped. This is not an operator overriding a judgment call on a tradeoff (like the two rows above) — it's a factual claim ("there is an API to wire") that current evidence contradicts. Status label updated to operator-active per instruction (not silently dropped), but no `add-provider.sh` command could be produced. Needs operator re-confirmation before further work. | `fleet/state/PROVIDER-WIRING-RECIPES.md` §4, `fleet/state/CG-PROVIDERS.md` | prior SKIP verdict (`PROVIDER-REVIEW-2026-07-10.md`) |
| Featherless | chunked economy-tier units (small decomposed dogfood tickets — not large-context builds) | 2026-07-13 | **needs-compaction-too** — chunking the WORK doesn't bound the per-request CONTEXT within one ticket's own multi-turn execution | aligned (grounded in real captured token data, not a guess) | Real per-unit cumulative input-token totals from `fleet/model-scorecard.tsv` (glm-5.2, TOKEN-CAPTURE cols, captured 2026-07-07): bugfix(S0)=138,760; money-path(S1)=408,237; routing(S2)=191,461; ci-infra(S3)=259,196; refactor(S4)=195,016; greenfield(S5)=402,029; frontend(S6)=2,806,841 — **the smallest observed real unit already sums to ~4.3x the 32K cap.** Caveat (stated, not hidden): this column is a session-total cumulative delta (confirmed via `benchmark/bench.sh`'s snapshot-diff pattern, same semantics as `SLOP-TOKEN-CAPTURE-RECIPE.md`'s `cumulative_usage()`), not a literal single-request peak — so it is an upper-bound signal, not a direct measurement of Featherless's per-request cap. But since agentic tool-call loops resend the growing transcript each turn, the cumulative total across only ~10-20 tool-call turns (observed turn counts in real dogfood `.charon-run.log` transcripts, e.g. `dogfood-eval/results/dogfood-PROVIDER-URL-HELPER-glm-5.2-*.charon-run.log`) means the LAST turn's single-request context is very plausibly already past 32K well before a small unit finishes — chunking bounds ticket SCOPE, not the live resent-context size of the multi-turn session executing that ticket. `RFL-5` (`src/charon/context_shaper.py`, gateway-level context compaction) is the mechanism that would keep live context under a cap like 32K — confirmed **NOT-STARTED** (`fleet/state/READY-AUDIT-2026-07-13.md`, `fleet/state/RUN-NOW-WAVE.md`: "product default; `context_shaper.py` ABSENT"). Verdict: Featherless is not viable for chunked units on chunking alone; it becomes viable only once RFL-5 (or equivalent per-request context trimming) ships. | `fleet/model-scorecard.tsv`, `fleet/state/dogfood-eval/results/`, `fleet/state/READY-AUDIT-2026-07-13.md`, `fleet/FREE-TIER-ROUTING.md` §2.5 | prior REJECTED-for-32K-cap verdict (`FREE-TIER-ROUTING.md` §2.5) — re-opened by chunking + RFL-5, not fully resolved |
| Stripe Smokescreen | egress CONNECT proxy: hostname+IP allowlist w/ resolve-and-pin as the gateway's sole outbound route (Track A) | 2026-07-21 | ADOPT — pin self-built/community image at a known commit | aligned | Tested for THIS deployment (solo, one docker-compose box): purpose-built, MIT, DNS-rebind-safe (resolve-and-pin), RFC1918-blocked-by-default, one compose service + one ACL file, key stays in TLS tunnel (no MITM). Only egress tool evaluated that closes the TOCTOU/rebind race. CAVEAT: release-frozen at v0.0.4 (2022) though master is commit-active to 2026-07-06; no official image — self-build/pin required. Enforcement requires it be the ONLY egress route (pair with docker-native denial). | scratchpad/DEEPDIVE-SECURITY.md §1; RESEARCH-ssrf-egress-proxies.md; github.com/stripe/smokescreen | — |
| Squid | egress allowlist proxy for the gateway (Track A) | 2026-07-21 | REJECT — not DNS-rebind-safe, static reconfig | aligned | HTTPS sees only CONNECT/SNI; SNI-only allowlisting is spoof/rebind-bypassable; restart-to-reconfigure. Smokescreen dominates it for this exact job. | scratchpad/DEEPDIVE-SECURITY.md §1; RESEARCH-ssrf-egress-proxies.md L93 | — |
| tinyproxy | egress allowlist proxy for the gateway (Track A) | 2026-07-21 | REJECT — not a security control | aligned | Basic CONNECT allow-by-host/port, no IP-classification or rebind defense. | scratchpad/DEEPDIVE-SECURITY.md §1; RESEARCH-ssrf-egress-proxies.md L95 | — |
| Envoy | egress SNI-allowlist gateway (Track A) | 2026-07-21 | REJECT — operational surface for solo box | aligned | Same enforcement as Smokescreen at multiples of the config (xDS/YAML). Overkill for one box. | scratchpad/DEEPDIVE-SECURITY.md §1; RESEARCH-ssrf-egress-proxies.md L94 | — |
| docker-native egress denial (internal:true / enable_ip_masquerade=false / DOCKER-USER iptables) | make the egress proxy the gateway's ONLY route out (Track A enforcement layer) | 2026-07-21 | ADOPT — as the enforcement substrate under Smokescreen; NOT alone | aligned | This is what converts "set HTTPS_PROXY" (cooperative) into enforcement. CAVEATS: internal:true breaks published ports (needs 2nd net); enable_ip_masquerade=false UNVERIFIED on 4-LOM; DOCKER-USER rules don't survive host reboot without systemd persistence. Must be backed by a startup self-test or it lapses silently. | scratchpad/DEEPDIVE-SECURITY.md §1,§3; DESIGN-egress-reframe.md §2.2; RESEARCH-community-practice.md §2 | — |
| credential-injecting reverse proxy (nginx envsubst per-provider prefix mount) | app never holds the provider key; broker injects only for mapped prefix (Track C custody) | 2026-07-21 | ADOPT — the custody substrate (Phase 2) | aligned | Commodity nginx + built-in envsubst-on-templates; gateway holds no key and emits only a prefix URL; unmapped prefix 404s (fail-closed); reverse-proxy shape avoids TLS MITM entirely. The per-provider prefix→base+key map is the ~30% glue. Changes the quantifier from ∀-code-paths (fail-open) to ∃-over-secret-store (fail-closed). | scratchpad/DEEPDIVE-SECURITY.md §1,§3; DESIGN-egress-reframe.md §4.1-4.2 | — |
| Infisical Agent Vault | AI-agent credential proxy w/ deny-allowlist as the gateway's custody broker (Track C) | 2026-07-21 | ADOPT-as-alternative / not-a-clean-win | mixed | Most purpose-built + best-maintained (MIT, v0.39.0 2026-06-24, single docker, unmatched_host_policy=deny). BUT requires a local MITM root CA the gateway must trust — the exact solo-hostile property DESIGN §4.1 rejects for forward-proxy custody. Adopt only if operator prefers a maintained product over hand-rolled nginx and accepts custodying a root-CA key on-box. | scratchpad/DEEPDIVE-SECURITY.md §1; RESEARCH-secretless-brokers.md; github.com/Infisical/agent-vault | — |
| CyberArk Secretless Broker | credential-injection broker (app has no creds) for the gateway (Track C) | 2026-07-21 | not-a-clean-win | mixed | Canonical Secretless pattern, Apache-2.0, active (v1.7.32 2026-02-05), single container. BUT generic-HTTP connector's Authorization: Bearer emission for LLM APIs is UNVERIFIED — must prototype; heavier than nginx for one static header shape. | scratchpad/DEEPDIVE-SECURITY.md §1; RESEARCH-secretless-brokers.md L47-54 | — |
| Envoy credential_injector (Generic) | per-route Bearer/API-key injection broker (Track C) | 2026-07-21 | REJECT — operational surface for solo box | aligned | Generic injector is stable core, but xDS/YAML curve and Envoy-Gateway HTTPRouteFilter still v1alpha1; nginx reverse-proxy does the same with far less. | scratchpad/DEEPDIVE-SECURITY.md §1; RESEARCH-secretless-brokers.md L76-92 | — |
| HashiCorp Vault / OpenBao / Infisical-core / Doppler | provider-key custody for the gateway (Track C) | 2026-07-21 | REJECT — wrong class (STORAGE, not injection) | aligned | App fetches and holds the raw key in its own memory/env — leak-to-attacker-URL vuln survives. Good for at-rest storage/rotation only. | scratchpad/DEEPDIVE-SECURITY.md §1; RESEARCH-secretless-brokers.md §critical-distinction | — |
| SPIFFE/SPIRE | provider-key custody for the gateway (Track C) | 2026-07-21 | REJECT — wrong shape + overkill | aligned | Issues mTLS SVIDs, does not attach a provider's static API key; server+agent+attestation excessive for one box. | scratchpad/DEEPDIVE-SECURITY.md §1; RESEARCH-secretless-brokers.md L132-139 | — |
| CodeQL | CI secret→network taint signal on the public GitHub mirror (defense-in-depth) | 2026-07-21 | ADOPT-as-defense-in-depth — never carries the invariant | aligned | Free for public repos (Charon's public mirror qualifies; private needs GHAS $30/committer/mo); GitHub maintains the Python HTTP-client library models, covering the "new client added later" class on someone else's budget. Cannot see runtime full_url mutation (E16). Failure mode is C1's — a query matching nothing looks clean — so whatever lands MUST assert ≥1 finding on a known-bad fixture. Runs on the downstream GitHub mirror, not the Gitea primary. | scratchpad/DEEPDIVE-SECURITY.md §1; DESIGN-egress-reframe.md §3 | — |
| non-overridable base_url + static preset-derived hostname allowlist (validating effective/nested config) | app-layer: provider endpoints from git-tracked presets only, no request override (interim + permanent app layer) | 2026-07-21 | ADOPT — do first (interim), keep as app layer | aligned | The LiteLLM v1.44.8 fix done right; zero infra; own code. MUST validate the effective/NESTED config, not the request's top-level shape — LiteLLM's first fix was a no-op because it checked flat top-level api_base while the value nested in litellm_params. Necessary-not-sufficient (app guards get bypassed) — back with Track A. Allowlist source MUST be git-tracked presets, never runtime providers.json (attacker-writable via add_provider). | scratchpad/DEEPDIVE-SECURITY.md §1; DESIGN-egress-reframe.md §2.1; RESEARCH-community-practice.md §0; RESEARCH-llm-gateway-peers.md | — |
| Claude Code hooks | in-session on-plan enforcement: PreToolUse plan-gate + doom-loop deny + SubagentStop/Stop block, MUST fire inside Task subagents and `claude -p` headless | 2026-07-21 | ADOPT (Claude-driven path) — with CI backstop + hook-config self-protection | aligned | Re-verified against CURRENT docs (code.claude.com/docs/en/hooks, 2026-07-20): SubagentStart/SubagentStop events now exist and PreToolUse/PostToolUse fire inside subagents (new `agent_id` field: "Present only when the hook fires inside a subagent call"); deny works headless. This CLOSES the prior "subagents skip parent hooks" assumption (#43772) for the Claude path. Residual: ~5% escape, Bash `>`/heredoc write paths, model can self-edit `.claude/settings.json` (RFC #45427) — so hooks are best-effort in-session early-warning, CI required-checks are the authority. Does NOT cover the opencode leg. | https://code.claude.com/docs/en/hooks ; https://github.com/anthropics/claude-code/issues/45427 | prior "subagent/headless skip hooks" assumption in RESEARCH-drift-spec-driven.md (#43772) and META-GATE-SYNTHESIS.md |
| OPA / Rego (opa eval / conftest, local binary) | tool-call or CI policy gate for on-plan enforcement | 2026-07-21 | DEFER — not adopted now | aligned | Real deterministic enforcement, but it needs a caller (a hook or CI step) to invoke `opa eval` — and that caller is exactly the Claude Code hook / CI required-check we are already adopting. Adds a second policy engine + versioned Rego surface for a solo op with low policy volume. Earns adoption only once shell-hook conditions outgrow readability. | RESEARCH-drift-guardrail-frameworks.md §OPA ; https://www.openpolicyagent.org/docs | — |
| Invariant Labs Guardrails Gateway | agent-proxy tool-call/flow guardrail in front of the LLM | 2026-07-21 | REJECT for this problem (not a security-supply-chain reject) | aligned | Apache-2.0, real tool-order+content interception, but deploys as a SECOND gateway/daemon in front of the model. Charon already IS an OpenAI-compatible gateway — flow/content enforcement belongs IN the Charon gateway process, not a parallel proxy that doubles operational surface for a solo/one-box operator. | RESEARCH-drift-guardrail-frameworks.md §Invariant ; https://github.com/invariantlabs-ai/invariant | — |
| Danger (JS/Ruby) | PR-time required-check for on-plan/convention enforcement | 2026-07-21 | SKIP | aligned | Real (fails the PR) but redundant: Semgrep + Charon's existing gates as required status checks already cover the merge boundary, and Danger adds a Node/Ruby bot to maintain. No unique enforcement it provides that the adopted CI checks do not. | RESEARCH-drift-guardrail-frameworks.md §Danger | — |
| Semgrep | CI required-check backstop (merge boundary) for on-plan/policy enforcement across ALL harnesses | 2026-07-21 | ADOPT | aligned | Server-side, un-bypassable, runs on the diff regardless of which harness produced it — the only enforcer that covers the opencode leg and any non-Claude gateway worker. Exit 1 blocks merge via branch protection; lands on the pending `charon-ci` runner. This is the authority tier under the "verify a gate EXECUTED, not just CI green" doctrine. | RESEARCH-drift-guardrail-frameworks.md §Semgrep ; https://semgrep.dev | — |
| ReviewerCircuitBreaker (in-tree, src/charon/failover.py:73-142) | reuse as the doom-loop circuit-breaker primitive (fingerprint same-shape failure, trip after 3, cooldown, reset) | 2026-07-21 | ADOPT-REUSE — do not import/rebuild | aligned | Already implements the exact primitive the research prescribes: CLOSED→OPEN after threshold=3 consecutive failures, OPEN→HALF_OPEN cooldown, thread-safe. The novel glue is (a) fingerprinting the APPROACH (diff-target + failing-gate hash) not just (tool,args), and (b) forcing a context RESET/reframe on trip — wrap this class, don't adopt an external circuit-breaker. | src/charon/failover.py:73-142 ; RESEARCH-drift-practitioner.md §D | prior UNRESOLVED "tenacity/litellm circuit-breaker" framing in EVAL-REGISTRY LiteLLM cooldown row |
| opencode native permission + doom-loop detection (opencode.json `permission` key) | enforcement layer for the `opencode run --model charon/*` leg | 2026-07-21 | ADOPT-CONFIGURE — partial, CI backstop required | mixed | opencode does NOT honor Claude Code `.claude/settings.json` hooks (separate harness), so Claude-path hooks give it ZERO coverage. Its native permission config + doom-loop detection are the closest in-harness lever, but its plugin `tool.execute.before` hook has a subagent-bypass bug (issue #5894, open) — same class of hole we just closed on the Claude side. Configure the permission ruleset for primary-agent gating; treat CI required-checks as the un-bypassable backstop for anything subagents route around. | https://github.com/sst/opencode/issues/5894 ; https://deepwiki.com/sst/opencode/5.2-permission-system | — |
| Independent adversarial verification as mechanized default | wire outcome-verification into fleet-droid.sh launch path + re-run as a CI required-check (never a steering doc) | 2026-07-21 | ADOPT | aligned | The ONLY mechanism that held every round this session — every bad key-fix was caught by an independent reviewer, never by a rule. Practitioner data: 20-30% of subagent SUCCESS summaries contain a claim not matching tool output. Must attach where work runs (the launcher we own) AND at CI so a lying "green" cannot land. Reuse ReviewerCircuitBreaker so a doom-loop of failed verifications trips rather than retries. | META-GATE-SYNTHESIS.md ; RESEARCH-drift-practitioner.md §C,§G ; src/charon/failover.py:73-142 | — |
| LiteLLM Router | gateway commodity plane as an adopted LIBRARY — litellm.Router for cooldown/allowed_fails/failover/mechanical-ordering, imported without the proxy/FastAPI/Prisma stack | 2026-07-21 | ADOPT | aligned | The prior UNRESOLVED row's non-decision rested on the stdlib-only / AP-1 basis REMOVED by MANAGER-OPERATING-RULES §0 (2026-07-21), plus an AP-5/AP-7 "port it natively in ~N lines" alternative that now carries NEGATIVE weight. litellm.Router imports as a library alone; ADR-0017 grades it PROVEN (spike PASS, deletes ~650-750 LOC of hand-rolled money-path). Gateway-MVP deep-dive confirms the adopt/build split: adopt the whole commodity plane, build only the outcome-grade overlay + grade store. | scratchpad/GATEWAY-MVP-ADOPT-VS-BUILD.md; docs/adr/0017-outcome-graded-gateway.md | prior UNRESOLVED LiteLLM Router cooldown/health row (EVAL-REGISTRY.md) |
| PyYAML | parsing fleet/board/*.md ticket frontmatter inside the rig's CI checks — the frontmatter reader for the substrate-first gate, replacing hand-rolled sed/case/awk field extraction | 2026-07-21 | ADOPT — shipped, pinned PyYAML 6.0.3 | aligned | The hand-rolled sed/awk frontmatter parser was not hypothetically worse, it was measurably worse: an adversarial review of v1 defeated it nine ways and five of those (unrecognised alignment defaulting to the permissive value, prefix tool matching, markdown-escaped-pipe column shift, CRLF skip, trailing-whitespace skip) exist only because a parser was hand-rolled. Replacing the commodity parsing layer with a real YAML parser and keeping only the novel registry-consult engine is adopt-first in action. The stdlib-only objection does not apply here — this is the RIG on ubuntu-latest, not the product's privileged core. Pinned in fleet/checks/requirements.txt so the parser version cannot drift under the gate. | fleet/checks/substrate_first_gate.py ; fleet/checks/requirements.txt ; .github/workflows/rig-ci.yml | — |
| gitleaks | CI required-check secret-scan (merge boundary) on the charon-ci runner | 2026-07-21 | ADOPT | aligned | gitleaks is the de-facto maintained secret scanner; this ticket ships only the thin wrapper+workflow+canary novel slice with no hand-rolled scanner; additive to (does not replace) the existing leak-guard.sh. | https://github.com/gitleaks/gitleaks ; fleet/board/GITLEAKS-ADOPT.md | — |
| bandit | CI required-check Python SAST (merge boundary) on the charon-ci runner | 2026-07-21 | ADOPT | aligned | bandit is the maintained best-in-class Python SAST; net-new danger-pattern coverage (eval/exec, subprocess shell=True, pickle, yaml.load, weak crypto) the rig has no gate for; ships only the thin wrapper+workflow+canary novel slice. HONEST tension: fleet/state/KSF-LINTER-TOOLS-REVIEW.md said "do-not-wrap bandit" — but that verdict was scoped to the PRODUCT/KSF stdlib-first core (adding a dep to a self-contained product); THIS scope is a rig CI required-check on ubuntu-latest where the stdlib constraint does not apply (same reasoning as the adopted Semgrep row). | https://bandit.readthedocs.io ; fleet/board/BANDIT-ADOPT.md | — |
| vulture | rig CI dead-code check evaluated as a REPLACEMENT for the hand-rolled tools/check_inert_code.py (reachability-from-entrypoint inert-code gate) in the product tree | 2026-07-22 | REJECT — KEEP-HANDROLL (cannot replace a reachability gate) | aligned | Tested vulture 2.16 on the real product tree AND a reproduction fixture, not docs. vulture is a REFERENCE-COUNTING detector: a symbol is "used" if referenced anywhere in the scanned files. The hand-roll is a REACHABILITY-FROM-ENTRYPOINT detector (BFS over the import/call graph from pyproject scripts / __main__). Proven decisive gap: a mutually-referencing "built-but-never-wired" island (A→B, B→A, neither reachable) — the exact bug class the gate exists for — is MISSED by vulture (0 findings, rc 0) but FLAGGED by the hand-roll (both symbols RED). vulture also has NO registration-awareness, NO @inert_by_design reason-bearing allowlist, and NO green-without-hiding structured disposition ledger; its default run emits 156 mixed-class findings (unused locals/dataclass-fields/interface-methods) vs the hand-roll's 66 dispositioned dead public symbols, and raising confidence to >=80 collapses to 12 findings that are ONLY unused local variables (ruff F841 territory), never the dead-public-symbol class. Not even viable as defense-in-depth: its sole delta over the hand-roll (unused locals/imports) is already ruff's domain, so DiD buys a redundant noisy signal for a new pip dep. The prior KSF linter review rejected vulture for the product/stdlib scope; re-tested honestly for the CI-check/replace scope the registration-awareness loss is real and reproduced, so KEEP-HANDROLL stands. | fleet/state/VULTURE-EVAL.md | — |
| Faktory reserve/ack (claim-lease substrate) | exactly-once ticket claim: close manager-sub double-claim + re-claim-of-done + restart-re-offer | 2026-07-24 | ADOPT-PARTIAL (Faktory RESERVE/ACK/reserve_for + RDB = atomic-terminal + restart-durable; PAIRED with WORK-LEASE-GATE enforcement + ticket-id idempotent-enqueue guard for the manager-sub bypass) | aligned | Read the real owned code: claim.sh:207 flock covers only pool-vs-pool selection, so it CANNOT constrain a manager-sub that never calls claim.sh (leak#1 = chokepoint defect, not race); the claims->submitted->done markers are 3 dirs written by 3 actors with a manual+gh-gated terminal marker (done.sh:148) and no fencing token, leaving a delivered-but-not-done re-claimable window (leak#2, LIVE: KSF-VENDOR-GATES = HEAD 1e6d174 has a stale claim + no done marker) that a restart misreads as fresh work (leak#3). Faktory ACK collapses the 3-marker dance into one atomic reserve->ack terminal transition and RDB makes an ACKed job durably un-fetchable across restart (structurally closes #2/#3, EXECUTED-verified in TRIAL-FAKTORY). OSS Faktory does NOT dedup PUSHes (unique_for is Enterprise, verified), and no queue can constrain a non-enqueuing worker, so leak#1 is closed by WORK-LEASE-GATE forcing every path through the same enqueue chokepoint + a ticket-id idempotent-enqueue guard reusing claim.sh's flock. Postgres SKIP LOCKED + UNIQUE(ticket_id) / river / etcd(mod_revision fence) are ADOPT-viable and correctness-stronger but need a new substrate + SQL port of the 5-axis ladder; Faktory wins on integration cost for this bash/file solo fleet and is already EXECUTED ADOPT-PARTIAL. One stack with FAKTORY-ADOPT; hand-roll rejected on AP-5/AP-7/AP-12. | fleet/state/CLAIM-INTEGRITY-EVAL.md ; fleet/claim.sh:207,299-302,337 ; fleet/done.sh:148 ; fleet/board/WORK-LEASE-GATE.md ; TRIAL-FAKTORY.md ; github.com/contribsys/faktory/wiki/Ent-Unique-Jobs | builds-on Faktory ADOPT-PARTIAL row (TRIAL-FAKTORY, 2026-07-24) |

## Backfill note

These 7 rows are the LiteLLM slice of the full 14-instance sweep in
`fleet/state/EVAL-REGISTRY-RECONCILE.md` JOB 1 (collapsed from 14 raw citations to 7
distinct tool×scope rows; the remaining raw citations were N/A/tangential mentions with no
independent verdict). Non-LiteLLM tool evals that already exist as standalone docs
(`MEMORY-TOOL-EVAL.md`, `KSF-LINTER-TOOLS-REVIEW.md`, `PRICING-TOOLS-EVAL.md`,
`WORK-DECOMPOSER-TOOL-EVAL.md`, `LIGHT-MODELS-EVAL.md`, `EVAL-FRAMEWORK-CAPABILITY.md`,
`EVAL-RelayFreeLLM.md`, `RELAYFREELLM-COMPARISON.md`, `HAND-ROLLED-AUDIT.md`, etc.) have NOT
yet been backfilled as rows here — do that incrementally the next time any of those tools
comes up for re-evaluation, rather than in one bulk pass.

## HAND-ROLL JUSTIFICATION ANTI-PATTERNS (negative weight — treat adversarially)

These are the reasons historically used to confidently recommend HAND-ROLLING or reject
ADOPTING. As of 2026-07-21 (`MANAGER-OPERATING-RULES.md` §0; memory `no-rig-as-product-adopt-dont-handroll`)
each carries NEGATIVE weight in any build-vs-adopt eval: seeing one is evidence the eval is
DRIFTED, not a point for the build side. If a hand-roll recommendation rests on one of these,
it is unsupported until the adversarial counter is met with tested evidence. Full derivation +
citations: `scratchpad/HANDROLL-JUSTIFICATION-ANTIPATTERNS.md` (mined from EVAL-REGISTRY-RECONCILE,
HAND-ROLLED-AUDIT, GAP-AUDIT-toolfirst, KSF-LINTER-TOOLS-REVIEW, ADRs 0003/0004/0005/0017).

Front-of-eval standing cost: months of ~zero shipped product while a hand-rolled rig grew
as large as the product; ~70% of every layer was reinvented commodity; hand-rolled guards
(sed/awk YAML parser, bespoke AST linter) shipped weaker & buggier than the tools they
replaced. The build side starts in this hole.

- AP-1  "core is stdlib / no-dependency / self-contained"  → INVALID for the substrate; applies ONLY to the product privileged core, never the rig/CI/dev-tooling. Name the boundary first. [root cause of the whole drift]
- AP-2  "it's the differentiator / don't outsource the core"  → prove it's the novel ~30% AND that the differentiation has shipped; else it's protecting commodity under a "core" label.
- AP-3  "too heavy / wrong stack (FastAPI/Postgres/Redis/200MB)"  → weigh only the SLICE needed (data-only / library-only / lazy plugin); check if the dep is already in-repo (FastAPI+uvicorn already ship for service/app.py).
- AP-4  "supply-chain / security risk"  → ZERO weight without a cited CVE/advisory/SUPPLY-CHAIN.md finding; otherwise drifted hand-waving.
- AP-5  "borrow the design, don't embed"  → porting a pattern IS a hand-roll (you own+maintain it); test library-only and plugin-wrap first.
- AP-6  "genuinely novel, no tool fits"  → is the novelty inherent or manufactured by a prior hand-roll? Requires a code-read of candidates; novel slice is only outcome-graded routing + OOB grader + disjoint-owns.
- AP-7  "we can do it in ~N lines / dependency-free"  → LOC is the floor not the cost; hand-rolled parsers/linters/guards shipped weaker than the tool. Preference presented as a finding.
- AP-8  "duplicative of our runner/core"  → question-begging; ask "does this make our hand-roll UNNECESSARY," not "does it duplicate it." Durable-exec engines / merge-queue were never even evaluated under this framing.
- AP-9  "overkill for a solo dev / not multi-tenant"  → solo-dev is a reason to ADOPT (offload maintenance); name the specific unused capability that costs you (server/broker/ops-team) or it doesn't count.
- AP-10 "single-person / low-star / new / unmaintained"  → judge by reading the code, not stars/age; Charon/SLOP are zero-star too. Low-star is not a disqualifier.
- AP-11 "lock-in / license"  → quantify: if adoption is reversible (your data, your format) lock-in ~0; read the license (MIT/Apache = adopt) instead of asserting it.
- AP-12 "source/README inspection shows it won't fit" (a REJECT justified by reading the code/docs, NOT running it)  → INVALID as disproof whenever the tool is RUNNABLE on an available host. The fleet has real hosts (4-LOM — Docker usable non-root, 12 cores/11GB; plus BB-8, Rocinante, LO-LA59, others); "we couldn't run it hands-on" is almost never true. A REJECT verdict requires an EXECUTED trial — commands + observed output/errors — not a file:line citation. Source-reading can conclusively confirm an ABSENCE (e.g. "no `gitea/` adapter dir exists"), but it can NEVER stand in for the make-or-break RUN of a runnable candidate. A registry receipt that is only source/docs inspection is a claim, not a trial.

Rule: a build-vs-adopt verdict that leans on any AP-# above is DRIFTED by default and must
be re-tested against its adversarial counter before it can be cited as settled. State the
COMMODITY-SUBSTRATE vs NOVEL-SLICE split explicitly and adopt the substrate unless there is
specific tested evidence (not one of these assertions) that it doesn't fit.

HAND-ROLL IS NEVER THE DEFAULT (operator, 2026-07-23; MANAGER-OPERATING-RULES.md §0). This binds
verdicts AND their rankings — including DESIGN-SPIKE deliverables, not only build tickets:
- A reject's "next-best" MUST LEAD with an ADOPT candidate. A hand-roll may appear as next-best
  ONLY after EVERY adopt candidate has been EXECUTED (AP-12) and adversarially disproven. Ranking
  hand-roll first — or presenting "~N LOC we own" as the recommended path — is itself an AP-5/AP-7
  drift and is REJECTED on sight.
- "Pattern-only / borrow the design / re-implement it ourselves" is a hand-roll (AP-5): it must
  clear the same executed-trial + adopt-disproof bar as any other build option.
- A design spike that recommends hand-roll for a runnable candidate without an executed trial for it
  has NOT met its accept bar, regardless of how well-cited the source read is — bounce it, do not land it.
