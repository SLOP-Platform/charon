S	FRONTIER tier still funding-blocked: fund/recover a non-Anthropic frontier leg (nanogpt weekly-cap reset OR top up openrouter OR add a direct paid leg) so GPT/Gemini-class models can be preflighted. No Anthropic.
Z	MONIT install is still gated on WATCHDOG-RESTART-CMDS-VERIFY landing. Two sudo steps then needed, both on the LOCAL WSL box (NOT 4-LOM): install the bench-grader systemd unit from fleet/watchdog/units/, then install+enable monit. Do NOT run 'monit enable --now' before the verify gate passes.
#2	SIX DEAD GATEWAY MODULES need wire-or-RETIRE calls: RequestInspector, SessionAffinity, Observability, SpeculativeExecutor, ConsensusRouter, VirtualKeyManager — all constructed at gateway.py:258-296, ZERO invocation sites. Retiring is destructive and needs your sign-off; SpeculativeExecutor/ConsensusRouter/SessionAffinity sound like routing behaviour someone may believe is live.
#5	F5 ANSWERED by research, needs your OK: adopt the ALREADY-BUILT src/charon/decompose_effort.py (2.0*difficulty + 0.15*size + 1.0*behaviours) instead of nsurf>=3. Measured on our board: nsurf vs tier rho=+0.075 (noise), difficulty rho=+0.413. External options killed on our own data or need training data we lack.
#6	GATE WEAKNESS (new, needs a call): rig merges are NOT test-gated. fleet/land.sh:298 auto-detects validate_board.sh (a board STRUCTURAL check) for the rig — it never runs fleet/gate.sh, so a rig branch can merge with any number of failing tests. MANAGER-OPERATING-RULES §8 mandates the FULL gate as the merge gate; the rig path does not honour it. Decide whether land.sh's auto-detect should pick gate.sh for the rig without breaking the product path.
#8	PUSH charon-private (21+ ahead, reconciled at c3312d6, never pushed) via the sanctioned path — land.sh/land-push.sh, NOT raw git push.
#9	4-LOM REDEPLOY held by decision 10a until qui-gon-jinn (SW-IDENTITY-FOLD) reports — its done-contract measures live /charon/status pool counts. Deployed build 289cf93 is 12 commits behind master; ships 0947401 (capability grading) + fd03840 (energy_kwh money bug).
#10	PREFLIGHT-OWNS-ARBITRATE now BLOCKS the push: land-push.sh gates on validate_board.sh and refuses on RED (exit 4). Launch it: cd /home/stack/charon-private && opencode --model charon/deepseek-v4-pro  then paste: Read /home/stack/charon-private/prompts/PREFLIGHT-OWNS-ARBITRATE-opencode.md and execute it exactly as written.
#11	V needs a board ticket before it can be routed to opencode (NVIDIA NIM cleanup: add-provider.sh --base-url, stop echoing key, NIM limits in free_tier_catalog). Say go and I author it.
#12	REAP FOLLOW-UP: 16 rig branches were SKIPPED — they landed by re-derivation so content is byte-identical but SHAs differ (1-6 unique commits each vs origin/master). ALL 16 are published on gitea at their exact local SHA, so approving them under a CONTENT-EQUIVALENCE rule (rather than SHA-reachability) is zero-risk. Approve? Also: SECRET-HOTROTATE never landed and its 11 eval implementations now exist ONLY in fleet/handoff-notes/salvage-reap-2026-07-26/ (now committed, a0d9000) — review before that dir is ever discarded.
