#15	GATE 4 UNDELIVERED: fleet/handoff-notes/LETTA-REVIEW.md and MEMORY-LAYER-REVIEW.md were commissioned, completed, and never read/reported. grep -c Letta EVAL-REGISTRY = 0. ~10 verdicts owed. Survived 2 sessions unread.
#16	MONEY LEAK: $30.86 spent today; openrouter (cost_rank 50) sits AHEAD of deepseek-direct (cost_rank 8) in pool order, direct legs verified working. No per-provider spend tracking, monthly_limit_usd=0.0 (no cap). Fixes: FORWARDER-COST-ORDER-FALLBACK + PARK-REARM-FUNDED-PROVIDER, both P0 READY.
#17	4 GATE-3 tickets approved 2026-07-31 and STILL never staged: SPAWN-VIA-CAPABILITY, ENGINE-CONVERGE, PRICING-FEED, ORCHESTRATION-RE-RUN. PRICING-FEED's operator-only content (Price-Per-Token MCP, MCP-first shape, multi-source-over-SSOT) exists ONLY in the satele-shan handoff.
#18	OPENCODE-GO UNUSABLE: providers.json has only funding_class for opencode-go — NO base_url and NO key_env, and no opencode-go secret exists. Enabling its models yields 'no route — no provider configured'. Operator: supply the opencode-go base_url + API key (you want it used with MiniMax M2.5, Deepseek V4 Flash rank=5, MiMo-V2, MiMo-V2.5). NOTE MiMo-V2/V2.5 do NOT exist under opencode-go in the catalog at all — only via openrouter/nanogpt/deepinfra.
#19	NAME-POOL LEAK: every fleet/handoff.sh and handoff-check.sh invocation makes claim-jedi-name.sh allocate a name and leave a 1-line SESSION-HANDOFF-<name>.md claim stub. Those stubs enter the exclusion set, so names are BURNED without a real session ever using them. Four burned this session (rey-skywalker, saba-sebatyne, sifo-dyas, tahiri-veila) — removed by hand. Pool is 69 names. Fix: only claim on real handoff generation, or reap unused stubs.
#20	END-SESSION GATE IS SELF-BLOCKING: fleet/end-session.sh creates fleet/SESSION-HANDOFF-<SESSION>.md via redirection, THEN calls handoff.sh, whose claim-jedi-name.sh allocator sees that 0-byte file and refuses the name as 'already in use'. The gate therefore ALWAYS fails before it runs its work-loss check. Confirmed 2026-08-01 with SESSION=tott-doneeta. This is the one gate meant to prevent work loss at session close and it cannot complete. Fix: have end-session.sh write to a temp path and move into place, or exempt its own target from the allocator's exclusion set.
#21	TOOL CAPABILITY ~20% ON: measured audit at fleet/state/TOOL-UTILIZATION-AUDIT.md. Top-5 enablements APPROVED, not started (ruff preview=12 defects; extend-select S/BLE/ARG/C90=184 findings incl 3 HIGH security; mypy 3 flags=176 bugs; shellcheck -o all=15 error-level; graphify affected=0 invocations). ALL outstanding work is in fleet/state/PRIORITY-TODO.md — next session MUST read that first.
#22	DURABILITY GAP: MANAGER-OPERATING-RULES.md (now carries the new §14 catalog-is-live-data doctrine) is loaded at SessionStart from /home/stack/code/charon/.claude/settings.local.json — a machine-local, untracked file. fleet/hooks/session-start.sh has ZERO references to it. A fresh clone or another box will NOT load the doctrine. Harden into the tracked hook.
#23	charon-bot PAT still needed (approved). Account EXISTS but all 16 open PRs show user.login=Nnyan, so GitHub refuses request-changes on our own PRs and the reviewer!=builder guard cannot key on user.login. Needs a PAT + collaborator access on BOTH repos.
#24	SESSION-END SELF-BLOCKING — HIT AGAIN 2026-08-01 and ESCALATED. handoff.sh refused SESSION=saba-sebatyne live at close; end-session.sh has the identical shape. SESSION-END-GATE-REPAIR is LIVE but UNCLAIMED (ticketed, never scheduled). Worse: HANDOFF-NAME-ALLOCATOR is archived+DONE and the allocator STILL BLOCKS — verify its firing layer, it may never have fired. Fix the ALLOCATOR (exempt the caller's own target file), not one call site. Detail: fleet/state/PRIORITY-TODO.md section J.
#25	STRANDED WORK at 2026-08-01 close: fix/shared-namespace-contention is 25 COMMITS AHEAD of its remote (PR #345 open — do NOT assume the PR covers them). Largest unpushed body on the box. Verify contents, land or explicitly drop. Detail: PRIORITY-TODO.md section K.
#26	ROOT/CLASS — 96 UNPUSHED COMMITS ON 47 LOCAL-ONLY BRANCHES (charon-private 25br/38c, charon 22br/58c; excludes backup/*). These have NO upstream at all — never pushed, so a disk failure or stray reset loses them permanently. Largest: feat/cwd-config = 26 unique commits. WHY IT RECURS: the fixes are THEMSELVES stranded — feat/stranded-work-detect-v2 (1 commit) and feat/session-end-push-gate-v2 (3 commits) both have NO remote, and SESSION-END-PUSH-GATE still reads not-started on the roadmap. Each session rebuilds the fix and loses it the same way. ACTION ORDER: (1) push all 47 branches to remote FIRST (non-destructive, reversible, triage later) or explicitly delete the dead ones; (2) land the two existing fix branches; (3) widen the gate to ALL loss classes not just ahead-of-upstream; (4) run it on a CADENCE not only at close. Detail: fleet/state/PRIORITY-TODO.md section L.
#27	NEXT SESSION FIRST SIX ACTIONS (operator-set 2026-08-01 close, launch in TABS): (0) RESCUE — push the 47 local-only branches / 96 commits BEFORE building anything; (1) land feat/stranded-work-detect-v2 (1 commit, no remote); (2) land feat/session-end-push-gate-v2 (3 commits, no remote); (3) verify HANDOFF-NAME-ALLOCATOR firing layer — archived+DONE yet still broken; (4) claim SESSION-END-GATE-REPAIR — LIVE but never scheduled; (5) ADOPT a tool to run the loss-gate CONTINUOUSLY in the background (try monit, already in the rig) because a close-gate never fires when a session dies on a token limit. Also: fix/shared-namespace-contention is reviewed and push-BLOCKED by a gate defect (ticket IS on master at f8266ef and owns the files) — diagnose the gate, do not --force. Detail: PRIORITY-TODO.md START HERE.
#30	STRANDED WORK: 8 finding(s) — 3 x dirty-worktree, 5 x unpushed-branch. Full list: fleet/checks/stranded-work.sh (SW_LIMIT=0). Recover with fleet/land.sh <branch> <repo>; never reap by hand.
#31	GRAPHQL QUOTA — TWO levers, we are only pursuing one. MEASURED 2026-08-02: GraphQL sat at 0/5000 AT THE RESET BOUNDARY (fleet drains 5000 the instant they refresh) while REST core sat UNTOUCHED at 5000/5000. Consequence: land-push cannot verify CI, gh pr review fails, merges stall fleet-wide — it is currently the binding constraint on landing anything. LEVER 1 (in flight): PR #392 PR-QUEUE-REST-ETAG cutover into review-pool.sh — REST + ETag gives a ZERO-QUOTA steady state because conditional 304s do not decrement. Item 1 of UNBLOCK-REVIEW-INFRA, claimed by a droid. LEVER 2 (needs you, and is currently mis-framed): the charon-bot PAT, operator action #23. It is filed as a review-attribution fix (all 16 PRs show user.login=Nnyan so GitHub refuses request-changes on our own PRs) — but a second identity is ALSO A SECOND 5000-point GraphQL pool AND a second REST pool. Splitting reviewer traffic onto charon-bot roughly doubles effective quota and isolates reviewer burn from the manager's landing path, so reviewers can never again starve merges. Worth doing for the quota alone even if attribution did not matter.
#32	RESCUE-PUSH NEEDS ITS OWN CRON ENTRY — the detect half is mechanized, the ACT half is not. fleet/checks/stranded-work-cron.sh (every 20 min, working — heartbeat fresh, escalating as action #30) only DETECTS. The thing that actually rescues at-risk branches, fleet/rescue-push.sh, has been running as a SESSION-BOUND loop in the manager's shell and DIES when that session ends. That is the same single-point-of-failure the whole work-loss class is about: a fix that only exists while one session is alive. Non-destructive by construction — it never force-pushes, never deletes, and parks diverged branches on rescue/* refs instead of overwriting. Install alongside the detector:  (crontab -l 2>/dev/null; echo '*/30 * * * * /home/stack/charon-private/fleet/rescue-push.sh --push >/dev/null 2>&1') | crontab -   . Verify: crontab -l | grep rescue-push  AND confirm at-risk count drops without a session running (bash fleet/rescue-push.sh with no args = dry run).
#33	CORRECTION to operator action #23 — its premise is FALSE and it is unactionable as written. VERIFIED 2026-08-02: gh api users/charon-bot resolves to a REAL THIRD-PARTY account (login=charon-bot, name='Mr. Charon', created 2018-11-05, 0 public repos). The operator has confirmed they have never had such an account. A prior session ran that same lookup, saw the name resolve, and recorded 'the account EXISTS' as evidence it was OURS — it was evidence only that the name is TAKEN. We can never hold a PAT for someone else's account. WORSE: fleet/droid-identity.sh:36-42 stamps every droid commit as charon-bot@users.noreply.github.com, which maps to THAT PERSON'S account, on a PUBLIC repo. Ticketed as DROID-IDENTITY-THIRD-PARTY (P0). If a second GitHub identity is still wanted (reviewer!=builder, and a second 5000-point API quota pool per action #31), it must be an account the OPERATOR creates and controls.
#34	RETIRE #18 — VERIFIED STALE 2026-08-02. opencode-zen AND opencode-go are both fully registered on the live gateway (base_url + key_env present, secret in /data/secrets.json) and both ROUTE: opencode-go serves deepseek-v4-flash-go + minimax-m2.5-go (the latter is in the live strong chain), opencode-zen serves 60 models. Nothing is needed from the operator. NEW RISK found while verifying: opencode-zen exposes claude-* models (claude-opus-4-1, claude-fable-5, claude-haiku-4-5). The HARD rule is SG NEVER routes via Claude/Anthropic and it is on record as regressing repeatedly. No tier chain currently includes them, but they are now reachable via a live provider. Recommend an ASSERTION that no tier chain may contain an Anthropic-served model — it survives catalog refreshes, whereas an exclusion list rots.
#35	SESSION CLOSE 2026-08-02 — TWO ITEMS FOR TRIAGE. (1) SALVAGE BRANCH: salvage/product-main-leak-20260802 (product repo, pushed) holds uncommitted droid work found in the PRODUCT MAIN CHECKOUT at close — edits to src/charon/decompose.py and tools/check_arch.py that differ from BOTH master and feat/api-decompose-cycle-fix, plus two gate tools never in git (tools/diff_cover_gate.sh, tools/mutmut_diff_gate.sh) and a PRODUCT-PR-QUEUE review-log. A droid wrote into the main checkout instead of its worktree — the leak-guard class. Found by a manual close sweep, NOT by a gate: nothing alarmed. Triage and either land onto the right ticket's branch or close with evidence. Backup: fleet/state/wip-backup-20260802-close/. (2) SPEND-METRIC-TRUSTWORTHY is quarantined AGAIN (droid=strong-2780986 count=3) — the zero-commit spin is still live and is ticket #1 for the next session (ZERO-COMMIT-SPIN).
