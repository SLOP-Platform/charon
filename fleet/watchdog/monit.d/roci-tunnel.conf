# --- roci-tunnel (owner: fleet) — GENERATED from service-registry.tsv; do not hand-edit ---
check program roci-tunnel with path "/usr/bin/test -S /home/stack/.charon/coordinator-charon.sock"
    start program = "/bin/sh -c 'fleet/bridge-reconnect.sh'" with timeout 60 seconds
    if status != 0 for 2 cycles then restart

