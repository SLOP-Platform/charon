# --- grader-daemon (owner: bench-grader) — GENERATED from service-registry.tsv; do not hand-edit ---
check process grader-daemon matching "grader-daemon\.py"
    start program = "/bin/sh -c '/usr/bin/pkill -u bench-grader -f grader-daemon.py; /usr/bin/setsid /usr/sbin/runuser -u bench-grader -- /usr/bin/python3 /home/stack/charon-private/fleet/benchmark/grader-daemon.py >>/tmp/grader-daemon.log 2>&1 </dev/null &'" with timeout 60 seconds
    if not exist for 2 cycles then restart
    if 4 restarts within 6 cycles then alert
check file grader-daemon-freshness path "/home/stack/charon-private/fleet/model-scorecard.tsv"
    if timestamp > 5400 seconds for 2 cycles then exec "/bin/sh -c '/usr/bin/pkill -u bench-grader -f grader-daemon.py; /usr/bin/setsid /usr/sbin/runuser -u bench-grader -- /usr/bin/python3 /home/stack/charon-private/fleet/benchmark/grader-daemon.py >>/tmp/grader-daemon.log 2>&1 </dev/null &'"
    if timestamp > 5400 seconds for 3 cycles then alert
    if does not exist then alert

