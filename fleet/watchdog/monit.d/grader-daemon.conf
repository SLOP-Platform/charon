# --- grader-daemon (owner: bench-grader) — GENERATED from service-registry.tsv; do not hand-edit ---
check process grader-daemon matching "grader-daemon\.py"
    start program = "/bin/sh -c 'systemctl restart bench-grader-daemon'" with timeout 60 seconds
    if not exist for 2 cycles then restart
    if 4 restarts within 6 cycles then alert
check file grader-daemon-freshness path "/home/stack/charon-private/fleet/model-scorecard.tsv"
    if timestamp > 5400 seconds for 2 cycles then exec "/bin/sh -c 'systemctl restart bench-grader-daemon'"
    if timestamp > 5400 seconds for 3 cycles then alert
    if does not exist then alert

