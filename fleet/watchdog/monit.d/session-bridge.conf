# --- session-bridge (owner: fleet) — GENERATED from service-registry.tsv; do not hand-edit ---
check process session-bridge matching "session-bridge/proxy\.py"
    start program = "/bin/sh -c '/usr/bin/pkill -u stack -f session-bridge/proxy.py; /usr/bin/setsid /usr/sbin/runuser -u stack -- /usr/bin/python3 /home/stack/.config/opencode/session-bridge/proxy.py >>/tmp/session-bridge.log 2>&1 </dev/null &'" with timeout 60 seconds
    if not exist for 2 cycles then restart
    if 4 restarts within 6 cycles then alert

