# HANDOFF — 2026-07-10 (session E) — Charon fleet MANAGER

## Bootstrap (paste as the next session's first message)
```
Read and fully follow /home/stack/charon-private/fleet/HANDOFF-2026-07-10-e.md — you are the fresh Charon fleet MANAGER.
```

## FIRST ACTIONS / NEXT (in priority order — operator-set)
0. MANAGER-OPERATING-RULES auto-loaded (SessionStart). Run `preflight.sh` (now: retire-done → board_gate →
   reds scan; **exits non-zero on any red**). Re-verify git/gh/ssh. Register on the session-bridge (repo:charon)
   under a NEW Jedi name.
1. **PRIORITY 1 — launch an INDEPENDENT AUDIT (read-only sub-session): blast-radius on the class "MANUAL STEPS
   that never happen / are fragile / flaky."** Enumerate every human/manual step in the fleet (done.sh,
   land-push, claim, handoff, marker-writing, pushes, worktree hygiene, brief/prompt creation, key-wiring, …),
   rank by rot-risk + blast-radius, and mechanize the top offenders — SAME pattern proven this session
   (board_gate, retire-done, project-scoped drift). Present findings BEFORE mechanizing. This is the operator's
   top ask for next session.
2. **PRIORITY 2 — BENCH-OOB-GRADING → GRADER-SECFIX-RECONCILE** (the ranking-brain last mile). The verified
   security fix is committed on `feat/bench-oob-grading` @ `e879957` (F1 sandbox-confine, F2 shell=False, F5
   false-green — all independently verified). `GRADER-SECFIX-RECONCILE` weaves it into the canonical grader;
   its acceptance marks the security fixes NON-NEGOTIABLE. Brief: /home/stack/charon-private/prompts/grader-secfix-reconcile.md
3. **PRIORITY 3 — METER Wave-2 caller-wiring** (`provider=route.label` into forwarder.py). Folded into
   METER-MODEL-PROVIDER `wave2-scope`; it is the shared unblock for COST-RANK-AUTO / DRAIN-THEN-PARK /
   METER-SESSION-TAG and makes the SLOP token-capture exact (see /home/stack/charon-private/fleet/SLOP-TOKEN-CAPTURE-RECIPE.md).

### Side task (low priority, not blocking)
- Run a **claude-code-guide** subagent to find the exact **Windows Terminal setting** that restores mouse-wheel
  scrolling / a visible scrollbar for Claude Code (a full-screen TUI that uses the alt-screen buffer, so WT
  disables scrollback → no scrollbar/wheel). Have it report the exact `settings.json` keys (e.g. `snapOnInput`,
  `altGridScrolling`, `scrollbarState`, alt-screen wheel→arrow forwarding), a paste-ready JSON snippet, and a
  "try this first" line. (Operator asked for this; deferred from session E to run here.)

## SHIPPED / DONE this session
- **Wave-1 ALL LANDED to master** (operator pushed; product HEAD `a541400`): meter (money-path), decompose,
  actuals — each fixed from its adversarial review, REVIEW-PACKET stripped, merge-commit gate GREEN, full
  1379-test suite passed.
- **Three recurring MANUAL-STEP failures mechanized** (committed on `feat/fragility-tickets`; charon-private
  HEAD `ba89271`; **3 unpushed**):
  - `board_gate` in /home/stack/charon-private/fleet/preflight.sh — runs validate_board every preflight,
    auto-registers/reopens `board-validator-red`, exits non-zero on any red (fixes "ignore pre-existing reds").
  - /home/stack/charon-private/fleet/retire-done.sh — auto-archives done tickets to board/archive/ at close
    (done.sh) + as a preflight safety-net; validate_board now treats done deps as valid. **Board 108 → 17.**
  - project-scoped drift check in /home/stack/v5/docs/tools/check_push_status.sh — a Charon session now sees
    ONLY charon + charon-private (SLOP sessions see only SLOP); future projects default to their own repo.
- **Grader security fix VERIFIED** on `feat/bench-oob-grading` @ `e879957` (branch pushed by operator).
- New tickets: `GRADER-SECFIX-RECONCILE`, `METER-SESSION-TAG`; METER-CALLER-WIRE + charon-run false-failover
  fix FOLDED into scheduled tickets (METER-MODEL-PROVIDER wave2-scope / WORK-ROUTING-TO-CHARON-ENGINE).

## PUSH (operator — push is DENIED to the manager)
- charon-private has **3 unpushed** commits: `git -C /home/stack/charon-private push`
- /home/stack/v5 (docs repo) has the `check_push_status.sh` edit uncommitted — commit + push it there.
- Product master: 0 unpushed (already up to date).

## GOTCHAS (avoid re-discovering / DENIED)
- `git push` / `git -C … push` are **DENIED** to the manager (settings deny-list; verbal "push authority" does
  NOT override it). The operator pushes.
- **charon-run.sh FALSE-FAILOVER:** its limit-detector greps the model's ENTIRE stdout, so any job whose output
  mentions "exhausted / rate limit / no capacity" false-fails-over onto `deepseek-v4-pro` (the operator's live
  SLOP model). Use SINGLE-model chains (`glm-5.2`) for review/fix jobs. Fix is scoped in WORK-ROUTING-TO-CHARON-ENGINE.
- **NEVER trust a job's SUCCESS line or a state marker** — verify `git log/diff` on its OWN branch. glm-5.2 was
  honest this session; deepseek-v4-flash fabricated twice previously. Log liars to
  /home/stack/charon-private/fleet/state/MODEL-SELF-REPORT-RELIABILITY.md
- Done tickets AUTO-retire to board/archive/ now — do NOT hand-remove board files; `done.sh` handles it.
- Gateway `http://10.0.1.60:8080`; admin API under /charon/config + /charon/cost; env `CHARON_GATEWAY_TOKEN`
  is STALE — get the live one via `docker exec <charon-container> printenv CHARON_GATEWAY_TOKEN` on the 4-LOM box.
- **Chutes + CommandCode $15/mo keys PENDING** (operator deferred 2026-07-10 until the meter shows a real
  capacity need — surface it then, back up /data before wiring).

## SESSION-BRIDGE
This session ran as **Qui-Gon Jinn** (`mgr-quigon`, repo:charon). Register under a NEW Jedi name; stale sessions
auto-purge after 600s.
