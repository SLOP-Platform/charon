# HANDOFF — 2026-07-09 (fresh Charon fleet MANAGER bootstrap)

## Bootstrap one-liner

```
Read and fully follow /home/stack/charon-private/fleet/HANDOFF-2026-07-09.md — you are the fresh Charon fleet MANAGER. Run preflight.sh + model-scorecard.sh --due, re-verify durable state (git/gh/ssh), register on the session-bridge (repo:charon) under a Jedi name, then continue the autonomous build per this handoff (adversarial review of substantial/money-path/security work; merge gate = full CI gate not pytest; all work in sub-sessions; move deliberately).
```

You are the COORDINATOR, not a worker. Gate / sequence / commit / push / operator-dialogue only; launch ALL substantive work in sub-sessions. Load `fleet/MANAGER-OPERATING-RULES.md` (SessionStart hook cats it) and `fleet/COORDINATOR-DOCTRINE-v2.md` before acting.

---

## SHIPPED this session

7 merges to product master (tip **73b1c08**, released as **v0.4.0** — verify deploy status), each ADVERSARIALLY reviewed; **6 real defects caught + fixed** (see `scratch/*-review.md`):

**Product (charon master):**
- **PFF-P1** — failover bleed-stopper: honor `Retry-After` + browser-UA handling.
- **PROXY-SERVER-DECOMPOSE** — `proxy_server` split into 4 modules (`forwarder` / `console_router` / `proxy_response` / `proxy_console_assets`) + a thin facade.
- **SR-6** — anthropic prompt-cache breakpoints.
- **SR-13** — console TOFU auth.

**RIG (charon-private) — the real-outcomes BENCHMARK PIVOT core:**
- **BENCH-REGROUND-LIVE** — grades computed on `source=live` only, fail-closed.
- **#20 BENCH-PROVISIONAL-SCORING** — stage gate for provisional scores.
- **#16 BENCH-AGGREGATE-N** — N-run aggregation + tie handling.

Reviews of record: `fleet/scratch/pff-p1-review.md`, `decompose-review.md`, `sr13-review.md`, `bench-reground-review.md`, `bench-provisional-review.md`, `bench-aggn-review.md`.

## DEPLOY status

Released as **v0.4.0**. Check the release/deploy first. If it is NOT live, the command (after CI builds the image) is:

```
bash fleet/deploy.sh v0.4.0
```

## NEXT (autonomous)

1. **METER-MODEL-PROVIDER** — per-model×provider cost sensor. This is the enabler: it unblocks **COST-RANK-AUTO** + **FREE-TIER-QUOTA-SPILL** (the sense→act cost loop). Build this first.
2. **Routing lane:** PFF-P2 (cross-model substitution, **default-off**) → DRAIN-ROUTING → COST-RANK-AUTO → POOLS-SIMPLIFICATION (data-dependent on METER + grades + `tiers.json`).
3. **Console lane:** RFL-2 (chat playground), RFL-4 (editable limits).
4. **CLINE-UNWRAP-SHIM** — parked behind decompose (latent).

## GATED ON OPERATOR

- **Q1 — bench-grader substrate** — unblocks #26 OOB-grading → #25 reds-replay.
- **Provider signups** — Featherless Premium + Cline wired; Featherless $100/256K tier pending benchmark cutover.
- **Model-per-tier testing — NO workhorse finalized for ANY tier** (memory `charon-no-workhorse-finalized`). Operator wants **2–4 quality-equivalent models per tier** (`POOLS-REDESIGN-ADR-v2.md`).

## KEY DOCS

- `fleet/COORDINATOR-DOCTRINE-v2.md`
- `fleet/MANAGER-OPERATING-RULES.md`
- `fleet/POOLS-REDESIGN-ADR-v2.md`
- `scratch/pivot-implementation-plan.md`
- `scratch/execution-optimization-plan.md`
- `scratch/proxy-decomposition-analysis.md`
- `scratch/best-stack-recommendation.md`

## GOTCHAS

- **PLAIN git only** — `cd <repo> && git ...`; the `git -C` form is deny-listed. Commit + push via `bash /home/stack/charon-private/fleet/land-push.sh master /home/stack/charon-private`.
- **Merge gate = FULL CI gate**, NOT pytest alone: `PYTHONPATH=src python3 -m charon.cli gate` (ruff + mypy + gate). A decompose merge passed pytest but was CI-red this session.
- **Product builds** happen in worktrees off master; **RIG builds** via `git worktree add` in charon-private. Prune stale worktrees (`git worktree list`).
- **State / done markers are gitignored / local** — don't expect them to survive on another host.
