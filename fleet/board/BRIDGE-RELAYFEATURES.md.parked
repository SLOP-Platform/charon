> RECONCILE before build: likely overlaps the RFL-* RelayFreeLLM port; trim to non-RFL
>   transformative gaps OR confirm it subsumes RFL-* (don't build both). 2026-07-08: the
>   RFL-1..RFL-5 tickets ARE the RelayFreeLLM feature port, and this ticket's header claims
>   "Incorporates RelayFreeLLM's superior features" — scope must be reconciled at un-park so
>   the same features are not built twice. Stays PARKED (also gated on BRIDGE-HARDEN).
tier: frontier
branch: feat/bridge-relay-features
depends_on: BRIDGE-HARDEN
owns: src/charon/proxy_server.py, src/charon/proxy.py, src/charon/gateway.py, src/charon/config.py
accept: PYTHONPATH=src python3 -m pytest -q ; ruff check ; mypy src tests
prompt: /home/stack/charon-private/prompts/bridge-relay-features.md
# PARKED — depends on BRIDGE-HARDEN (bridge server improvements must land first).
# Incorporates RelayFreeLLM's superior features + transformative gaps neither project covers.
