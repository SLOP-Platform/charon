retired: superseded-by GATEWAY-PROGRAM (synthetic ranker RETIRED per 3-reviewer adjudication 2026-07-10)
tier: strong
difficulty: 3  # auto-seeded from tier (D1 hybrid); refine when purpose is fresh
work_class: tests
branch: feat/bench-difficulty-cal
depends_on: BENCH-AGGREGATE-N
real-dep: BENCH-AGGREGATE-N — #17 needs #16's per-(model,section) mean/variance to compute
  discrimination and to build promotion-gate-v2 (the CI-aware test that replaces #20's v1 raw
  score-spread). Also shares the grades.py/tier_chart.py single-owner chain + benchmark/promote.py
  (created by #20) — rebase onto #16, never co-write.
owns: capability/grades.py, benchmark/lib/tier_chart.py, benchmark/promote.py
accept: cd /home/stack/charon-private/fleet && python3 capability/selftest.py  # a saturated section's composite weight -> ~0 on the current 5/7-saturated data; promotion gate v2 rejects a unit whose between-model spread is within the noise band
prompt: /home/stack/charon-private/prompts/bench-difficulty-cal.md
scope: #17 (P1 — statistical-validity companion to #16). Difficulty calibration: compute per-section
  discrimination from the observed score distribution (variance/spread across models); saturated
  sections (all models ~100 — currently 5/7) carry ~0 discrimination and must be down-weighted or
  retired from the composite, not equal-weighted. Upgrades #20's promotion gate v1 (raw score-spread)
  to a v2 CI-aware discrimination test (promote only if between-model variance exceeds the within-model
  noise band by a margin). Adds headroom by construction — reds-replay (#25) and hidden-diagnosis
  sections are the source of new difficulty; synthetic S0 stays only as the sanity/smoke gate. RIG-ONLY.
  D&S: tail of the grades.py/tier_chart.py chain (after #16). Authoritative:
  scratch/pivot-implementation-plan.md §5 (#17) + §1 (near-cycle break: #20 ships v1 gate; #16/#17 upgrade to v2).
note: PARKED — staged so no droid claims ahead of manager gating.
