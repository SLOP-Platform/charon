repo: charon-private
tier: strong
difficulty: 3
work_class: rig-meta
priority: 0
branch: fix/client-model-list-converge
depends_on:
owns: /home/stack/.config/opencode/opencode.json, fleet/sync-client-models.sh, fleet/tests/client-models.test.sh
serial_justified: |
  ONE list and the generator that replaces it. Hand-editing the list while building the generator
  produces two sources of truth for the same data — the exact defect being removed.
execution: |
  ASSIGNED TO A NON-ANTHROPIC MODEL via an `opencode` session (charon/* gateway model), NOT Claude.
  Graded sample: record into fleet/model-scorecard.tsv. One checkout, one agent.
source: |
  Operator decision 20(b), 2026-07-26. Found when `opencode --model charon/minimax-m3` silently
  opened on the DEFAULT model instead of the requested one.
note: |
  ## THE DEFECT — INV-SW1 violated on the CLIENT side
  `~/.config/opencode/opencode.json` hand-maintains a **36-model list** under its `charon` provider.
  `minimax-m3` is not in it (it has `minimax-m2.5`, `minimax-m2.7`, `minimax-m3-together`,
  `minimax-m3-free`), so `--model charon/minimax-m3` **silently fell back to the default**.

  ADR-0011 (Accepted), INV-SW1: *"no tool enumerates, ranks, or holds its own set of
  providers/models."* This list is exactly that, one layer out from the gateway. The gateway has a
  healthy 7-leg `minimax-m3` pool and serves **2585 models**; the client simply cannot NAME them.
  Same disease as the 175 hand-pinned legs in SW-STATIC-LEGS-RETIRE — a hand-typed list that rots.

  ## THE SECOND BUG — the default points at a dead pool
  `"model": "charon/gpt-5.4"`. The live `gpt-5.4` pool is `[nanogpt, openrouter]` — nanogpt is 429
  (monthly cap), openrouter is 402 (out of credit). **Both dead.** So the silent fallback lands on a
  model that cannot be served, and the failure surfaces as a confusing runtime error rather than
  "that model does not exist".

  These are independent bugs. A silent fallback is bad; a silent fallback to a DEAD default is worse.

  ## WHAT NOT TO DO
  Adding `minimax-m3` to the list is the "re-type the integers correctly" move ADR-0016 explicitly
  rejects — it works today and rots the next time the catalog changes. The list must be GENERATED
  from the gateway's `/v1/models`, not curated. If opencode requires a static list format, then the
  deliverable is a GENERATOR plus a drift check, not a hand-edit.
accept: |
  DONE-CONTRACT (observable, by EXECUTION):
  - `opencode --model charon/<any model the gateway serves>` opens on THAT model. Demonstrate with a
    model absent from today's 36 — `minimax-m3` is the known case. Show the before (falls back) and
    after (opens correctly).
  - **Silent fallback is eliminated**: requesting a model the gateway does NOT serve fails LOUDLY
    naming the model, instead of quietly opening something else. This matters more than the list
    itself — a wrong-model session that looks fine corrupts a graded run.
  - The list is regenerated by a command, not by hand. Show the command and its output.
  - A DRIFT CHECK: something detects when the client list and the gateway catalog disagree. A
    generator nobody re-runs rots exactly like the hand list did.
  - The default model is no longer a dead pool. State what you set it to and show its pool has at
    least one live provider (deepseek, nvidia, deepinfra, together and zai all served 200s on
    2026-07-26; nanogpt 429 and openrouter 402 did not).
  - RED-PROOF BY EXECUTION: revert the generator -> the drift check goes RED naming the missing
    model. Report BOTH exit codes.
  - NON-VACUOUS: a drift check that passes against an empty catalog is RED.
  - **Back up `opencode.json` before writing it** — it is live config for every opencode session on
    this box, including sessions currently running. A malformed write breaks the fleet's ability to
    start any session at all.

## Dependencies & sequence

- **Depends on: NOTHING technically. Startable immediately.**
- **Related (read first, do not duplicate):** SW-STATIC-LEGS-RETIRE removes the same class of
  hand-maintained list inside the GATEWAY. This ticket is its client-side twin. If that ticket
  produces a reusable discovery/verification pattern, reuse it rather than inventing a second one.
- **Concurrency safety:** owns client config + two new rig files. No product-repo files. Cannot
  collide with the Switchboard wave.
- **Wave:** parallel lane, P0.
