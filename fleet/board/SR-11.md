tier: economy
branch: feat/sr-11-dependabot-actions
depends_on:
owns: .github/dependabot.yml
prompt: /home/stack/charon-private/prompts/sr-11.md
scope: CI hygiene — mechanize GitHub Actions SHA-pin version bumps via Dependabot. Own wave SR-W5;
  infra-only, disjoint from every SR code ticket and every workflow ticket (owns a SINGLE NEW FILE
  `.github/dependabot.yml`, touches no `src/charon/*`, no `.github/workflows/*`). Adds
  `package-ecosystem: github-actions` ONLY (weekly, grouped, PR-only / no auto-merge) so the six
  SHA-pinned actions + their trailing `# vX` comments stop going stale — clears the Node-20
  deprecation warnings WITHOUT weakening the SHA-pin supply-chain posture (Dependabot bumps SHA→new
  SHA, never SHA→floating tag, and rewrites the comment). DELIBERATELY excludes the `docker` and
  `pip` ecosystems (§ prompt). depends_on EMPTY — board-unblocked, lands anytime. The optional
  actor-aware `runs-on` hardening (route dependabot PRs to ubuntu-latest) is a SEPARATE follow-on
  that owns `.github/workflows/*.yml` — noted, but NOT in this ticket's owns.
