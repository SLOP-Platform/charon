tier: strong
work_class: greenfield-feature
branch: feat/bench-provisional-scoring
depends_on: BENCH-REGROUND-LIVE
real-dep: BENCH-REGROUND-LIVE — shared single-owner of capability/grades.py + benchmark/lib/tier_chart.py.
  #20 adds the provisional/active stage column + reader filters ON TOP OF A2's re-grounded source;
  it must rebase onto A2's version of both files, never co-write them.
owns: capability/grades.py, benchmark/lib/tier_chart.py, benchmark/lib/grade_state.py, benchmark/bench.sh, model-scorecard.sh, benchmark/promote.py, benchmark/units.tsv
accept: cd /home/stack/charon-private/fleet && python3 capability/selftest.py  # a provisional unit changes NO active grade/tier/assign pick; a saturated unit provably cannot promote; legacy 13/15-col rows default to active
prompt: /home/stack/charon-private/prompts/bench-provisional-scoring.md
scope: #20 (P0 ENABLER — BUILD FIRST after A2). Provisional-vs-active scoring: new
  tests/sections/replayed-reds collect data WITHOUT touching live grades until proven to
  discriminate. State machine per unit (section|red): provisional -> (promotion gate) -> active ->
  retired. Add a 16th trailing `stage` column to model-scorecard.tsv (backward-compat, defaults to
  active — mirrors the tokens_in/out trailing-col pattern); a unit registry benchmark/units.tsv
  (unit_id,kind,stage,promoted_on); pass stage through bench.sh -> model-scorecard.sh append via a
  CHARON_SCORECARD_STAGE env var; make grades.py/tier_chart.py/model-scorecard.sh render FILTER to
  stage==active by default; add promotion gate v1 (benchmark/promote.py: score_spread>=SPREAD_MIN
  AND distinct_models>=K). RIG-ONLY. Authoritative: scratch/pivot-implementation-plan.md §2 (+ §8 Q4/Q5).
  D&S: enabler that unblocks #25/#16/#17. Shares bench.sh/grade_state.py/model-scorecard.sh with #26
  (BENCH-OOB-GRADING) -> #26 is file-sequenced AFTER this ticket even though logically parallel per
  pivot §1. owns marked from pivot §2; re-confirm at activation.
note: PARKED — staged so no droid claims ahead of manager gating.
