repo: charon-private
tier: economy
priority: 2
difficulty: 3
work_class: ci-infra
branch: feat/shellcheck-ratchet
owns: fleet/checks/shellcheck-ratchet.sh, fleet/checks/shellcheck-baseline.tsv, fleet/tests/shellcheck-ratchet.test.sh
depends_on:
dep-kind:
serial_justified: The owned surfaces are a mechanism, its baseline DATA file, and its red-proof —
  one indivisible unit, not parallel workstreams. The baseline is generated BY the mechanism and is
  meaningless without it, and the test red-proofs that exact pairing. Splitting them would land a
  ratchet with no floor (rejects every PR) or a floor with no ratchet (enforces nothing).
work_class_note: ci-infra — a debt ratchet over the rig's own shell surface. It is D-004 work
  (turn on capability we already own) and it carries two LIVE runtime bug fixes, so it is not
  lint hygiene.
note: |
  MINTED RETROACTIVELY 2026-08-04. The branch and commit (61c5316) already existed; work-lease
  refused it for having no ticket carrying a matching `branch:` field, and the agent used
  WORK_LEASE_BYPASS. This ticket removes that bypass. Ordering rule for next time: MINT THE
  TICKET BEFORE CREATING THE BRANCH.

  D-004 CONTEXT: TOOL-UTILIZATION-AUDIT.md (2026-08-01) measured shellcheck at 0 of 11 optional
  checks enabled, i.e. `-o all` was off everywhere in this rig. Bare enablement is not an option:
  the tree carries ~36,700 findings at full coverage and would auto-reject every PR. Hence a
  BASELINE RATCHET.

  ⛔ TWO LIVE RUNTIME BUGS — MANAGER-VERIFIED 2026-08-04 BY EXECUTION, not by trusting shellcheck
  and not by trusting the authoring agent's self-report:
    1. fleet/retire-done.sh:70 — `local src=... dst=...` sits in a top-level `for` loop, NOT in a
       function (`retire_safe()` opens at line 28 and CLOSES at line 33). bash refuses it with
       "local: can only be used in a function", the assignment never happens, and because the
       script runs `set -uo pipefail` (line 16) the next `[ -f "$dst" ]` aborts the ENTIRE
       retirement sweep with "dst: unbound variable". Reproduced standalone. That function has
       never worked, which is consistent with tickets piling up on the active board.
    2. fleet/handoff.sh:173,260,277,301,315 — `echo "```...```"` places backticks inside a
       DOUBLE-quoted string, so bash treats them as command substitution. Reproduced:
       `bash: command substitution: syntax error near unexpected token 'git'`, and the fallback
       text is swallowed entirely (empty output). This is in the HANDOFF GENERATOR itself — the
       one script whose output the next session is supposed to trust.

  THE MECHANISM: fleet/checks/shellcheck-ratchet.sh runs shellcheck `-o all` whole-tree. The
  git-tracked fleet/checks/shellcheck-baseline.tsv is the accepted FLOOR. Only a (file, SC-code)
  pair whose count EXCEEDS its baselined count REDs, so pre-existing debt never blocks a PR while
  a NEW violation always fails. Wired at two points: a direct `rig-ci-scope.sh shellcheck` step in
  .github/workflows/rig-ci.yml, and fleet/tests/shellcheck-ratchet.test.sh in CI_SUITES.

  KNOWN LIMIT, recorded honestly: a per-(file,code) COUNT baseline does not catch a new violation
  that REPLACES an existing one of the same code in the same file. It is a ratchet, not a proof.

  ⚠ SEQUENCING: this edits fleet/checks/rig-ci-scope.sh, which is `owns:`-claimed by the live
  ticket HANDOFF-GATE-NONBYPASSABLE, and feat/status-board-v1 adds a CI_SUITES entry to the SAME
  file. Both changes are additive (one case arm + one allowlist entry each). LAND ONE AT A TIME
  and re-run the second's test after the first lands.

  ACCEPTANCE: (a) both runtime bugs fixed and each proven by executing the real script path, not
  by shellcheck agreeing; (b) the ratchet REDs on a freshly introduced finding and returns clean
  when it is removed; (c) a MISSING baseline fails CLOSED (rc 2), never a silent pass.
