tier: strong
branch: feat/sr-10-deploy-hygiene
depends_on:
owns: docker-compose.yml, .github/workflows/release.yml, Dockerfile
prompt: /home/stack/charon-private/prompts/sr-10.md
scope: Deploy hygiene — enforce a HARD single-producer rule so the deploy can never drift. Own wave
  SR-W4; infra-only, disjoint from every SR code ticket (owns compose/release/Dockerfile, never
  proxy_server.py). NON-NEGOTIABLE: deploy docker-compose.yml contains EXACTLY ONE producer — never both
  build: and image:. The only DECISION-GATED choice is WHICH single source (RECOMMENDED image-only/CI
  sole producer vs ALTERNATIVE build-only). depends_on EMPTY — board-unblocked. NOTE: surfacing
  CHARON_BUILD_SHA in /charon/status is a ~2-line RIDER folded into SR-2 (proxy_server.py owner), NOT here.
  Also registers the image-only/single-producer deploy row in docs/DECISIONS.md (Owner OP, Settled) — an
  append-only shared-register doc deliverable, NOT added to owns.
