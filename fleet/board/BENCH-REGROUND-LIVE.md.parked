tier: strong
difficulty: 3  # auto-seeded from tier (D1 hybrid); refine when purpose is fresh
work_class: refactor
branch: feat/bench-reground-live
depends_on:
owns: capability/grades.py, benchmark/lib/tier_chart.py
accept: cd /home/stack/charon-private/fleet && python3 capability/selftest.py && bash model-scorecard.sh render  # human sign-off: tier/grade now prefers source=live; synthetic S0-S6 composite demoted to smoke, no longer feeds the grade
prompt: /home/stack/charon-private/prompts/bench-reground-live.md
scope: "A2" from the real-outcomes pivot (CONFIRMED 2026-07-08) — the immediate, near-zero-build
  first move. Re-ground the ranking/capability-grades brain in REAL OUTCOMES: make
  ScorecardGradesProvider PREFER source=live actuals rows (already out-of-band-valid by
  construction) and DEMOTE the synthetic S0-S6 composite to a smoke-only signal that no longer
  feeds the grade or the tier position. Mostly a weighting/source-filter change in
  capability/grades.py + benchmark/lib/tier_chart.py (the module docstring already names this as
  the documented swap point). Delivers the pivot thesis on existing data, independently reversible.
  RIG-ONLY (fleet repo) — never ships in the Charon product.
  D&S: depends_on EMPTY (ship first). Head of the grades.py + tier_chart.py single-owner chain
  A2 -> #20(BENCH-PROVISIONAL-SCORING) -> #16(BENCH-AGGREGATE-N) -> #17(BENCH-DIFFICULTY-CAL);
  every downstream ticket rebases onto this file, never co-writes. Authoritative source:
  scratch/pivot-implementation-plan.md §0/§1/§7 (A2) + §8 Q7.
note: PARKED — staged so no droid claims ahead of manager gating. NOT a numbered backlog item;
  derived from the pivot plan as the day-one enabler and the dep-root for #27. Flag for manager:
  confirm A2 should be filed/activated as its own ticket (recommended by pivot-implementation-plan.md §1).
