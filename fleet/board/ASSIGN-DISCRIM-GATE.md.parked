tier: strong
work_class: refactor
branch: feat/assign-discrim-gate
depends_on: BENCH-REGROUND-LIVE, BENCH-AGGREGATE-N
real-dep: BENCH-REGROUND-LIVE — #27 guards the grades source that A2 re-grounds on real outcomes
  (pivot §6: "depends on A2"). BENCH-AGGREGATE-N — the within-CI-band no-differentiation test the
  guard fires on is defined "post-#16" (needs #16's CI band on the Grade object). Disjoint owns from
  both (assign.py/selftest.py vs grades.py/tier_chart.py) — JUSTIFIED as a real build/correctness
  prereq (the guard reads fields those tickets add), NOT merge-order.
owns: capability/assign.py, capability/selftest.py
accept: cd /home/stack/charon-private/fleet && python3 capability/selftest.py  # on a frozen saturated fixture assign() reports "no differentiation, fell back" instead of ranking on noise; on a frozen discriminating fixture it still makes the capability pick; rationale states which regime fired
prompt: /home/stack/charon-private/prompts/assign-discrim-gate.md
scope: #27 (P1). Gate the assignment consumer (#14/#15 ticket->best-agent, built ba0f9d5) on
  discrimination — close the same inert-composite carve-out the validity review flagged. Promote
  selftest.py's proof-of-effect from test-time to a RUNTIME guard in assign(): before trusting the
  capability pick, check whether the top candidates' grades actually SEPARATE for this work_class — if
  all eligible candidates are within each other's CI band (post-#16) OR all LOW_CONFIDENCE / all
  generalist-fallback, the capability signal is inert -> FALL BACK to cost/availability-only pick WITH
  explicit disclosure in the rationale (recommended over refusal; mirrors routing's always-valid
  cost/health terminal state). RIG-ONLY. Guardrail against the exact "inert-but-shipped" failure.
  D&S: PENDING operator decision Q6 (fall-back-with-disclosure vs refuse — recommend fall back).
  Authoritative: scratch/pivot-implementation-plan.md §6 + §8 Q6.
note: PARKED — staged so no droid claims ahead of manager gating.
