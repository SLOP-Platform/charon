tier: strong
rebase-after: PROXY-FAILOVER-FIX (parked 2026-07-08 to let the Phase-1 failover fix land solo on proxy_server.py; un-park + rebase on top after it merges)
work_class: routing
branch: feat/rfl-3-image-routing
depends_on: RFL-1
real-dep: RFL-1 build (single-owner file src/charon/proxy_server.py) — proxy_server.py is the
  single-writer bottleneck; RFL-1 is the current tail of the chain once it lands (after SR-13). RFL-3
  edits the SAME failover-loop candidate build in proxy_server.py `_handle`, so it rebases onto RFL-1's
  file, never a concurrent second writer. Shared-file sequencing, JUSTIFIED (not merge-order). Depending
  on RFL-1 also transitively orders RFL-3 after the whole SR proxy_server.py chain (via RFL-1 -> SR-13).
owns: src/charon/proxy_server.py, tests/test_image_routing.py
prompt: /home/stack/charon-private/prompts/rfl-3.md
scope: RelayFreeLLM comparison R3 — finish the image-aware routing path. Charon ALREADY detects
  `has_images` (request_inspector.py) and carries `vision` per-model metadata (_META_KEYS, surfaced at
  proxy_server.py ~:556); only the routing EXCLUSION is missing. In the failover-loop candidate/exclude
  step in proxy_server.py `_handle`, when the request contains images, drop chain entries whose model
  meta is not `vision:true` so an image request is never routed to a text-only model (which 400s/fails).
  Symmetric hook left open for audio. Small, high-correctness win. Suggested agent: DeepSeek V4-Pro
  (strong tier) — WHY: small, mechanical filter over data that already exists (has_images + vision
  meta); a one-branch exclusion with a hermetic test, no design ambiguity — no reason to spend Claude.
  Stdlib-only guardrail; CI gate is the backstop.
