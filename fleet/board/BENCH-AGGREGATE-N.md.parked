retired: superseded-by GATEWAY-PROGRAM (synthetic ranker RETIRED per 3-reviewer adjudication 2026-07-10)
tier: strong
difficulty: 3  # auto-seeded from tier (D1 hybrid); refine when purpose is fresh
work_class: tests
branch: feat/bench-aggregate-n
depends_on: BENCH-PROVISIONAL-SCORING
real-dep: BENCH-PROVISIONAL-SCORING — shared single-owner of capability/grades.py +
  benchmark/lib/tier_chart.py. #16 changes aggregation from latest/only-row to mean±variance and
  must build on #20's stage-filtered readers; rebase onto #20, never co-write.
owns: capability/grades.py, benchmark/lib/tier_chart.py
accept: cd /home/stack/charon-private/fleet && python3 capability/selftest.py  # tier chart shows N and a variance/CI per (model,section); a sub-band gap renders as a TIE not a rank; re-run assign frozen fixture to confirm no unintended pick changes
prompt: /home/stack/charon-private/prompts/bench-aggregate-n.md
scope: #16 (P1 — proven needed by the recorded glm-5.2 S3 100->75 / S5 100->60 across-run variance).
  Aggregate multiple runs per (model, section/red): switch grades.py + tier_chart.py aggregation from
  "latest/only" to mean ± variance over >=3 repeat runs and publish the noise band; a tier gap smaller
  than the band is a TIE, not a rank. grades.py already has Wilson bounds for merge/block proportions —
  add mean/stddev (or small-sample CI) for the bench-score aggregate and expose the band on the Grade
  object. RIG-ONLY. Feeds #17 (difficulty) and #27 (the assign guard uses this CI band).
  D&S: on the grades.py/tier_chart.py chain after #20, before #17. Authoritative:
  scratch/pivot-implementation-plan.md §5 (#16) + §8 Q5.
note: PARKED — staged so no droid claims ahead of manager gating.
