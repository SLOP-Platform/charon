tier: strong
branch: feat/sr-13-gui-session-login
depends_on: SR-2, SR-5b, DRAIN-ROUTING
real-dep: SR-2 build (single-owner file src/charon/proxy_server.py) — SR-13 refactors the single auth
  gate in proxy_server.py:_handle into surface-aware dispatch; it must rebase onto the SR proxy_server.py
  chain (SR-2 -> SR-6 -> SR-7 -> SR-8 -> TIER-SELECT), never write the file concurrently.
real-dep: SR-5b build (single-owner file src/charon/proxy_server.py) — SR-5b is the current LAST
  proxy_server.py owner; SR-13 lands AFTER it so it rebases onto the final file. Depending on SR-5b also
  transitively orders SR-13 after TIER-SELECT (the live cli.py owner) via SR-5b -> TIER-SELECT, so
  SR-13's cli.py edits (charon login/logout) never collide either. Shared-file sequencing, JUSTIFIED.
real-dep: DRAIN-ROUTING build (single-owner file src/charon/proxy_server.py) — DRAIN-ROUTING also
  sits right after SR-5b in the chain (same depends_on: SR-5b) with no ordering vs SR-13/RFL-1..4;
  sequencing SR-13 after it (rather than pushing DRAIN-ROUTING to the tail) keeps DRAIN-ROUTING in
  its natural post-SR-5b slot. Shared-file sequencing, JUSTIFIED (not merge-order-only).
  (board-validator-red audit 2026-07-05 — resolves the unordered proxy_server.py collision.)
owns: src/charon/proxy_server.py, src/charon/cli.py
prompt: /home/stack/charon-private/prompts/sr-13.md
scope: Friendly session login for the /charon/* web console (from AUTH-GUI-DESIGN.md, Option C — "TOFU
  session, the token IS the credential"). Add a /charon/login page (paste token into a form once, not
  the URL) + a `charon login` CLI that prints a click-once pre-authed URL + a 30-day HMAC-signed
  HttpOnly `charon_sess` cookie that authorizes /charon/* ONLY. /v1/* API auth stays byte-for-byte
  unchanged (Bearer token) so opencode/ACP/LAN clients don't break. New CHARON_SESSION_KEY secret
  (auto-generated, decoupled from the gateway token). Stdlib-only. OWNS-OVERLAP FLAG: proxy_server.py is
  shared with SR-2/SR-5b (and the SR-6/7/8/TIER-SELECT chain) — SR-13 sequences AFTER all of them via
  the depends_on above. NOTE: the AUTH-GUI-DESIGN full owns also touched secrets.py + gateway.py; this
  ticket is SCOPED to proxy_server.py + cli.py per the board owns — the CHARON_SESSION_KEY generation +
  get_or_create helper + banner change must be COORDINATED with the secrets.py/gateway.py owners (and
  the pending token->secrets.json rotation) as a follow-on, not written here. Sequence AFTER the current
  SR wave.
