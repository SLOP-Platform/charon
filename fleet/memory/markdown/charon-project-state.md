---
description: Charon state 2026-07-11 (session end) — ROUTER critical path COMPLETE on master; lint+mypy gate-debt cleared+pushed (@5f6f580); v0.5.0 release FAILED→dead tag, DEPLOY HELD by operator; METER is #1 remediation (inert half-measure, see [[charon-meter-inert]]); KSF MVP built (keystone repo, KS3/KS4 merged, KS4 false-pos-fix in flight, KS6 private-repo push pending gate-green). Prior: v0.4.1 deployed to self-hosted-runner.
metadata: 
name: charon-project-state
node_type: memory
originSessionId: a8f924d0-22f6-4f2b-ac52-79591b72effd
type: project
tags: [charon, project]
last_referenced: 2026-07-13
---
**2026-07-11 (session end — mace-windu) — gate-debt cleared, DEPLOY HELD, METER=#1.** `origin/master` @ **`5f6f580`**. The **v0.5.0 release FAILED** at the gate's lint step (tonight's R3/R8/R17 waves had unused imports + over-long lines + 3 mypy errors — merges were verified pytest-only, not the FULL gate: the [[merge-gate-is-full-ci-not-pytest]] rule, violated). Fixed on master (ruff no-suppression + mypy: `pc`→`pc2` shadow, `capability_matrix: Any` declared on GatewayProxyServer, latency-test URL from literal 127.0.0.1 bind), full `charon gate` exit 0, committed+pushed. **v0.5.0 tag is now a DEAD tag (no image published)** — next release re-tags **v0.5.1** AFTER the meter work. **Operator decisions this session:** (1) deploy to `.60` = **HELD**; (2) **METER = #1 remediation** — the per-provider cost meter is INERT (BalanceTracker never constructed from config; `record_spend` guarded off; ledger empty by the code's own comment) — the R4 "meter-wire" was a half-measure; full detail + fix in [[charon-meter-inert]]; (3) KSF lives in its **own private GitHub repo `SLOP-Platform/keystone`** (KS6) — but the push is gated on `ksf gate` being green on KSF itself, which needs the KS4 inert-code **false-positive fix** (still running as a detached NW job, no report yet). **KSF MVP** built at `~/code/keystone` (KS3 Graphify + KS4 inert-gate merged @ `1a467ec`). NW note: base 6kWh spent, into the **$22 PAYG (~$19 and falling)** — be sparing.

**2026-07-11 (overnight autonomous run) — ROUTER cost/capability routing brain BUILT.** Product `origin/master` @ `8f48982`. 11 tickets landed overnight via detached NW `opencode run --model charon/kimi-k2.6-nw` jobs (build → adversarial-review → self/NW gate → merge+push, autonomous lever ON), each suite-verified (only 2 pre-existing env failures: test_boundary transitive-import + test_routing_proxy_cli_reports_port, both pass with PYTHONPATH=src). **ROUTER critical path complete:** R4 meter-wire (per-(model,provider) meter fed under real traffic, record_spend only on committed 200) → R5 cost-rank-auto (live-metered cheapest-first + cost_class priority, configured fallback) → R3 capability-matrix (deny-table openrouter/novita✗reasoning) + R3-wire (forwarder eligibility, never-strands) → R2 router-core (per-request live cheapest-first via `order_pool_by_live_cost`) → R7 capability-engine (max_context/max_concurrency eligibility + balanced `_inflight` counter). Also R8 latency-signal (EWMA, cooldown-primary; **review caught+fixed a real cooled-bucket double-sort bug**) + R17 pricing-limits-checker (isolated drift checker) + fleet F17/F18/F44/F39. **2 OPERATOR DECISIONS PENDING:** (1) cost-vs-latency ordering — R8's `order_by_cooldown` fresh-bucket latency sort currently overrides R2 cost order once latency data exists; design says cheapest-first primary; plan recommends option (A) cost-primary/latency-tiebreak (delete `proxy_server.py:645 fresh.sort`); (2) the 2 pre-existing test fails. Remaining ROUTER R10/R11/R16 BLOCKED (R11 needs R12 drain-routing + a provider balance source; R10 needs limits-config wiring; R16 needs R10+R11) — all touch forwarder.py/gateway.py = sequential. Full handoff: `fleet/state/overnight/MORNING-BRIEF.md` + `ROUTER-NEXT-PLAN.md` + `OVERNIGHT-RUN.md`. Detached-job survival rule: [[background-jobs-detach-to-survive-quit]].

**2026-07-10 (session B) — PR backlog cleared via the headless-Charon review loop ([[charon-headless-review-loop]]).** master `6096fb3`; source v0.4.1 (v0.4.1 is what's deployed on self-hosted-runner). Merged this session: #92 TEST-HARDEN (A-only; Defect B → new WIRE-MOCKLINT-ENFORCE ticket), #90 CI-WORKFLOW-POLICY-GATE, #91 RESPONSE-ADAPTER-UNIVERSAL (money-path, adversarially reviewed on Charon). #93 ACTION-PIN-POLICY closed as a regression (master already 19/19 SHA-pinned; ticket done → DOCKER-SMOKE-CLEANUP unblocked). Cross-PR semantic merge conflict caught by full-CI-on-merge ([[merge-gate-is-full-ci-not-pytest]]). NEXT (queued, operator-gated): bump→v0.4.2, deploy to self-hosted-runner, reset prod `/data/spend.json` (kill $223 phantom), flip routing to credit-drain-first. Full handoff: `fleet/HANDOFF-2026-07-10-b.md`. Older session log below (point-in-time, may be stale).

**2026-07-03 — SR reliability & billing wave shipped to the integration branch.**

**Merged into `feat/prod-install` (@ `6afd0dd`) + pushed; draft PR #82 → master is CI-GREEN (gate 1m48s, wheel-smoke pass, 1048 tests). Merge-to-master + release + deploy are OPERATOR-GATED (not done).**
- **SR-1** — silent-downgrade double-bill fix ([[charon-silent-downgrade-leak]]): `_normalize_model_id` compares the FINAL path segment (`rsplit("/",1)[-1]`), so a provider echoing `accounts/.../deepseek-v4-pro` for a request of `deepseek-v4-pro` is no longer misclassified+rebilled.
- **SR-2** — money-path: serve a genuine downgrade ONCE with `X-Charon-Downgrade` (never discard-and-rebill a 200); never cache a downgrade or a truncated stream; opt-in `failover_on_downgrade` toggle (default off) restores failover with honest `count_usage=True`. DTC found+fixed 2 blockers, then APPROVE-FOR-MERGE.
- **SR-3** cache hit/miss stats (exact-match keying kept). **SR-5** provider pricing CAPTURE (per-token units, NaN/negative guard, operator-price clobber-protect, config_dir honored) — but computing `cost_usd` from it is the follow-on **SR-5b** (still 0 until then → cost-ranking still availability-only). **SR-10** deploy hygiene (single-producer image-only compose, `CHARON_HOME`/`CHARON_STATE_DIR=/data`, build-SHA at `/charon/status`). **SR-11** dependabot (github-actions, PR-only). DECISIONS: deploy=D024, failover-billing=D025.

**self-hosted-runner gateway (<COORDINATOR_HOST>) live-fixed this session** (via `/charon/models`+`/charon/pools` hot-reload, backed up each time): leak closed; all provider keys in `/data/secrets.json` ONLY (added deepseek-direct, nanogpt, neuralwatt, valid openrouter inference key); cross-account FALLBACK pools wired across 47 pools (opencode-zen/go primary → deepseek/nanogpt/neuralwatt/openrouter). `gpt-5.5`/`gpt-5.4-pro` reordered openrouter-FIRST (opencode returns terminal 400 "not supported" for them). **2026-07-05 UPDATE-4 — session-3 wrap:** `origin/master` = **`377d4a3`**. Landed: **v0.3.2 + v0.3.3 released & DEPLOYED to self-hosted-runner** (self-hosted-runner now runs **v0.3.3**, healthy, routing preserved). **Failover-on-unsupported-400** shipped (cross-provider/tier fallback; follow-up ticket #20 = match WRAPPED provider errors in `error.metadata.raw`, OpenRouter). **Version-gate #14 DONE + hardened + tested** (CI-aware + source-literal scan). Model×work matrix → `fleet/MODEL-WORK-MATRIX.md`. quality_scorer already wired (0.5 floor). **3 droids launched** (#6 auto-cost_rank/cost_class → gateway.py; #5 strip reasoning_content → proxy_server.py; #17 balance module → new balance.py) — disjoint, own worktrees+bridge claim, Claude-review before merge. STANDING RULE added: name the best model per droid brief ([[recommend-model-for-droid-work]]). Operator cost+quality routing principle in [[charon-free-tier-routing]]. **NEXT SESSION FIRST TASK (operator): investigate the very-high Claude Code token/context burn** before working tickets (see handoff bootstrap). Handoff: `fleet/HANDOFF-2026-07-04-v2.md`.

**2026-07-04 UPDATE-3 — session-2 wrap:** `origin/master` now **`d16c34c`**, CI green, 1140 tests. Landed this session: SR-7/SR-5b/TIER-SELECT-A, then tool-repair(`tool_repair.py`)+RFL-1(`quota.py`)+SR-12(regression test, recovered from a `branch -D` data-loss), then a 4-layer **windows-exe fix** (`charon.exe` had NEVER built — bad action SHA, pwsh-footgun smoke test→bash, bare-module frozen entry→`packaging/charon_entry.py` wrapper, cp1252 non-ASCII crash→UTF-8 reconfigure in `cli.py:main()`; verified green). Handoff for the fresh session: **`fleet/HANDOFF-2026-07-04-v2.md`** (13 tickets incl. de-tangle/worktree-trim, tool-repair allow_mutating gate, proxy wiring for quota+tool_repair, reasoning_content request-normalizer, auto-derive cost_rank, primitive→product via obol, mandatory Fleet-Droid-process isolation, mechanize CI pin-safety, fragility-triage sweep). **RELEASED + DEPLOYED: v0.3.2 cut (tag v0.3.2 @ `00988d1`, release green — gate+image-smoke+publish, GHCR `v0.3.2` live) and DEPLOYED to self-hosted-runner** (container on v0.3.2, healthy, build_sha `00988d1` source-matched; VERIFIED post-swap: 50 pools, 9 keys, deepseek-v4-pro→`X-Charon-Provider=nanogpt` routing + tool-calling all preserved on the `/data` volume; backup `~/charon-data-backup-v032-afee7660.tgz`, rollback target v0.3.1). NOTE: version gate false-positives on every bump locally (compares pyproject to stale editable-install metadata) — CI is fine; deploy is still a hand-run recipe (both ticketed #14/#15 to mechanize). Lesson: 3 droids sharing ONE worktree tangled branches + one `branch -D` deleted another's commit → Fleet Droid worktree+bridge isolation is MANDATORY for all sessions.

**2026-07-04 UPDATE-2 — SR-7 · SR-5b · TIER-SELECT-A MERGED to master** (`origin/master` now `8d960c9`, was `0e54265`). Manager reviewed + gated + merged all three in an isolated worktree (never touched the shared session tree): merged master passes `charon gate` + **1090 pytest** (was 1067). SR-5b (money-path, built by a Claude sub-session, commit `ef0eb7d`): NameError fixed, `model_pricing` wired end-to-end through gateway→proxy_server→observer with hot-reload `set_pricing`, per-token cost compute w/ namespaced-id resolution, `cost>0` record guard NOT reinstated. Follow-up: auto-derive `cost_rank` from the now-real pricing (would automate the manual draw-down). SR-7=spend-cap hardening (record est cost for zero-cost served). TIER-SELECT-A=model_catalog.py + CLI picker. NOT yet released/deployed (still v0.3.1 on self-hosted-runner) — these ride the next release. Session-bridge MCP now wired into the manager's Claude config (local scope `~/.claude.json`, NOT committed — daemon PID at `~/.charon/bridge.sock`); needs a `/mcp` reconnect to expose `mcp__session-bridge__*` tools (board/register/update/nudge; pass repo:"charon"). Stale merged worktrees remain as cleanup candidates: charon-sr-5b, charon-sr-7, charon-fleet-SR-3/4/5/11.

**2026-07-04 UPDATE — v0.3.1 DEPLOYED to self-hosted-runner.** SR wave merged to master (PR #82/#84/#85), v0.3.1 published on GHCR (release run 28690333579 green), and the live container swapped v0.2.1→v0.3.1 via `~/charon/docker-compose.yml` (compose bak3 saved; `/data` backed up to `~/charon-data-backup-20260704`+.tgz). Post-swap VERIFIED: healthy, `/v1/models` 200 (194 models — down 1 from v0.2.1's 195, consistent with SR-1 namespaced-id dedup, i.e. more-correct), pools=50 preserved, `CHARON_HOME=/data`, external volume `charon_charon-config` untouched, build-SHA `0e54265`=master HEAD, silent-downgrade fix present in source (`proxy.py:186` rsplit-last-segment, SR-1). Runtime reorders (gpt-5.5/gpt-5.4-pro openrouter-first) live on the volume's pools.json → preserved. Image-level band-aids retired ([[charon-deploy-drift-lessons]]).

**Queued / not built:** SR-5b (cost_usd wiring in proxy.py — unlocks real cost-ranking), SR-13 (simple GUI session-login, design in `fleet/AUTH-GUI-DESIGN.md`: "token IS the credential" + 30-day cookie, /v1 stays token-auth), SR-6/7/8/TIER-SELECT (later wave, SR-6/8 design/decision-gated). Token rotation DEFERRED (would log out the operator's opencode client mid-session). Both DeepSeek droid sessions (A=SR-5/4, Plo Koon=SR-3/11) are done.

**Recurring papercut (fix the briefs):** local gate `mypy src/charon` (64 files) ≠ CI `python3 -m charon.cli gate` (mypy over 148 files incl tests). Caused a CI red here (test `.calls` attr) AND previously (#72). SESSION-*/WORKFLOW briefs should prescribe `python3 -m charon.cli gate` as the pre-commit gate.

Relates to [[charon-production-readiness-mindset]], [[charon-vision-gateway-first]], [[charon-modular-agent-and-provider-agnostic]], [[optimize-execution-wallclock-tokens]].
