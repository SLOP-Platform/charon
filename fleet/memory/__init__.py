"""memory — manager session memory store (basic-memory-adopt, MCP-native).

Markdown = source of truth. Local FastEmbed semantic + full-text indexed.
PINNED core loaded at session start; pull-on-demand via memory.search MCP tool.
"""
