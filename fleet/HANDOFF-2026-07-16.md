# HANDOFF — 2026-07-16 crash-recovery session

Recovery from a Windows crash that killed a droid wave mid-flight. **No work was lost**, but the
recovery surfaced live defects that matter more than the recovery itself. Full evidence for every
claim below is in this directory (6 reports, 1473 lines) — they were written to /tmp and would have
died with the session; they are persisted here on purpose.

---

## 0. LANDED LATE IN SESSION (supersedes parts of §1 below)

- ✅ **never-Anthropic is now GATEWAY-WIDE** — product **PR #173 merged** (`643ea1c`). It was NOT a
  stale comment: `recommend.py:51-88` `_find_trusted_models` selected every configured model with a
  live key and applied **no vendor filter**; `:357-360` fed `trusted[:3]` straight to
  `_post_tier_ranking` → a real POST to `api.anthropic.com`. Aggravated by `config/tiers.py:17-18`
  seeding opus/sonnet/haiku into the tier default and `recommend.py:353-355` sorting
  `tier_members("high")` to the FRONT of the voter pool. Fix: `providers.is_anthropic_route()` as
  the rule's single home (direct vendor + OpenRouter/`@anthropic-ai` namespacing + Bedrock
  `us.anthropic.*` + bare `claude-*`), enforced in the tier-voter, planner re-pointed at it. Guard
  is a cross-path AST check: every routing SELECTOR must compose the predicate and carry no vendor
  literal, so a new selector must join `_ROUTING_SELECTORS`. Revert-proof 4/4 (incl. a plausible
  bespoke `"anthropic" in prov_name` reimplementation → RED). ruff+mypy pass; 1878 tests pass.
  **OPEN SCOPE CALLS for the operator**: Anthropic WIRE support (`WIRE_ANTHROPIC`/`translate.py`/
  the preset) was deliberately NOT purged (it is the shipped SR-6 prompt-cache feature and
  describes HOW to speak to a provider, not a routing decision); `cli.py:847`
  `tier resolve --executor anthropic` left (user explicitly names the executor);
  `recommend._heuristic_rank` left (a catalog classifier that invokes nothing). Confirm or widen.
- ✅ **Budget/synthetic-source reconciled** — rig **PR #111 merged** (`88e4124`). **CORRECTION to
  §1 below**: "synthetic scores steer the budget TODAY" was NOT literally true — the ledger is 46
  rows, 100% `source=live`, zero bench. It was ARMED-AND-LATENT. The real severity: **0 of 46 live
  rows carry `time_s`** while `run.sh:140` DOES record it, so the first benchmark run would have
  made synthetic rows the ONLY timed rows and flipped the budget to ~100% synthetic-derived wearing
  a `derived` label. Fixed as one SSOT (`grades.py` exports `REAL_OUTCOME_SOURCES`; budget-derive
  imports it; failed import is FATAL — guessing wide IS the bug). Budget verified byte-identical.
- ✅ **Board repaired** (`15396a7`): retired 3 merged tickets; dep-sequenced the `charon-run.sh`
  owns-collision that was RED-blocking every land.

### 🔴 NEW CLASS BUG FOUND — VERIFY-MERGED-REPO-AWARE (not fixed, high value)
`_lib.sh verify_merged` hardcodes `VERIFY_MERGED_REPO_DEFAULT=/home/stack/code/charon` and **never
reads a ticket's `repo:` field**. Every `repo: charon-private` ticket is therefore merge-verified
against the PRODUCT repo, always fails, and **can never be retired**. Proof: SALVAGE-STASH-CHARON-RUN's
done marker literally reads `merged:#83`, rig PR #83 IS merged, yet `retire-done.sh` reports "NOT
merge-verified" and holds it — which is exactly how it ended up colliding with a live ticket and
blocking all landing. Same shape for SETUP-KEY-UX, SR-1, SR-2, SR-5, SR-5b, SR-7, SR-8. **This is
why done tickets accumulate as active (90 by 2026-07-10).** verify_merged gates DESTRUCTIVE actions,
so fix carefully + adversarially. Owns: `fleet/_lib.sh` (+ test).

### 🟡 Two pre-existing reds needing a ticket (found by the budget fix, proven on pristine master)
`fleet/capability/selftest.py` — `IndexError` on `money_rank[-1]`; `fleet/tests/assign-dispatch.test.sh`
— 3 failed ("real-outcome pick leads the resolved chain", `--print-model` empty). They use the frozen
fixture, so these are genuine failures in the assign/grades ranking path (likely the
eval-system-under-repair work). Not folded into the money-path fix to keep its blast radius honest.

### 🟡 Gate caveat discovered
`validate_board.sh` is only board-accurate in the LIVE tree: `fleet/state/` is gitignored, so a
git worktree has no `state/done/`, and done tickets read as live → false WCI-redundancy REDs. Land
rig branches gating against `/home/stack/charon-private`, not the worktree.

---

## 1. ACT ON THESE FIRST (live defects, ranked)

### 🔴 Synthetic benchmark scores are steering the real budget TODAY
Standing rule [[benchmark-not-a-valid-ranker]] says synthetic S0-S6 is SMOKE-TEST ONLY and must
never rank models. It is being violated right now, on the money path:
- `fleet/capability/grades.py:339` — `_REAL_OUTCOME_SOURCES = {"live"}`
- `fleet/benchmark/budget-derive.py:245` — **same constant name**, value `{"live","bench","bench2"}`
- budget-derive gates on the WIDE set (`:254`), and `run.sh:140` sets no stage (grep count 0) so
  synthetic rows default to `active` → synthetic `bench/MERGE` rows are KNOWN-GOOD for budget.

**Proposed ticket: BUDGET-SOURCE-RECONCILE.** Independently urgent — does NOT need #26 or the
stage work. Evidence: `bench-provisional-deepdive.md`.

### 🔴 Two rig PRs would cause damage if merged — DO NOT LAND AS-IS
- **#101 GITHUB-LIMITS-HARDENING** — `gh pr list -r` is not a valid flag → the cache is never
  written → `done.sh` owns-match silently REFUSES. It would break **done-marking itself**, i.e.
  the mechanism that unblocks dependents. 19/19 tests pass because every test uses fixtures and
  never hits the real gh call. Fix: `-r`→`-q`, plus a non-fixture cache test.
- **#103 DROID-LIFECYCLE-REAP** — `reap-orphans.sh:155-162`: an unresolvable base ref yields
  `unique=0` → DEAD+CLEAN → `git branch -D` **deletes committed work**. Reproduced verbatim; the
  reaper never fetches. This is the exact strand class this session just recovered from. Fix:
  fail-closed the unique-commit check + fetch, at BOTH call sites. 40/40 tests pass because
  fixtures always resolve the base.
- **#105 ASSIGN-DISPATCH-PICK-FIX** — the swap at `assign.py:528` sits in `main()` = the whole CLI,
  silently un-gating F13 for `assign.sh`. Its docstring claims a separate F13-gated consumer that
  does not exist. Fix: gate on `args.candidates` (the discriminator already exists).

### 🔴 #104 MEMORY-INDEX-COMPACTION — do not merge
Its hook guts the real memory index: strips the summary from **every** entry (118 entries,
20045B→8172B), because it treats text after `)` as leaked detail while the index's by-design form
IS `- [Title](file.md) — summary`. Its "no hardcoded path" test asserts against
`/dev/null/should-not-be-touched`, which can never exist → passes unconditionally. The review log
cites both as proof of success. Evidence: `review-104.md`.

### 🟡 never-Anthropic narrowed on master — OPERATOR RULED: regression, fix gateway-wide
`src/charon/decompose_planner.py:281-283` (landed via #168) states *"SG-never-Anthropic is a
planner-only invariant — the gateway's tier-voter is allowed Anthropic"*. Operator confirmed
2026-07-16 this is a REGRESSION, not a deliberate narrowing: **the rule is gateway-wide**. Purge
Anthropic from the tier-voter path too and add a guard test ([[sg-never-anthropic]] notes it
"keeps regressing" — mechanize the guard). NOT yet done.

### 🟡 #170 — its cycle is FAKE; do not let it degrade product code
`_scan_module_level_imports` uses `ast.walk`, which descends into function bodies despite the name
and docstring. Two of three cycle edges are DEFERRED imports (`providers.py:260` literally reads
`# deferred: config has no reverse dependency on this module`; `keyprobe.py:41`). `import
charon.config` works fine — no runtime cycle. The `api↔decompose` cycle it "fixes" is also fake.
Worse, its 2-line src fix hardcodes `state_dir: str = ".charon"` over `api.DEFAULT_STATE_DIR` — an
SSOT break leaving `decompose.py` the lone divergent default against 8 sites. **Proven fix**:
skip function bodies in the scanner + keep #170's correct per-alias change → `arch OK` on pure
master src with ZERO source edits. #170 should revert `decompose.py` entirely.

---

## 2. DONE THIS SESSION

- **Recovery**: crash-trail notes committed; **`fee3eb8` rescued** — the STARTUP-CONTEXT-DIET
  implementation was on a local-only branch on NO remote ref and would have died with the disk.
  Now rig PR #108, **merged**.
- **PARKED class bug — FIXED + LANDED (PR #109, `fa07ca0`)**. `parked:` was tested as the literal
  `true|yes|1` in 4 independent implementations, so a park written as PROSE parsed as UNPARKED.
  BENCH-PROVISIONAL-SCORING's "Do NOT route to an SG droid" never held — an SG droid claimed it
  anyway and built its 425-line design (PR #107). Fixed in `_lib.sh` (canonical predicate),
  `claim.sh` (inside the perf-critical awk), `status.sh` (new PARKED state), `foreman.sh`,
  `launch-plan.sh`. Two tests, both fail-on-revert verified: `parked-semantics.test.sh` (extracts
  claim.sh's LIVE rule rather than transcribing it; exec's launch-plan's python for real) and
  `parked-claim-e2e.test.sh` (dogfood: real claim.sh, real droids, drains a synthetic board).
- **Starvation root cause**: 9 tickets were loop-guard QUARANTINED by the crash — each client crash
  looked like a zero-commit release, so loop-guard blamed the tickets. Cleared 4.
- **Merged**: rig #98, #99, #94, #92, #106 (Foreman), #108; product #166.
- **BENCH-PROVISIONAL-SCORING**: directive corrected per operator — it is CLAUDE-reserved
  (manager's own bg session), NOT operator-led. Deliberately did NOT add a `claude_reserved:`
  field: nothing reads it (grep: zero consumers), so it would be decoration that looks like a
  guard and isn't — the identical failure mode to the prose park.
- **PRICE-REFRESHER** un-parked (operator-approved; its `parked: true` contradicted its own note
  "READY, runs now" and its condition was already met). Claimed by a live droid.

---

## 3. WORK POOL — proposed tickets (audit-harvest, all board-grepped, no existing coverage)

Sweep result: ALREADY-FIXED 9 / ALREADY-TICKETED 13 / STALE-WRONG 7. Details: `audit-harvest.md`.

| # | id | tier | why |
|---|---|---|---|
| 1 | GH-SEAM-CHOKEPOINT | strong | 15 direct `gh`/`git fetch` sites in 8 files bypass gh-cache. Real boot fail recorded (GraphQL 0/5000). Quota exhaustion blocks ALL landing+boot. depends_on GITHUB-LIMITS-HARDENING |
| 2 | INERT-INSTANCE-DETECT | strong | 6 gateway modules have 0 call sites yet `check_inert_code` runs GREEN → WORK-GATE-UNIVERSAL Gate B ships green-but-blind. ONE detector fix ≫ 6 wiring tickets |
| 3 | SELFCHECK-CYCLE-GATE | strong | guard exists but no fail-on-revert test; 12 other tests shell out to real fleet scripts. This cycle hit ~18,900 procs |
| 4 | METER-DOC-RECONCILE | economy | 10 lines in proxy.py assert the ledger is empty/deferred while the meter is LIVE — source of the standing "meter is inert" belief |
| 5 | FT-LIMITS-GROQ-RECONCILE | economy | row self-flagged `MISMATCH_UNRECONCILED`; groq = biggest free tier (14,400 RPD) |
| 6 | AVAILABILITY-WRITETHROUGH | strong | fleet parked-state and product `models.json enabled` never sync → routes to models known dead |
| 7 | HANDOFF-ROOT-ARCHIVE | economy | the Jul-10 root HANDOFF.md actively misled THIS crash recovery |

Plus from the deep-dive: **STAGE-DEMUX** (daemon must honor requested stage) — #26 is NOT actually
unblocked by the design as claimed, since the daemon can't distinguish an OOB verdict.

**Tier depth is the bottleneck**: frontier 0 / economy 0 ready → tabs idle. These refill 3 strong
+ 3 economy.

---

## 4. CLASS ISSUES (fix the class, not the instances)

1. **Board-field predicates are re-parsed per consumer and drift.** `parked` was 4 copies, wrong in
   4. `_lib.sh`'s own header already promised to prevent exactly this ("so the gating scripts
   can't diverge again", audit 2026-06-27) — `parked` was simply never moved into it. Same shape:
   every script carries its own `meta()`/`field()`; `tier`, `depends_on`, `owns`, `note` all carry
   the same exposure. Audit-harvest independently found the identical shape in the GH seam
   (finding #1). **Proposed: a gate that fails any fleet script implementing its own board parsing.**
   Check SSOT-DRIFT-GATE (#97) first — it may already be the right home.
2. **Two park mechanisms.** `.md.parked` (rename) is correct-by-construction — the file stops
   matching `board/*.md`, so every tool honors it for free; it never broke. The `parked:` field
   needs N implementations and broke in 4/4. **Proposed convergence: gate on the rename, keep a
   `parked:` field inside the file for the REASON, have display glob both.** Migration = 6 tickets.
   **Proposed: PARK-CONVENTION-UNIFY.**
3. **CLAUDE-RESERVED-ROUTING** — nothing expresses "not for SG droids, but DO work it via Claude".
   `parked` means nobody works it, which a Claude-reserved ticket must never be
   ([[claude-reserved-tickets-manager-builds]]). This gap is why an SG droid built #107.
4. **Fixtures that always pass.** #101 (19/19), #103 (40/40), #104's `/dev/null` assert, and the
   deep-dive's `test_capture_pipeline.py:122/165` all pass green while the real path is broken.
   Green is not proof — every one of these needed a non-fixture path exercised.

---

## 5. STATE AT HANDOFF

- rig master: pushed, clean. product master: `75fcd12`.
- 5 droid tabs live; 3 working (PRICE-REFRESHER, CAPTURE-WIRING-TIMEOUT-FIX, GRACEFUL-DEGRADE),
  2 idle for lack of frontier/economy work.
- Backup tag `backup/recover-20260716-d7c2f17` holds the pre-surgery state.
- Still quarantined (stale, not on board): DEDUP-GRAPHS-LEDGERS, LAUNCH-PLAN-GATE,
  PRICING-LIMITS-CHECKER, PROVIDER-CATALOG-REFRESH, SESSION-CTX-PROPAGATE.
- `land-push.sh` printed "pushing 'master'" + success while pushing NOTHING (it pushed the local
  `master` ref while HEAD was on a feature branch). The SessionStart hook compounded it by
  reporting "1 unpushed commit vs origin/master" and advising `push origin master`. **Not fixed.**
