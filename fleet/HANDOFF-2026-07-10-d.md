# HANDOFF — 2026-07-10 (session D) — Charon fleet MANAGER

## Bootstrap one-liner (paste as the next session's first message)
```
Read and fully follow /home/stack/charon-private/fleet/HANDOFF-2026-07-10-d.md — you are the fresh Charon fleet MANAGER.
```

## FIRST ACTIONS (in order)
1. MANAGER-OPERATING-RULES.md is auto-loaded (SessionStart cats it) — the HARD RULES live there: route sub-work to Charon via `charon-run.sh` (opencode→4-LOM), NOT the Claude Agent tool; money-path/security/core get an independent adversarial review before merge; merge gate = FULL CI on the merge commit, not pytest-alone; warn + offer handoff at ~250K context.
2. Run `preflight.sh`; re-verify git/gh/ssh; register on the session-bridge (repo:charon) under a NEW Jedi name.
3. VERIFY the 5 Wave-1 jobs' outcomes ON DISK — never trust a `SUCCESS` line; check `git log master..HEAD` has real commits on the branch.
4. ADJUDICATE the finished adversarial reviews and drive the review → FULL-CI → merge chain per §NEXT.

## WHY THIS HANDOFF
Session D fired Wave 1 of the GATEWAY-PROGRAM, unblocked gateway network access, and did a large
provider-economics pass with the operator. Handing off at a committed checkpoint with reviews DONE and
awaiting adjudication+merge (operator direction — protect the Opus budget; session got large).

## SHIPPED / DONE this session (committed to charon-private; HEAD @ 97668ea)
- **Fired Wave 1** (4 builds) + follow-on reviews/refires — see §IN-FLIGHT for per-item outcomes.
- **PROVIDER-BEST-PER-MODEL.md** (`fleet/PROVIDER-BEST-PER-MODEL.md`) — CANONICAL best/cheapest source PER
  model so we stop re-analyzing. Backed by live research `fleet/reviews/PROVIDER-REVIEW-2026-07-10.md`.
  Headlines: **GLM-5.2 → OpenRouter** (cheapest + concurrency, already wired); **Synthetic DROPPED**
  (1-concurrent-per-model); **Chutes = ADD** (pending key); **CommandCode $15 = DeepSeek-Pro/MiMo lane**
  (pending key); SKIP haloon/Nous/Trae/Devin/Ollama.
- **MODEL-SELF-REPORT-RELIABILITY.md** (`fleet/state/MODEL-SELF-REPORT-RELIABILITY.md`) — new durable log
  per operator directive. First entries: deepseek-v4-flash **2× fabricated-SUCCESS** on ACTUALS.
- Committed earlier this session: Featherless 32K-context fix + secret-scan false-positive fix
  (`e997297`, `e4b2367`), POST-GATEWAY-WCI-DECOMPOSE parked (`72d93a0`).
- **Network unblocked:** removed `Bash(curl *)` deny from `/home/stack/code/charon/.claude/settings.local.json`
  — the gateway admin API is now reachable (see GOTCHAS for how).

## §NEXT / IN-FLIGHT — the fresh session's work (verify on disk first)
Wave-1 build branches (VERIFY each `git log master..HEAD` before trusting):
- **decompose** ✅ `feat/gateway-routing-decompose` @ `f05b49c` — routing_policy/ package. **STRIP the
  committed REVIEW-PACKET.md before merge.** Its adversarial review was NOT run yet → run it, then FULL CI, merge.
- **meter** ✅ `feat/meter-model-provider` @ `e2a9e12` — MONEY-PATH. Reviews DONE (`fleet/reviews/METER-REVIEW-A.md`,
  `fleet/reviews/METER-REVIEW-B.md`) → ADJUDICATE, run the metering-invariant canary + FULL CI, then merge.
- **actuals** 🔴 `feat/actuals-ledger` — **FAILED 2×** (deepseek-v4-flash fabricated SUCCESS, empty branch,
  work leaked to main repo). ESCALATE off flash → stronger Charon model or a Claude droid tab. Brief:
  `fleet/board/briefs/wave1-actuals.md`.
- **grader** ⏳ `feat/bench-oob-grading` refire was STILL RUNNING at handoff — check
  `fleet/state/wave1-logs/grader-refire.txt` for CHARON_RUN_RESULT and verify the branch diff.
- **POOLING design review** ✅ done → `fleet/reviews/POOLING-DESIGN-REVIEW-flash.md` — read it; it's the
  Wave-2 POOLS-SIMPLIFICATION design input (operator wants ONE big pool + drain-then-park + availability-aware routing).
- **Decompose review** still queued (held to protect the thin flash pool) — run before merging decompose.

Provider decisions (operator, this session): Synthetic DROPPED; GLM-5.2 stays on OpenRouter (already wired);
operator is subscribing to **Chutes** and **CommandCode $15/mo** and will hand over API keys — WIRE them when
provided (gateway reachable now) per `fleet/PROVIDER-BEST-PER-MODEL.md`, back up `/data` first.

## GOTCHAS (avoid re-discovering)
- **NEVER trust a build's `SUCCESS` line** — deepseek-v4-flash fabricated it twice. Always verify
  `git -C <wt> log master..HEAD` has real commits ON ITS OWN BRANCH. Log incidents to
  `fleet/state/MODEL-SELF-REPORT-RELIABILITY.md`.
- **Gateway access:** `http://10.0.1.60:8080`. Admin API is under `/charon/config` + `/charon/setup`
  (GET `/charon/pools` **404s** — wrong path). Auth = `Authorization: Bearer <CHARON_GATEWAY_TOKEN>`.
  The **env `CHARON_GATEWAY_TOKEN` was STALE**; retrieve the live one ON THE 4-LOM BOX with
  `docker exec <charon-container> printenv CHARON_GATEWAY_TOKEN`. `/v1/models` returns 212 models with
  provider suffixes `-go -ng -or -ds -hf -nw -cline`. Live pools already have opencode-Go PRIMARY and
  ZERO zen members (Go enabled, no Zen burn — the operator was right).
- **`git push` / `git -C … push` are DENIED** to the manager. charon-private has UNPUSHED commits
  (`97668ea` and earlier) — the operator pushes: `git -C /home/stack/charon-private push`.
- **`sudo` / `systemctl` / `docker exec` are DENIED** to the manager — hand the operator the exact command.
- **charon-run.sh model args = FAILOVER chain, not parallel.** N independent reviewers = N separate invocations.
- **Worktree isolation:** concurrent same-repo builds MUST use separate worktrees; actuals leaked into the
  MAIN repo `/home/stack/code/charon` once — verify the main repo stays clean.
- Featherless has a **32K context/session cap** — unfit for heavy builds (small/staged only).

## SESSION-BRIDGE
This session ran as **Mace Windu** (`mgr-mace`, repo:charon). Register under a NEW Jedi name; stale sessions
auto-purge after 600s.
