# HANDOFF — 2026-07-10 (fresh Charon fleet MANAGER — lean restart to shed context cost)

## Bootstrap one-liner
```
Read and fully follow /home/stack/charon-private/fleet/HANDOFF-2026-07-10.md — you are the fresh Charon fleet MANAGER. Load MANAGER-OPERATING-RULES.md (SessionStart cats it). Run preflight.sh; re-verify git/gh/ssh; register on the session-bridge (repo:charon) under a Jedi name. Then continue per this handoff. HARD RULES THIS ERA: (1) route sub-work to Charon (opencode→4-LOM) not Claude — launching Claude droids/sub-agents spends the operator's Claude limit; only use Claude with explicit OK; (2) warn + offer handoff at ~250K context; (3) adversarial review before merge on money-path; (4) merge gate = full CI, not pytest.
```

## WHY THIS HANDOFF (cost lesson — read)
The 2026-07-09 session drained the Claude monthly limit during a perceived "pause." Consumers: **~1M+ tokens across ~15 manager Claude sub-agents**, **3 opus/sonnet droid tabs** (incl. a ~9-session BENCH-OOB parked-loop, now fixed), and **the long manager session itself** (Opus re-reads full context each turn). The spend limit is a CEILING (stops work only after budget is gone). → Route sub-work to Charon; keep the session lean; hand off early. See memory `route-work-to-charon-not-claude`.

## SHIPPED / DONE this session
- **v0.4.1 deployed to live 4-LOM** (drift-guard reconciled: deepseek-v4-pro expects cline-pass; CLINE_PASS_API_KEY required). SR-13 auth verified.
- **Merged (adversarially reviewed):** #88 BILLING-EST-COST-FIX (kills fabricated est_cost floor → the $223 phantom was ~100% fiction), #89 NORMALIZE-CASE-QUANT-FIX (case/quant model-id + #30 detector; SR-1 double-bill NOT reintroduced).
- **Loop-guard engine fix committed+pushed** (claim.sh skips `parked: true` + quarantines zero-commit re-claims; fleet-droid loop-guard; validate/preflight checks; tests). Fixes the parked-loop token waste.
- **Routing reprioritized LIVE** (setup-API, rollbacks in scratch/): opencode-go cheap-first on glm-5.2/kimi-k2.6/deepseek-v4-pro/deepseek-v4-flash (nanogpt fallback); minimax-m3 stays nanogpt (go has no catalog entry); cline-pass→spill (envelope bug DEFUSED for real traffic); opencode-zen deactivated but retained; NeuralWatt kept enabled to drain (blocked until normalize deployed).

## OPEN PRs — review + merge (gate = full CI; money-path = adversarial review)
- **#91 RESPONSE-ADAPTER-UNIVERSAL** — universal ResponseAdapter (Identity default + Cline unwrap, non-stream). Money-path → needs adversarial review before merge.
- **#92 TEST-HARDEN-CONTRACT** — the FIXED redo of #87. VERIFY the 2 rejection defects are fixed: (a) anthropic no longer fed a fabricated OpenAI mock (self-mirroring), (b) mock-lint actually wired into `charon.cli gate` as an error.
- **#90 CI-WORKFLOW-POLICY-GATE** — backlog drain; review for value/completeness.
- **#93 ACTION-PIN-POLICY** — "droid exited without committing → launcher auto-commit"; review for completeness (may be partial).

## DESIGNS READY (review → build; route builds to Charon)
- **ADR-BENCH-OOB-GRADING** (Q1=A): grader-daemon+drop-spool, bench-grader unix user on LOCAL box, 0700 keys. **#20 is actually DONE** (stale `.parked`); #26 build-ready after ADR review + operator runs ~8 root setup cmds (in ADR). Q2/Q3 gate #25 only (recs in ADR). BENCH-OOB-GRADING ticket stays parked until then.
- **ADR-DECOMPOSED-BY-DESIGN** — reviewed **REVISE (near-ratify)**. Fixes before HARD: force-decompose cli.py (2043) now + ratchet grace (hot-file trap); keep owner-count ADVISORY. Build **Lever 1 (Module-Layout section) + Lever 2 (line budget 400 WARN/600 HARD) first**, Lever 3 advisory, defer Lever 4 (scaffolder). Review: scratch/review-adr-decomposed-by-design.md.
- **WCI plan** (scratch/designed-work-concurrency-plan.md): critical path `BILLING✓→METER→DRAIN-ROUTING→COST-RANK-AUTO→FREE-TIER-SPILL+DRAIN-THEN-PARK(FOLDED)`. Parallel lanes off spine: METER, OPENROUTER-FLAKINESS-FIX, CAPABILITY-ENGINE, RFL-2→RFL-4. gateway.py decompose DEFERRED (GATEWAY-ROUTING-DECOMPOSE, parked).

## BOARD-PREP INCOMPLETE (died on spend limit) — REDO
Operator approved: FOLD FREE-TIER-QUOTA-SPILL+DRAIN-THEN-PARK; +2 frontier (METER, OpenRouter) +1 strong (Capability). NOT done: create METER ticket, fold, un-park, produce tab commands, open DETECTOR-30-WIRE ticket, sweep 6 orphan done-markers. Plan: scratch/board-prep-launch.md was not written. **Prefer routing these builds to Charon/opencode, not Claude droids.**

## QUEUED OPS
- **Deploy v0.4.2** (with #88+#89, then adapter/test-harden) to 4-LOM → **then reset prod /data/spend.json** (clears the $223 phantom for real) → **then flip routing to credit-drain-first** (NeuralWatt $22 / OpenRouter $9.90 / DeepSeek $9.99 / Together $9.83 ahead of flat-fee; needs normalize deployed + OpenRouter fix).
- Clean big-pickle pool's stale zen leg (not one of the 5).
- Triage ~100 untracked design docs in fleet/ (durability/clutter).
- Commit the pending charon-private board/scratch state (many untracked).

## DECISIONS PENDING FROM OPERATOR
- Bench Q2 (reds corpus scope) / Q3 (check_cmd bar) — recs in ADR-BENCH-OOB-GRADING.
- Run the ~8 root bench-grader setup commands (in ADR) on the LOCAL box.
- Confirm force-decompose cli.py now (review rec: yes).

## STANDING DIRECTIVES (memory, new/updated this session)
`route-work-to-charon-not-claude` · `decomposed-by-design-not-reactive` · `charon-drain-then-park-provider-class` (funding classes + balances + Featherless 32K/conc=1 + opencode-go=flat/zen=inactive + don't-orphan-pool guard) · `charon-bench-grader-substrate` (Q1=A) · `do-not-auto-stop-subsessions`.

## GOTCHAS
- PLAIN git only (`cd <repo> && git …`); push via `land-push.sh master /home/stack/charon-private` (AUTONOMOUS flag on).
- gh must run from the PRODUCT repo dir (or `--repo SLOP-Platform/charon`) — running gh from charon-private targets the wrong repo (bit me on `gh pr close`).
- Merge flow: `gh pr ready N` → `gh pr merge N --squash` → `fleet/done.sh <TICKET>`. `--delete-branch` fails while a worktree pins the branch → prune the worktree after.
- claim.sh now skips `parked: true`; park a ticket by adding that field (survives release, unlike a claims-marker).
- WORK-ROUTING-TO-CHARON-ENGINE ticket exists (parked) — build it to mechanize route-to-Charon.
