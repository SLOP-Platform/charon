# Charon Manager HANDOFF — 2026-07-05

> Bootstrap: `Read and fully follow /home/stack/charon-private/fleet/HANDOFF-2026-07-05.md — you are the fresh Charon fleet MANAGER. Run the first-acts (preflight.sh, model-scorecard.sh --due). Re-verify state via git/gh/ssh. Gate/merge/push/dialogue only; never build product in-session; never spawn droids (operator opens opencode tabs). Keep auto-compact ON; sub-sessions return pointers not dumps; read narrow slices. Move slowly, one instruction at a time; never ignore pre-existing red (investigate+ground, don't guess).`

## DONE THIS SESSION
- **Token-burn diagnosed+fixed:** root cause was `autoCompactEnabled:false` (global settings) → long manager sessions never compacted. Operator flipped it ON (settings.json L77=true). Enshrined: memory `manager-context-burn-control`; context-discipline rules baked into START-SESSION.md / BOOTSTRAP.md / handoff.sh; `SETTINGS-GUARD-PROPOSAL.md` = operator-to-apply SessionStart guard (still UNAPPLIED — hand operator the snippet).
- **#5 `strip-reasoning-content` MERGED** to origin/master (`f79a898..2b443aa`), gate green + 1196 tests. Request-normalizer strips `reasoning_content` → unblocks cross-provider multi-turn.
- **#17 `balance-drain`**: already merged (ancestor of master) → branch+worktree deleted. FOLLOW-UP: balances are in-memory only; **persistence gap** (restart resets a drained provider → routes to exhausted). Must be a hard req on the DRAIN proxy-wiring ticket (persist to /data).
- **Branch cleanup (grounded via file-diff triage, `reviews/BRANCH-TRIAGE.md`):** deleted `feat/tool-repair` (byte-identical on master) + `feat/wci-semantic-slice` (superseded; unique line was an unwanted `DEFAULT_TIER` conflict). Main tree switched to master.
- **Build-rig shipped (fleet repo, pushed):** `model-scorecard.sh` + `model-scorecard.tsv` (model×work-class ledger, seeded 3 rows, `--due` anti-rot, relocated out of gitignored state/); `MODEL-BENCHMARK-SPEC.md` (tiered 0-100 model benchmark, 6 sections, S2 = the #6 catcher); `SESSION-SR6-FIX.md`.

## IN-FLIGHT (sub-sessions PAUSED/KILLED by operator to save context — RESUME these)
1. **SR-6 re-review** (DeepSeek redo of #6 on `feat/sr6-auto-cost-rank`, worktree `charon-sr6`) — **INCONCLUSIVE, DO NOT MERGE YET.** Killed mid-investigation. Partial signal: worktree gate green + 1206 tests, BUT the reviewer was about to prove the committed branch is **STILL INERT** (committed `add_model` may still stamp 1000 → derivation never fires → the production-path test would FAIL on committed code). MUST finish this check before any merge. If still inert → BLOCK again. Decider unchanged: does the new test exercise the real models.json path AND fail on buggy code? Record in scorecard w/ op-note "needed nudge / no visible progress in opencode".
2. **Salvage** before deleting `feat/global-fallback-provider` — killed mid-work. Partial: CONNECT-OMP-WSL test on the branch FAILS on master (its source lives on separate `feat/connect-omp-wsl`) → defer, don't extract here. `tools/check_public_clean.py` salvage status UNKNOWN — re-run to confirm before deleting the branch. Do NOT delete `feat/global-fallback-provider` until salvage confirms SAFE-TO-DELETE: YES.
3. **Reds Registry** — **DONE + PUSHED** (`fleet/reds.tsv` + `preflight.sh`, wired into first-acts + grounded-rec line). Real scan of 7 seeds: 4 STILL-RED (board-validator, handoff-pipefail-mask, version-gate-false-positive, failover-401), 2 NOW-GREEN (bridge-daemon-down — daemon PID 6709 answers on `/tmp/charon-bridge.sock`; silent-downgrade-double-bill — SR-1 rsplit fix already on master, **contradicts P0 memory `charon-silent-downgrade-leak` → confirm & close + update memory**), 1 MANUAL (gpt55-pool-order, needs ssh). Close-refuses-without-evidence verified.

## NEXT (priority order)
1. Land SR-6 if re-review clean; else send tightened fix back (droid tab / Claude sub-session).
2. Commit+push the reds registry; then run `preflight.sh` and start closing reds with grounded check_cmds.
3. **Tracked reds** (seeded in registry): `handoff-pipefail-mask` (quick), `failover-401-not-classified` (P1 live-impact) + `gpt55-pool-order` (live 4-LOM reorder to NanoGPT-primary), `silent-downgrade-double-bill` (P0), `board-validator-red`, `version-gate-false-positive`, `bridge-daemon-down` (operator wants fixed — Claude Code MCP register fails vs stale daemon.py).
4. DeepSeek #6 fix, if landed, closes the routing/cost path → then DRAIN proxy-wiring (with persistence req) + cost_rank/cost_class already largely in.
5. **Model tiering:** scorecard + benchmark now exist; accumulate rows, don't mechanize tiering on n=3. Benchmark = tier calibration; scorecard = drift. Track code-quality AND operational-autonomy separately (DeepSeek needed nudging).

## STATE / RULES
- origin/master = `2b443aa`. Fleet repo `Nnyan/charon-private` (PRIVATE) pushed through `35ebb61`.
- Push ONLY via `bash /home/stack/charon-private/fleet/land-push.sh <branch> [repo]` (AUTONOMOUS flag ON). Merge to master in an isolated `git worktree add … master`.
- Gate = BOTH `PYTHONPATH=src python3 -m charon.cli gate` AND `PYTHONPATH=src python3 -m pytest -q`.
- Product ships standalone (no fleet/SLOP/runner/`/home/stack` in src/committed config). fleet repo is PRIVATE build-rig.
- Held product branches NOT triaged/kept: `feat/global-fallback-provider` (delete after salvage). Others (dtc/*, obs-ui, prod-install, console-provider-mgmt, model-discovery, tier-select, wci-mvp, connect-omp-wsl, ci-smoke*, sr-2/sr-10/sr-1 worktrees, yoda-session3) NOT yet triaged.
- Memories written this session: `manager-context-burn-control`. Update MEMORY.md index if adding more.
