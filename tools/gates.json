[
  {
    "id": "boundary-check",
    "name": "host-project boundary check",
    "domain": "boundary",
    "enforcer": "tools/check_boundary.py",
    "covers": "no host-project/build-rig references in src/",
    "ci_step": true,
    "invariant": "All src/ paths are product-clean",
    "red_proof": "tests/test_boundary.py",
    "min_work_units": 50,
    "work_units": "python files under src/ parsed for host-project references"
  },
  {
    "id": "version-check",
    "name": "Version consistency",
    "domain": "version",
    "enforcer": "tools/check_version.py",
    "covers": "single source of truth for version in pyproject.toml",
    "ci_step": true,
    "invariant": "Installed package version matches pyproject.toml",
    "red_proof": null,
    "min_work_units": 50,
    "work_units": "python files under src/ scanned for __version__ literals"
  },
  {
    "id": "ruff-lint",
    "name": "Ruff lint",
    "domain": "lint",
    "enforcer": "ruff check",
    "covers": "Python code style and correctness",
    "ci_step": true,
    "invariant": "Python code conforms to Ruff lint rules",
    "red_proof": null
  },
  {
    "id": "mypy-type",
    "name": "Mypy static type checking",
    "domain": "type",
    "enforcer": "mypy src tests",
    "covers": "static type correctness across src/ and tests/",
    "ci_step": true,
    "invariant": "All Python code passes static type checking",
    "red_proof": null
  },
  {
    "id": "pytest",
    "name": "Pytest test suite",
    "domain": "test",
    "enforcer": "python3 -m pytest",
    "covers": "all functional and regression test assertions",
    "ci_step": true,
    "invariant": "All tests pass with PYTHONPATH=src",
    "red_proof": null,
    "min_work_units": 1500,
    "work_units": "test cases collected by pytest"
  },
  {
      "id": "diff-cover",
      "name": "Diff-coverage gate",
      "domain": "diff-coverage",
      "enforcer": "python3 tools/diff_cover_gate.py",
      "covers": "every line this change adds to src/ must be executed by the test suite",
      "ci_step": true,
      "invariant": "No added src/ line is unexercised",
      "red_proof": "tests/test_diff_cover_mutmut_gate.py",
      "min_work_units": 0,
      "work_units": "added executable src/ lines analysed by diff-cover",
      "work_units_note": "DIFF-SCOPED by design: 0 is the correct and PROVEN count on a trunk build or a change that touches no src/**.py. The gate prints the merge-base SHA and the changed-file list as evidence before it reports 0, and fails closed on every other route to an empty measurement (unresolvable base, untracked source, unmeasured file, diff-cover analysing fewer lines than the diff contains)."
    },
    {
      "id": "mutmut",
      "name": "Mutation gate (diff-scoped)",
      "domain": "mutation",
      "enforcer": "python3 tools/mutmut_diff_gate.py",
      "covers": "every changed src/ file must have a test that kills its mutants",
      "ci_step": false,
      "invariant": "No mutant of a changed src/ file survives",
      "red_proof": "tests/test_diff_cover_mutmut_gate.py",
      "min_work_units": 0,
      "work_units": "mutants generated and tested for the changed src/ files",
      "work_units_note": "DIFF-SCOPED by design: 0 is the correct and PROVEN count on a trunk build or a change that touches no src/**.py. Once a src/**.py does change, a 0-mutant run is treated as a MISCONFIGURATION and fails closed -- mutmut exits 0 even when mutants survive, so the count is the only honest signal.",
      "cadence_note": "NOT a PR gate (ci_step:false), on cost and on a live blocker, both MEASURED 2026-08-04. COST: mutmut re-runs the whole suite single-process to collect its baseline stats before a single mutant runs -- 78s -- and one 8-line changed function (charon.gitutil.commit_all) generates 29 mutants, each of which re-runs the tests covering it. A typical multi-function PR is minutes, against a 20-minute CI job budget and a repo directive that treats latency as a failure class. BLOCKER: mutmut runs the suite inside its own mutants/ sandbox, which has no .git and re-enters the gate scripts, so the suite is RED there and the gate can currently only ever fail closed (measured: 1 failed, 426 passed, 16.7s). The gate LOGIC is proven correct end-to-end on a fixture -- RED on a surviving mutant, GREEN on a real assertion (tests/test_diff_cover_mutmut_gate.py). Wiring it to a nightly cadence needs the sandbox suite green FIRST; a scheduled job that is guaranteed red is a gate nobody reads."
    },
  {
    "id": "validate-board",
    "optional": true,
    "name": "Fleet board validation",
    "domain": "fleet",
    "enforcer": "../charon-private/fleet/validate_board.sh",
    "covers": "fleet board file integrity and consistency",
    "ci_step": false,
    "invariant": "Fleet board files are well-formed and consistent",
    "red_proof": null
  },
  {
    "id": "check-arch",
    "name": "Architecture boundary checker",
    "domain": "arch",
    "enforcer": "tools/check_arch.py",
    "covers": "architecture layer isolation, circular imports, product-clean",
    "ci_step": true,
    "invariant": "Engine never imports gateway, no circular imports, product-clean",
    "red_proof": "tests/test_check_arch.py",
    "min_work_units": 50,
    "work_units": "python files under src/ parsed for layer/import violations"
  },
  {
    "id": "check-decisions",
    "name": "Decision register linter",
    "domain": "decisions",
    "enforcer": "tools/check_decisions.py",
    "covers": "DECISIONS.md register integrity and cross-references",
    "ci_step": true,
    "invariant": "Decision register is monotonic, well-formed, and cross-references are valid",
    "red_proof": "tests/test_check_decisions.py",
    "min_work_units": 5,
    "work_units": "decision rows in docs/DECISIONS.md linted"
  },
  {
    "id": "render-review-log",
    "name": "Review log staleness check",
    "domain": "docs",
    "enforcer": "tools/render_review_log.py",
    "covers": "docs/REVIEW-LOG.md freshness vs fragment directory",
    "ci_step": true,
    "invariant": "Review log rollup matches its source fragments",
    "red_proof": "tests/test_render_review_log.py",
    "min_work_units": 5,
    "work_units": "review-log fragments rendered"
  },
  {
    "id": "gate-registry",
    "name": "Gate registry self-validator",
    "domain": "registry",
    "enforcer": "tools/check_gate_registry.py",
    "covers": "gate registry consistency and coverage completeness",
    "ci_step": true,
    "invariant": "Every gate has a living enforcer and no two gates overlap",
    "red_proof": null,
    "min_work_units": 10,
    "work_units": "gate entries in tools/gates.json validated"
  },
  {
    "id": "security-scan",
    "name": "Security anti-pattern scanner",
    "domain": "security",
    "enforcer": "tools/check_security.py",
    "covers": "bare excepts, secrets in source, hardcoded IPs, dangerous eval/exec/shell=True",
    "ci_step": true,
    "invariant": "No source file silently swallows exceptions, embeds secrets, hardcodes IPs, or uses eval/exec/subprocess-shell",
    "red_proof": "tests/test_check_security.py",
    "min_work_units": 50,
    "work_units": "python files under src/ AST-walked for anti-patterns"
  },
  {
    "id": "test-patterns",
    "name": "Test-pattern enforcement gate",
    "domain": "test-patterns",
    "enforcer": "tools/check_test_patterns.py",
    "covers": "duplicate test names, missing docstrings, parametrize ratio, test function line count",
    "ci_step": true,
    "invariant": "All test files are free of duplicate names and encouraged toward parametrization and documentation",
    "red_proof": "tests/test_check_test_patterns.py",
    "min_work_units": 50,
    "work_units": "test_*.py files under tests/ parsed"
  },
  {
    "id": "dogfood-gate",
    "name": "Dogfood gate — real-routing-path observable effects",
    "domain": "dogfood",
    "enforcer": "tools/check_dogfood.py",
    "covers": "observable routing effects through the real load_pools/choose_from_pool selection path (pool ordering, selection decisions, exhaustion, code-safe filtering)",
    "ci_step": true,
    "invariant": "The real routing path produces observable effects on every surface an operator inspects (pool ordering, selection, failover, code-safe filtering, cost-class priority); a zero-catalog is RED",
    "red_proof": "tests/e2e/test_dogfood_gate.py",
    "min_work_units": 9,
    "work_units": "observable routing assertions exercised against real config"
  },
  {
    "id": "charon-gate",
    "name": "Unified CHARON GATE runner",
    "domain": "gate",
    "enforcer": "python3 -m charon.cli gate",
    "covers": "all validation checks in one command (ruff check, mypy, boundary, version, gate-registry)",
    "ci_step": true,
    "invariant": "Gate passes iff all sub-checks pass",
    "red_proof": null
  },
  {
    "id": "workflow-policy",
    "name": "CI workflow policy gate",
    "domain": "ci-infra",
    "enforcer": "tools/check_workflows.py",
    "covers": "action-ref pin policy, fragile Windows smoke patterns, packaging-trigger path scoping",
    "ci_step": true,
    "invariant": "Every .github/workflows/*.yml conforms to the action-pin/Windows-smoke/path-trigger policy",
    "red_proof": "tests/test_check_workflows.py",
    "min_work_units": 1,
    "work_units": "CI workflow files under .github/workflows/ scanned"
  },
  {
    "id": "public-clean",
    "name": "Public-clean info-leak guard",
    "domain": "public-clean",
    "enforcer": "tools/check_public_clean.py",
    "covers": "personal/internal tokens (internal IPs, home paths, rig/host names, hex secrets) in tracked files",
    "ci_step": true,
    "invariant": "No tracked file contains unallowlisted personal/internal info",
    "red_proof": "tests/test_public_clean.py",
    "min_work_units": 100,
    "work_units": "git-tracked files scanned for personal/internal patterns"
  },
  {
    "id": "no-rig-import-check",
    "name": "no-rig-import guard",
    "domain": "no-rig-import",
    "enforcer": "tools/check_no_rig_import.py",
    "covers": "product hot path never imports the rig grader (benchmark/grader_daemon)",
    "ci_step": true,
    "invariant": "no src/charon/ module imports benchmark or grader_daemon (outside privileged-loop zones)",
    "red_proof": "tests/test_workclass_taxonomy.py",
    "min_work_units": 50,
    "work_units": "python files under src/ scanned for rig imports"
  },
  {
    "id": "inert-code",
    "name": "Inert-code (dead/unwired symbol) detector",
    "domain": "inert",
    "enforcer": "tools/check_inert_code.py",
    "covers": "0-caller unreachable public symbols in src/charon (built but never wired in)",
    "ci_step": true,
    "invariant": "Every 0-caller public symbol in src/charon is either reachable, @inert_by_design, or tracked in tools/inert-code-disposition.json with a {reason, disposition} entry",
    "red_proof": "tests/test_check_inert_code.py",
    "min_work_units": 50,
    "work_units": "python files under src/ walked for public symbols"
  },
  {
    "id": "catalog-case-quant",
    "name": "Catalog case/quant mismatch detector",
    "domain": "catalog",
    "enforcer": "tools/check_catalog_case_quant.py",
    "covers": "catalog model ids are canonical (no case mismatches, no quantization suffix, no namespace prefix, no normalization collisions)",
    "ci_step": true,
    "invariant": "Every catalog entry has a canonical id; no two entries collide under normalization",
    "red_proof": "tests/test_catalog_gate_wire.py",
    "min_work_units": 5,
    "work_units": "catalog model ids compared"
  }
]
