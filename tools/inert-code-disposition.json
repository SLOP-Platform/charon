{
  "charon.egress.nginx_credproxy_template_stub": {
    "disposition": "keep-pending-phase2-credproxy",
    "reason": "Deliberately inert Phase-2 placeholder. The credential-injecting nginx reverse proxy (Track C custody) is the NEXT unit after this Phase-1 egress-allowlist work; this stub returns a TODO string and is intentionally not wired until custody moves out of the process. Exercised by tests/test_egress_allowlist.py::test_nginx_credproxy_template_is_an_inert_phase2_stub, which asserts it stays inert."
  },
  "charon.api.list_ledgers": {
    "disposition": "keep-detector-false-positive-uvicorn-string-load",
    "reason": "Called from charon.service.app (api.list_ledgers()) but that call site doesn't count: service/app.py is loaded by uvicorn via the string 'charon.service.app:app' (see service/__main__.py), which is invisible to static AST reachability, so the whole module reads as unreachable and this call cascades as a false positive."
  },
  "charon.api.show_config": {
    "disposition": "keep-detector-false-positive-uvicorn-string-load",
    "reason": "Same cascading false positive as charon.api.list_ledgers: called from charon.service.app (api.show_config()), a module only reachable via uvicorn's string-based ASGI load."
  },
  "charon.cache.format_stats": {
    "disposition": "keep-detector-false-positive-module-unreachable-cascade",
    "reason": "Referenced 3x in src/tests (import/instantiate/annotate/test) but flagged dead via the detector's module-unreachable cascade \u2014 false positive, not dead code."
  },
  "charon.context_shaper.ShapeResult": {
    "disposition": "keep-pending-wiring-rider",
    "reason": "RFL-5 context_shaper is an EXPERIMENTAL, OPT-IN, OFF-BY-DEFAULT compaction module intentionally NOT wired into proxy_server.py yet \u2014 the wiring is a documented follow-on rider (see the module's '## Wiring rider' note). Its public surface is exercised by tests/test_context_shaper.py but has no production caller BY DESIGN until the rider lands; not dead code."
  },
  "charon.context_shaper.ShaperConfig": {
    "disposition": "keep-pending-wiring-rider",
    "reason": "RFL-5 context_shaper is an EXPERIMENTAL, OPT-IN, OFF-BY-DEFAULT compaction module intentionally NOT wired into proxy_server.py yet \u2014 the wiring is a documented follow-on rider (see the module's '## Wiring rider' note). Its public surface is exercised by tests/test_context_shaper.py but has no production caller BY DESIGN until the rider lands; not dead code."
  },
  "charon.context_shaper.estimate_message_tokens": {
    "disposition": "keep-pending-wiring-rider",
    "reason": "RFL-5 context_shaper is an EXPERIMENTAL, OPT-IN, OFF-BY-DEFAULT compaction module intentionally NOT wired into proxy_server.py yet \u2014 the wiring is a documented follow-on rider (see the module's '## Wiring rider' note). Its public surface is exercised by tests/test_context_shaper.py but has no production caller BY DESIGN until the rider lands; not dead code."
  },
  "charon.context_shaper.estimate_messages_tokens": {
    "disposition": "keep-pending-wiring-rider",
    "reason": "RFL-5 context_shaper is an EXPERIMENTAL, OPT-IN, OFF-BY-DEFAULT compaction module intentionally NOT wired into proxy_server.py yet \u2014 the wiring is a documented follow-on rider (see the module's '## Wiring rider' note). Its public surface is exercised by tests/test_context_shaper.py but has no production caller BY DESIGN until the rider lands; not dead code."
  },
  "charon.context_shaper.estimate_tokens": {
    "disposition": "keep-pending-wiring-rider",
    "reason": "RFL-5 context_shaper is an EXPERIMENTAL, OPT-IN, OFF-BY-DEFAULT compaction module intentionally NOT wired into proxy_server.py yet \u2014 the wiring is a documented follow-on rider (see the module's '## Wiring rider' note). Its public surface is exercised by tests/test_context_shaper.py but has no production caller BY DESIGN until the rider lands; not dead code."
  },
  "charon.context_shaper.rank_sentences": {
    "disposition": "keep-pending-wiring-rider",
    "reason": "RFL-5 context_shaper is an EXPERIMENTAL, OPT-IN, OFF-BY-DEFAULT compaction module intentionally NOT wired into proxy_server.py yet \u2014 the wiring is a documented follow-on rider (see the module's '## Wiring rider' note). Its public surface is exercised by tests/test_context_shaper.py but has no production caller BY DESIGN until the rider lands; not dead code."
  },
  "charon.context_shaper.shape": {
    "disposition": "keep-pending-wiring-rider",
    "reason": "RFL-5 context_shaper is an EXPERIMENTAL, OPT-IN, OFF-BY-DEFAULT compaction module intentionally NOT wired into proxy_server.py yet \u2014 the wiring is a documented follow-on rider (see the module's '## Wiring rider' note). Its public surface is exercised by tests/test_context_shaper.py but has no production caller BY DESIGN until the rider lands; not dead code."
  },
  "charon.context_shaper.summarize": {
    "disposition": "keep-pending-wiring-rider",
    "reason": "RFL-5 context_shaper is an EXPERIMENTAL, OPT-IN, OFF-BY-DEFAULT compaction module intentionally NOT wired into proxy_server.py yet \u2014 the wiring is a documented follow-on rider (see the module's '## Wiring rider' note). Its public surface is exercised by tests/test_context_shaper.py but has no production caller BY DESIGN until the rider lands; not dead code."
  },
  "charon.discover.discover_models": {
    "disposition": "keep-detector-false-positive-module-unreachable-cascade",
    "reason": "Referenced 21x in src/tests (import/instantiate/annotate/test) but flagged dead via the detector's module-unreachable cascade \u2014 false positive, not dead code."
  },
  "charon.discover.load_cost_map": {
    "disposition": "keep-detector-false-positive-module-unreachable-cascade",
    "reason": "Referenced 4x in src/tests (import/instantiate/annotate/test) but flagged dead via the detector's module-unreachable cascade \u2014 false positive, not dead code."
  },
  "charon.doctor.probe_handoff": {
    "disposition": "keep-detector-false-positive-module-unreachable-cascade",
    "reason": "Referenced 10x in src/tests (import/instantiate/annotate/test) but flagged dead via the detector's module-unreachable cascade \u2014 false positive, not dead code."
  },
  "charon.engine.reconcile.FindingKind": {
    "disposition": "keep-detector-false-positive-module-unreachable-cascade",
    "reason": "Referenced 30x in src/tests (import/instantiate/annotate/test) but flagged dead via the detector's module-unreachable cascade \u2014 false positive, not dead code."
  },
  "charon.engine.reconcile.ReconcileFinding": {
    "disposition": "keep-pending-decision",
    "reason": "Part of the original hand-rolled dead-code audit (engine/reconcile.py + board.set_cert() flagged unwired). set_cert() itself is now test-referenced so the detector no longer flags it, but this dataclass has zero callers anywhere. Whether reconcile.py should be wired into the coordinator or removed is an open call, not made here."
  },
  "charon.engine.reconcile.Severity": {
    "disposition": "keep-detector-false-positive-module-unreachable-cascade",
    "reason": "Referenced 9x in src/tests (import/instantiate/annotate/test) but flagged dead via the detector's module-unreachable cascade \u2014 false positive, not dead code."
  },
  "charon.engine.reconcile.reconcile_static": {
    "disposition": "keep-detector-false-positive-module-unreachable-cascade",
    "reason": "Referenced 26x in src/tests (import/instantiate/annotate/test) but flagged dead via the detector's module-unreachable cascade \u2014 false positive, not dead code."
  },
  "charon.engine.semantic_proof.IndependenceCertificate": {
    "disposition": "keep-detector-false-positive-module-unreachable-cascade",
    "reason": "Referenced 8x in src/tests (import/instantiate/annotate/test) but flagged dead via the detector's module-unreachable cascade \u2014 false positive, not dead code."
  },
  "charon.engine.semantic_proof.compute_certificate": {
    "disposition": "keep-detector-false-positive-module-unreachable-cascade",
    "reason": "Referenced 22x in src/tests (import/instantiate/annotate/test) but flagged dead via the detector's module-unreachable cascade \u2014 false positive, not dead code."
  },
  "charon.failover.ReviewerCircuitBreaker": {
    "disposition": "keep-detector-false-positive-module-unreachable-cascade",
    "reason": "Referenced 12x in src/tests (import/instantiate/annotate/test) but flagged dead via the detector's module-unreachable cascade \u2014 false positive, not dead code."
  },
  "charon.failover.next_entry": {
    "disposition": "keep-detector-false-positive-module-unreachable-cascade",
    "reason": "Referenced 6x in src/tests (import/instantiate/annotate/test) but flagged dead via the detector's module-unreachable cascade \u2014 false positive, not dead code."
  },
  "charon.failover.proxy_excluded_keys": {
    "disposition": "keep-detector-false-positive-module-unreachable-cascade",
    "reason": "Referenced 3x in src/tests (import/instantiate/annotate/test) but flagged dead via the detector's module-unreachable cascade \u2014 false positive, not dead code."
  },
  "charon.handoff.is_resumable": {
    "disposition": "keep-needs-triage",
    "reason": "No callers found anywhere in src/ or tests/ (grepped the bare name). Genuinely unreferenced; needs a human wire-or-delete call."
  },
  "charon.handoff.rehydrate_remaining": {
    "disposition": "keep-needs-triage",
    "reason": "No callers found anywhere in src/ or tests/. Genuinely unreferenced; needs a human wire-or-delete call."
  },
  "charon.intake.autonomous_intake": {
    "disposition": "keep-needs-triage",
    "reason": "No callers found anywhere in src/ or tests/. Genuinely unreferenced; needs a human wire-or-delete call."
  },
  "charon.intake.register_adapter": {
    "disposition": "keep-needs-triage",
    "reason": "No callers found anywhere in src/ or tests/. Genuinely unreferenced; needs a human wire-or-delete call."
  },
  "charon.land.acceptance_from": {
    "disposition": "keep-needs-triage",
    "reason": "No callers found anywhere in src/ or tests/. Genuinely unreferenced; needs a human wire-or-delete call."
  },
  "charon.land.auto_land_batch": {
    "disposition": "keep-needs-triage",
    "reason": "No callers found anywhere in src/ or tests/. Genuinely unreferenced; needs a human wire-or-delete call."
  },
  "charon.land.propose_pr": {
    "disposition": "keep-detector-false-positive-attribute-value-ref",
    "reason": "Actually wired: src/charon/cli.py references it as a bare attribute value ('opener = pr_opener or land.propose_pr') rather than a direct call. The detector's value-ref collector only tracks bare Name references, not qualified attribute (module.attr) references, so this is a detector false positive, not dead code."
  },
  "charon.lifecycle.bootstrap": {
    "disposition": "keep-detector-false-positive-module-unreachable-cascade",
    "reason": "Referenced 27x in src/tests (import/instantiate/annotate/test) but flagged dead via the detector's module-unreachable cascade \u2014 false positive, not dead code."
  },
  "charon.lifecycle.scheduled_refresh": {
    "disposition": "keep-detector-false-positive-module-unreachable-cascade",
    "reason": "Referenced 11x in src/tests (import/instantiate/annotate/test) but flagged dead via the detector's module-unreachable cascade \u2014 false positive, not dead code."
  },
  "charon.lifecycle.subprocess_preflight_seam": {
    "disposition": "keep-needs-triage",
    "reason": "Every hit for this name outside its own def line is inside a docstring/comment string, not real code (':func:`subprocess_preflight_seam`' and a prose example string). No real caller found; needs a human wire-or-delete call."
  },
  "charon.pricing_limits_checker.CheckerConfig": {
    "disposition": "keep-detector-false-positive-module-unreachable-cascade",
    "reason": "Referenced 15x in src/tests (import/instantiate/annotate/test) but flagged dead via the detector's module-unreachable cascade \u2014 false positive, not dead code."
  },
  "charon.pricing_limits_checker.Finding": {
    "disposition": "delete",
    "reason": "Part of the original hand-rolled dead-code audit: pricing_limits_checker.py is a standalone script never imported by any production entrypoint. Slated for deletion."
  },
  "charon.pricing_limits_checker.ModelPriceSpec": {
    "disposition": "keep-detector-false-positive-module-unreachable-cascade",
    "reason": "Referenced 8x in src/tests (import/instantiate/annotate/test) but flagged dead via the detector's module-unreachable cascade \u2014 false positive, not dead code."
  },
  "charon.pricing_limits_checker.NonTokenPricing": {
    "disposition": "keep-detector-false-positive-module-unreachable-cascade",
    "reason": "Referenced 10x in src/tests (import/instantiate/annotate/test) but flagged dead via the detector's module-unreachable cascade \u2014 false positive, not dead code."
  },
  "charon.pricing_limits_checker.ProviderCanonical": {
    "disposition": "keep-detector-false-positive-module-unreachable-cascade",
    "reason": "Referenced 9x in src/tests (import/instantiate/annotate/test) but flagged dead via the detector's module-unreachable cascade \u2014 false positive, not dead code."
  },
  "charon.pricing_limits_checker.ProviderLimitSpec": {
    "disposition": "keep-detector-false-positive-module-unreachable-cascade",
    "reason": "Referenced 5x in src/tests (import/instantiate/annotate/test) but flagged dead via the detector's module-unreachable cascade \u2014 false positive, not dead code."
  },
  "charon.pricing_limits_checker.check_pricing_limits": {
    "disposition": "delete",
    "reason": "Same module as charon.pricing_limits_checker.Finding \u2014 unwired standalone script, slated for deletion per the original audit."
  },
  "charon.pricing_limits_checker.findings_to_dicts": {
    "disposition": "delete",
    "reason": "Same module as charon.pricing_limits_checker.Finding \u2014 unwired standalone script, slated for deletion per the original audit."
  },
  "charon.pricing_limits_checker.load_canonical": {
    "disposition": "delete",
    "reason": "Same module as charon.pricing_limits_checker.Finding \u2014 unwired standalone script, slated for deletion per the original audit."
  },
  "charon.pricing_limits_checker.load_configured": {
    "disposition": "delete",
    "reason": "Same module as charon.pricing_limits_checker.Finding \u2014 unwired standalone script, slated for deletion per the original audit."
  },
  "charon.pricing_limits_checker.marginal_cost_per_token": {
    "disposition": "delete",
    "reason": "Same module as charon.pricing_limits_checker.Finding \u2014 unwired standalone script, slated for deletion per the original audit."
  },
  "charon.pricing_limits_checker.run_check": {
    "disposition": "delete",
    "reason": "Same module as charon.pricing_limits_checker.Finding \u2014 unwired standalone script, slated for deletion per the original audit."
  },
  "charon.pricing_limits_checker.save_canonical": {
    "disposition": "delete",
    "reason": "Same module as charon.pricing_limits_checker.Finding \u2014 unwired standalone script, slated for deletion per the original audit."
  },
  "charon.quota.QuotaTracker": {
    "disposition": "keep-detector-false-positive-module-unreachable-cascade",
    "reason": "Referenced 24x in src/tests (import/instantiate/annotate/test) but flagged dead via the detector's module-unreachable cascade \u2014 false positive, not dead code."
  },
  "charon.scanners.benchmark_scanners": {
    "disposition": "keep-detector-false-positive-module-unreachable-cascade",
    "reason": "Referenced 7x in src/tests (import/instantiate/annotate/test) but flagged dead via the detector's module-unreachable cascade \u2014 false positive, not dead code."
  },
  "charon.service.app.RunRequest": {
    "disposition": "keep-detector-false-positive-module-unreachable-cascade",
    "reason": "Referenced 8x in src/tests (import/instantiate/annotate/test) but flagged dead via the detector's module-unreachable cascade \u2014 false positive, not dead code."
  },
  "charon.service.app.create_run": {
    "disposition": "keep-detector-false-positive-uvicorn-string-load",
    "reason": "FastAPI route handler (@app.post) in a module loaded by uvicorn via the string 'charon.service.app:app' (service/__main__.py), which the AST reachability graph can't trace (no static import edge into app.py). Real production HTTP surface, not dead code \u2014 a detector limitation with decorator/string-based ASGI wiring."
  },
  "charon.service.app.dashboard": {
    "disposition": "keep-detector-false-positive-uvicorn-string-load",
    "reason": "Same as charon.service.app.create_run: FastAPI route in the uvicorn string-loaded app module."
  },
  "charon.service.app.get_config": {
    "disposition": "keep-detector-false-positive-uvicorn-string-load",
    "reason": "Same as charon.service.app.create_run: FastAPI route in the uvicorn string-loaded app module."
  },
  "charon.service.app.get_run": {
    "disposition": "keep-detector-false-positive-uvicorn-string-load",
    "reason": "Same as charon.service.app.create_run: FastAPI route in the uvicorn string-loaded app module."
  },
  "charon.service.app.healthz": {
    "disposition": "keep-detector-false-positive-uvicorn-string-load",
    "reason": "Same as charon.service.app.create_run: FastAPI route in the uvicorn string-loaded app module."
  },
  "charon.service.app.list_runs": {
    "disposition": "keep-detector-false-positive-uvicorn-string-load",
    "reason": "Same as charon.service.app.create_run: FastAPI route in the uvicorn string-loaded app module."
  },
  "charon.service.app.require_token": {
    "disposition": "keep-detector-false-positive-uvicorn-string-load",
    "reason": "FastAPI dependency function wired in via Depends(require_token) inside the same uvicorn string-loaded app module \u2014 same detector limitation as the routes above."
  },
  "charon.service.get_app": {
    "disposition": "keep-needs-triage",
    "reason": "Public factory function (service/__init__.py) with no internal caller found; may be an external embedding surface. Needs a human wire-or-delete/keep-public-API call."
  },
  "charon.tool_repair.RepairResult": {
    "disposition": "wire",
    "reason": "Part of the original hand-rolled dead-code audit: tool_repair.py is imported only by tests (grepped \u2014 no src/ import), never wired into any production entrypoint. ToolCallRepair itself no longer flags because tests reference it directly by name, but this dataclass has zero references outside its own file. Slated to be wired in."
  },
  "charon.tool_repair.ToolCallRepair": {
    "disposition": "keep-detector-false-positive-module-unreachable-cascade",
    "reason": "Referenced 27x in src/tests (import/instantiate/annotate/test) but flagged dead via the detector's module-unreachable cascade \u2014 false positive, not dead code."
  },
  "charon.types.GuardrailViolation": {
    "disposition": "keep-detector-false-positive-module-unreachable-cascade",
    "reason": "Referenced 11x in src/tests (import/instantiate/annotate/test) but flagged dead via the detector's module-unreachable cascade \u2014 false positive, not dead code."
  },
  "charon.types.ObsEvent": {
    "disposition": "keep-detector-false-positive-module-unreachable-cascade",
    "reason": "Referenced 21x in src/tests (import/instantiate/annotate/test) but flagged dead via the detector's module-unreachable cascade \u2014 false positive, not dead code."
  },
  "charon.types.ObsTarget": {
    "disposition": "keep-detector-false-positive-module-unreachable-cascade",
    "reason": "Referenced 25x in src/tests (import/instantiate/annotate/test) but flagged dead via the detector's module-unreachable cascade \u2014 false positive, not dead code."
  },
  "charon.types.QualityRecord": {
    "disposition": "keep-detector-false-positive-module-unreachable-cascade",
    "reason": "Referenced 5x in src/tests (import/instantiate/annotate/test) but flagged dead via the detector's module-unreachable cascade \u2014 false positive, not dead code."
  },
  "charon.types.RequestHints": {
    "disposition": "keep-detector-false-positive-module-unreachable-cascade",
    "reason": "Referenced 3x in src/tests (import/instantiate/annotate/test) but flagged dead via the detector's module-unreachable cascade \u2014 false positive, not dead code."
  },
  "charon.types.SpendDecision": {
    "disposition": "keep-detector-false-positive-module-unreachable-cascade",
    "reason": "Referenced 8x in src/tests (import/instantiate/annotate/test) but flagged dead via the detector's module-unreachable cascade \u2014 false positive, not dead code."
  },
  "charon.request_inspector.RequestInspector": {
    "disposition": "wire",
    "reason": "Instance-inert (R43-WIRING-AUDIT row 9, SR-4 re-confirmed): constructed by the _MODULE_SPECS table at gateway.py:88-89, stored at proxy_server.py:577 via self.modules.get(\"request_inspector\"), and inspect() is never reached from _handle() -> forward_with_failover(). WIRE \u2014 this is the ONLY one of the six with a REAL planned consumer, so it is the one case where the retire default is overturned: ticket RFL-3 (PARKED, not archived, so still live work) and its written brief specifies vision-aware route exclusion built directly on srv.request_inspector.inspect(messages).has_images, and 6 of 8 RFL-3 dogfood attempts independently produced that exact call site in forwarder.py (diff + provenance captured in the dogfood-salvage record on the ticket (build-rig doc, out of tree) before the dogfood-worktree reap). RFL-3's change IS the WIRING-AUDIT-MATRIX intervention for this module (insert the inspect() call pre-forward alongside the guardrails scan). Cost to wire: ~28 lines in forwarder.py, already drafted six times. Cost to retire instead: lose the only image/token/tool request-shape detection the gateway has, and strand RFL-3. The wiring itself is downstream work \u2014 not done here (ticket DO (c)).",
    "inert_class": true
  },
  "charon.observability.Observability": {
    "disposition": "retire",
    "reason": "Instance-inert (R43-WIRING-AUDIT row 11): constructed at gateway.py:80-81, stored at proxy_server.py:574, and export()/get_metrics() have ZERO call sites in src/charon/. NO REAL CONSUMER: the wiring map assigns \"downstream owner R44 (dogfood-gate)\", but R44 HAS NO BOARD TICKET (the ticket board contains no R44 entry \u2014 it survives only as a \"next\" roadmap row in old session-handoff reports), and its scope, \"e2e merge-gate: real-config request asserts observable effects\", is a TEST GATE, not a caller of this module. the unified-reconciliation-gate design records R44 as SUBSUMED into the unified reconciliation gate \u2014 folded, not scheduled. DOES ANYTHING EXPECT IT TO BE LIVE? No \u2014 but the NAME collides dangerously: the gateway's REAL, WIRED observability is GatewayProxy, stored as srv.observer (proxy.py:448-493, called on every served 200), a different object entirely. Every \"observability\" reference in api.py:282,340 and failover.py:59 means srv.observer, NOT this module. charon.types.ObsEvent/ObsTarget exist for this module's export contract but are separately dispositioned as cascade false positives; retiring this module should re-triage them. Retiring loses 150 lines of an export backend nothing feeds. Operator decision 2026-07-24 (INERT-INSTANCE-DETECT scope-split (b), decision #2): the default is RETIRE, overturned only by a REAL planned consumer found in the R43-WIRING-AUDIT INERT-to-WIRED map (build-rig doc, out of tree). NOTHING IS DELETED BY THIS PASS \u2014 retiring is destructive and needs a recorded operator sign-off line on the ticket (D3); the exact removal diff (files + line ranges) is specified in the INERT-SIX retire plan recorded on the ticket (build-rig doc, out of tree).",
    "inert_class": true
  },
  "charon.speculative_execution.SpeculativeExecutor": {
    "disposition": "retire",
    "reason": "Instance-inert (R43-WIRING-AUDIT row 12; SR-4 confirmed): constructed at gateway.py:92-95 (opt_in), stored at proxy_server.py:579, and execute() has ZERO call sites in src/charon/. NO REAL CONSUMER: the wiring map assigns \"downstream owner R45 (inert-startup-check)\", but R45 HAS NO BOARD TICKET (only a \"next\" roadmap row in old SESSION-HANDOFF reports), and its scope, \"startup self-check: active vs inert optional components (fail-loud)\", would REPORT this module as inert \u2014 it is the opposite of a consumer. the unified-reconciliation-gate design records R45 as SUBSUMED into the unified reconciliation gate \u2014 folded, not scheduled. DOES ANYTHING EXPECT IT TO BE LIVE? YES \u2014 THREE THINGS, AND ALL THREE ARE WRONG. (1) speculative.json {\"enabled\": true} is a real operator-facing ON SWITCH (opt_in gate at gateway.py:340): flipping it constructs the executor and changes NOTHING about request handling, silently. (2) gateway.py:320's own docstring calls speculative/consensus \"cost-multiplying features that need explicit opt-in\", which reads as if opting in turns them on. (3) docs/adr/0019-provider-key-egress-choke-point.md:166 lists speculative_execution.py as one of \"four key-bearing send sites\" the egress choke point must cover \u2014 a SECURITY ADR treating it as a live egress path, when it never sends anything. Ticket DESTIFF-SPECULATIVE (landed) also spent frontier-tier work hardening its upstream call with failover_loop.invoke_with_failover \u2014 maintenance investment in code that has never run. Retiring loses 237 lines of N-way racing fan-out; ADR-0019 and the gateway.py:320 docstring must be corrected in the same change. Operator decision 2026-07-24 (INERT-INSTANCE-DETECT scope-split (b), decision #2): the default is RETIRE, overturned only by a REAL planned consumer found in the R43-WIRING-AUDIT INERT-to-WIRED map (build-rig doc, out of tree). NOTHING IS DELETED BY THIS PASS \u2014 retiring is destructive and needs a recorded operator sign-off line on the ticket (D3); the exact removal diff (files + line ranges) is specified in the INERT-SIX retire plan recorded on the ticket (build-rig doc, out of tree).",
    "inert_class": true
  },
  "charon.session_affinity.SessionAffinity": {
    "disposition": "retire",
    "reason": "Instance-inert (R43-WIRING-AUDIT row 10): constructed at gateway.py:90-91, stored at proxy_server.py:578, and pin()/resolve()/clear()/touch()/cleanup()/size() have ZERO call sites in src/charon/. NO REAL CONSUMER: the wiring map assigns \"downstream owner R44 (dogfood-gate)\", but R44 HAS NO BOARD TICKET (the ticket board contains no R44 entry \u2014 it survives only as a \"next\" roadmap row in old session-handoff reports), and its scope, \"e2e merge-gate: real-config request asserts observable effects\", is a TEST GATE, not a caller of this module. the unified-reconciliation-gate design records R44 as SUBSUMED into the unified reconciliation gate \u2014 folded, not scheduled. DOES ANYTHING EXPECT SESSION PINNING TO BE LIVE? Partly yes, and that is the hazard: session_affinity.json is a live operator-facing config file whose ttl knob is read at gateway.py:91, so an operator can set a TTL and reasonably believe requests are being pinned to a provider \u2014 they are not, the map is written by nobody and read by nobody. No doc, ADR or README claims the feature. tests/test_session_affinity.py (102 lines) tests the class in isolation, which is why the suite stays green. Retiring loses 61 lines of an unused TTL map. Operator decision 2026-07-24 (INERT-INSTANCE-DETECT scope-split (b), decision #2): the default is RETIRE, overturned only by a REAL planned consumer found in the R43-WIRING-AUDIT INERT-to-WIRED map (build-rig doc, out of tree). NOTHING IS DELETED BY THIS PASS \u2014 retiring is destructive and needs a recorded operator sign-off line on the ticket (D3); the exact removal diff (files + line ranges) is specified in the INERT-SIX retire plan recorded on the ticket (build-rig doc, out of tree).",
    "inert_class": true
  },
  "charon.consensus.ConsensusRouter": {
    "disposition": "retire",
    "reason": "Instance-inert (R43-WIRING-AUDIT row 13; SR-4 confirmed): constructed at gateway.py:96-100 (opt_in), stored at proxy_server.py:580, and verify() has ZERO call sites in src/charon/. NO REAL CONSUMER: the wiring map assigns \"downstream owner R45 (inert-startup-check)\", but R45 HAS NO BOARD TICKET (only a \"next\" roadmap row in old SESSION-HANDOFF reports), and its scope, \"startup self-check: active vs inert optional components (fail-loud)\", would REPORT this module as inert \u2014 it is the opposite of a consumer. the unified-reconciliation-gate design records R45 as SUBSUMED into the unified reconciliation gate \u2014 folded, not scheduled. DOES ANYTHING EXPECT IT TO BE LIVE? YES, in the same shape as SpeculativeExecutor: consensus.json {\"enabled\": true} is a real ON SWITCH that silently does nothing, and gateway.py:320 advertises it as an opt-in feature. SEPARATELY \u2014 AND THIS IS THE MAIN CONFUSION RISK \u2014 the ~40 \"consensus\" hits across fence.py, coordinator.py, decompose.py and ports/reviewer.py are the AGENT-PLANE consensus gate (ADR-0003 \u00a76, autonomy L2 apply-with-consensus), a completely different and genuinely WIRED mechanism. Retiring this gateway module does NOT touch it. Retiring loses 60 lines of N-response similarity voting that has never run. Operator decision 2026-07-24 (INERT-INSTANCE-DETECT scope-split (b), decision #2): the default is RETIRE, overturned only by a REAL planned consumer found in the R43-WIRING-AUDIT INERT-to-WIRED map (build-rig doc, out of tree). NOTHING IS DELETED BY THIS PASS \u2014 retiring is destructive and needs a recorded operator sign-off line on the ticket (D3); the exact removal diff (files + line ranges) is specified in the INERT-SIX retire plan recorded on the ticket (build-rig doc, out of tree).",
    "inert_class": true
  },
  "charon.virtual_keys.VirtualKeyManager": {
    "disposition": "retire",
    "reason": "Instance-inert (R43-WIRING-AUDIT row 14): constructed at gateway.py:101-102, stored at proxy_server.py:581, and create()/resolve()/list_keys()/revoke() have ZERO call sites in src/charon/ \u2014 the gateway auth path never consults it, so a virtual key would not authenticate anything. NO REAL CONSUMER: the wiring map assigns \"downstream owner R44 (dogfood-gate)\", but R44 HAS NO BOARD TICKET (the ticket board contains no R44 entry \u2014 it survives only as a \"next\" roadmap row in old session-handoff reports), and its scope, \"e2e merge-gate: real-config request asserts observable effects\", is a TEST GATE, not a caller of this module. the unified-reconciliation-gate design records R44 as SUBSUMED into the unified reconciliation gate \u2014 folded, not scheduled. DOES ANYTHING EXPECT IT TO BE LIVE? YES \u2014 SHIPPED DOCUMENTATION DOES, AND IT IS FALSE: docs/docker.md:117 tells operators not to mount the config volume read-only because \"the quality scorer, the balance tracker's auto-park state, VIRTUAL KEYS, and the policy router\" persist files there while running. VirtualKeyManager takes state_dir at construction but nothing ever calls create(), so it never writes. That doc line must be corrected in the same change. Retiring loses 122 lines of an unreferenced key store; note that retiring REMOVES A SECURITY SURFACE that today provides no security, rather than removing protection. Operator decision 2026-07-24 (INERT-INSTANCE-DETECT scope-split (b), decision #2): the default is RETIRE, overturned only by a REAL planned consumer found in the R43-WIRING-AUDIT INERT-to-WIRED map (build-rig doc, out of tree). NOTHING IS DELETED BY THIS PASS \u2014 retiring is destructive and needs a recorded operator sign-off line on the ticket (D3); the exact removal diff (files + line ranges) is specified in the INERT-SIX retire plan recorded on the ticket (build-rig doc, out of tree).",
    "inert_class": true
  },
  "charon.capability.grades_import.GradesImport": {
    "disposition": "keep-detector-instance-inert-false-positive",
    "reason": "Instance-inert detector false positive: GradesImport has 5 callable methods (load_into, reconcile_with_real, is_provisional, provenance_of, real_samples) but none of their names appear in any instance-variable \u2014 the heuristic cannot distinguish method-name collisions from genuine invocations and errs on the side of not flagging. The class IS genuinely used in production; this entry documents the detector limitation.",
    "inert_class": true
  },
  "charon.capability.taxonomy.Classification": {
    "disposition": "keep-detector-instance-inert-false-positive",
    "reason": "Instance-inert detector false positive: Classification has a callable name() method (unusual for a frozen dataclass). The name() method is accessed as an attribute in practice (via dotted access like c.name), not called as c.name(...), but the detector's heuristic collects all dotted .method(...) patterns and cannot resolve attribute-vs-call without type information.",
    "inert_class": true
  },
  "charon.ports.agent_launch.OpencodeRenderer": {
    "disposition": "keep-detector-instance-inert-false-positive",
    "reason": "Instance-inert detector false positive: OpencodeRenderer has a callable render() method that IS invoked at runtime via the AgentRenderer protocol dispatch, but the invocation goes through a Protocol interface (caller holds AgentRenderer, not OpencodeRenderer) which the heuristic cannot resolve statically.",
    "inert_class": true
  },
  "charon.types.CapSet": {
    "disposition": "keep-detector-instance-inert-false-positive",
    "reason": "Instance-inert detector false positive: CapSet has a callable covers() method that IS invoked in production (capability/taxonomy.py and related modules), but the invocation pattern involves method calls on objects typed as the parent protocol/interface rather than the concrete CapSet type, which the heuristic cannot resolve statically.",
    "inert_class": true
  }
}
